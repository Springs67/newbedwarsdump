-- Decompiled with Potassium's decompiler.

local u1 = {};
u1.__index = u1;

function u1.new(p2, p3) -- Line: 21
    -- upvalues: u1 (copy)
    assert(p2 ~= nil, "Symbol ID cannot be nil");

    if p3 ~= nil then
        local v4 = u1.Is(p3);
        assert(v4, "Scope must be a Symbol or nil");
    end;

    return setmetatable({
        ClassName = "Symbol",
        _id = p2,
        _scope = p3
    }, u1);
end;

function u1.Is(p5) -- Line: 35
    -- upvalues: u1 (copy)
    local v6;

    if type(p5) == "table" then
        v6 = getmetatable(p5) == u1;
    else
        v6 = false;
    end;

    return v6;
end;

function u1.IsInScope(p7, p8) -- Line: 40
    -- upvalues: u1 (copy)
    local v9 = u1.Is(p7) and p7._scope == p8;

    return v9;
end;

function u1.__tostring(p10) -- Line: 45
    return ("Symbol<%s>"):format(p10._id);
end;

return u1;