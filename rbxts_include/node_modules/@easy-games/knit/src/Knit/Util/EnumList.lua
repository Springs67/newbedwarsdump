-- Decompiled with Potassium's decompiler.

local Symbol = require(script.Parent.Symbol);
local u1 = {};

function u1.new(u2, p3) -- Line: 26
    -- upvalues: Symbol (copy), u1 (copy)
    local v4 = Symbol.new(u2);
    local u5 = {};

    for _, v in ipairs(p3) do
        u5[v] = Symbol.new(v, v4);
    end;

    return setmetatable({
        _scope = v4
    }, {
        __index = function(p6, p7) -- Line: 35, Name: __index
            -- upvalues: u5 (copy), u1 (ref), u2 (copy)
            if u5[p7] then
                return u5[p7];
            end;

            if u1[p7] then
                return u1[p7];
            end;

            error("Unknown " .. u2 .. ": " .. tostring(p7), 2);
        end,

        __newindex = function() -- Line: 44, Name: __newindex
            -- upvalues: u2 (copy)
            error("Cannot add new " .. u2, 2);
        end
    });
end;

function u1.Is(p8, p9) -- Line: 52
    -- upvalues: Symbol (copy)
    return Symbol.IsInScope(p9, p8._scope);
end;

return u1;