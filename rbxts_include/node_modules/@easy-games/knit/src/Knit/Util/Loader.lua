-- Decompiled with Potassium's decompiler.

return {
    LoadChildren = function(p1) -- Line: 18, Name: LoadChildren
        local v2 = {};

        for _, child in ipairs(p1:GetChildren()) do
            if child:IsA("ModuleScript") then
                local v3 = require(child);
                table.insert(v2, v3);
            end;
        end;

        return v2;
    end,

    LoadDescendants = function(p4) -- Line: 30, Name: LoadDescendants
        local v5 = {};

        for _, descendant in ipairs(p4:GetDescendants()) do
            if descendant:IsA("ModuleScript") then
                local v6 = require(descendant);
                table.insert(v5, v6);
            end;
        end;

        return v5;
    end
};