-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Reflect = v1.import(script, v1.getModule(script, "@flamework", "core").out).Reflect;
local v2 = v1.import(script, v1.getModule(script, "@flamework", "core").out);
local Controller = v2.Controller;
local External = v2.External;
local LobbyClientConfig = v1.import(script, script.Parent.Parent, "config", "lobby-client-config").LobbyClientConfig;
local LobbyClientEvents = v1.import(script, script.Parent.Parent, "events", "events").LobbyClientEvents;
local QueueState = v1.import(script, script.Parent.Parent.Parent, "shared", "party", "queue-state").QueueState;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 13, Name: __tostring
        return "LobbyQueueController";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 18
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5, p6) -- Line: 22
    p5.notificationController = p6;
end;

function u3.onStart(p7) -- Line: 25
end;

function u3.joinQueue(p8, p9) -- Line: 27
    -- upvalues: LobbyClientConfig (copy), QueueState (copy), LobbyClientEvents (copy)
    local title = LobbyClientConfig.queueMeta[p9].title;

    if LobbyClientConfig.store:getState().Party.queueState ~= QueueState.NONE then
        p8.notificationController:sendErrorNotification({
            message = "You are already in another queue. Leave to join " .. title .. "."
        });

        return nil;
    end;

    LobbyClientEvents.joinQueue:fire({
        queueType = p9
    });
end;

function u3.leaveQueue(p10) -- Line: 40
    -- upvalues: LobbyClientEvents (copy)
    LobbyClientEvents.leaveQueue:fire();
end;

Reflect.defineMetadata(u3, "identifier", "@easy-games/lobby:client/controllers/lobby-queue-controller@LobbyQueueController");
Reflect.defineMetadata(u3, "flamework:parameters", { "@easy-games/game-core:client/controllers/notification-controller@NotificationController" });
Reflect.defineMetadata(u3, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u3, "$:flamework@Controller", Controller, { {} });
Reflect.decorate(u3, "$:flamework@External", External, {});

return {
    LobbyQueueController = u3
};