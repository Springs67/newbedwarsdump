-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Reflect = u1.import(script, u1.getModule(script, "@flamework", "core").out).Reflect;
local DeviceUtil = u1.import(script, u1.getModule(script, "@easy-games", "game-core").out).DeviceUtil;
local v2 = u1.import(script, u1.getModule(script, "@flamework", "core").out);
local Controller = v2.Controller;
local External = v2.External;
local v3 = u1.import(script, u1.getModule(script, "@rbxts", "services"));
local LocalizationService = v3.LocalizationService;
local Players = v3.Players;
local LobbyClientConfig = u1.import(script, script.Parent.Parent, "config", "lobby-client-config").LobbyClientConfig;
local LobbyClientEvents = u1.import(script, script.Parent.Parent, "events", "events").LobbyClientEvents;
local u4 = setmetatable({}, {
    __tostring = function() -- Line: 16, Name: __tostring
        return "LobbyClientController";
    end
});
u4.__index = u4;

function u4.new(...) -- Line: 21
    -- upvalues: u4 (ref)
    local v5 = setmetatable({}, u4);

    return v5:constructor(...) or v5;
end;

function u4.constructor(p6) -- Line: 25
end;

function u4.onStart(p7) -- Line: 27
    -- upvalues: LocalizationService (copy), DeviceUtil (copy), u1 (copy), Players (copy), LobbyClientEvents (copy)
    local SystemLocaleId = LocalizationService.SystemLocaleId;
    local v8 = DeviceUtil.guessCurrentDeviceType();
    local v9 = DeviceUtil.getInputDeviceType();
    local u10 = false;
    u1.try(function() -- Line: 32
        -- upvalues: u10 (ref), Players (ref)
        u10 = game:GetService("VoiceChatService"):IsVoiceEnabledForUserIdAsync(Players.LocalPlayer.UserId);
    end, function(p11) -- Line: 34
        warn(p11);
    end);
    LobbyClientEvents.replicateClientData({
        subRegion = "",
        region = SystemLocaleId,
        device = v8,
        inputDevice = v9,
        locale = SystemLocaleId,
        translationLocale = SystemLocaleId,
        voiceChatEnabled = u10
    });
end;

function u4.getQueueMeta(p12, p13) -- Line: 47
    -- upvalues: LobbyClientConfig (copy)
    return LobbyClientConfig.queueMeta[p13];
end;

Reflect.defineMetadata(u4, "identifier", "@easy-games/lobby:client/controllers/lobby-client-controller@LobbyClientController");
Reflect.defineMetadata(u4, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u4, "$:flamework@Controller", Controller, { {} });
Reflect.decorate(u4, "$:flamework@External", External, {});

return {
    LobbyClientController = u4
};