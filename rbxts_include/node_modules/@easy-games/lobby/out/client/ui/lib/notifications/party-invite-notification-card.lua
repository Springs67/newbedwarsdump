-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local v2 = v1.import(script, v1.getModule(script, "@easy-games", "game-core").out);
local Empty = v2.Empty;
local GameTheme = v2.GameTheme;
local ImageId = v2.ImageId;
local NotificationProgressBar = v2.NotificationProgressBar;
local NOTIFICATION_ASPECT_RATIO = v2.NOTIFICATION_ASPECT_RATIO;
local PlayerRender = v2.PlayerRender;
local SoundManager = v2.SoundManager;
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v4 = v1.import(script, v1.getModule(script, "@rbxts", "roact-rodux").src);
local LobbyClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "lobby-client-config").LobbyClientConfig;
local v5 = u3.Component:extend("PartyInviteNotificationCardBase");

function v5.init(p6) -- Line: 18
end;

function v5.didMount(p7) -- Line: 20
    -- upvalues: SoundManager (copy), LobbyClientConfig (copy)
    SoundManager:playSound(LobbyClientConfig.sounds.PARTY_INCOMING_INVITE);
end;

function v5.render(u8) -- Line: 23
    -- upvalues: u3 (copy), GameTheme (copy), Empty (copy), PlayerRender (copy), NOTIFICATION_ASPECT_RATIO (copy), ImageId (copy), NotificationProgressBar (copy)
    return u3.createElement("ImageButton", {
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        BackgroundColor3 = GameTheme.backgroundPrimary,
        AnchorPoint = Vector2.new(0.5, 1),
        Position = UDim2.fromScale(0.5, 1),

        [u3.Event.MouseButton1Click] = function() -- Line: 30
            -- upvalues: u8 (copy)
            u8.props:joinParty(u8.props.fromPlayer.userId);
            u8.props:removeNotification(true);
        end,

        [u3.Event.MouseEnter] = function() -- Line: 34
        end,

        [u3.Event.MouseLeave] = function() -- Line: 35
        end
    }, { u3.createElement("UICorner", {
            CornerRadius = UDim.new(0.1, 0)
        }), u3.createElement(Empty, {
            Size = UDim2.fromScale(1, 1)
        }, {
            u3.createElement("UIListLayout", {
                FillDirection = "Horizontal",
                VerticalAlignment = "Center"
            }),
            u3.createElement(PlayerRender, {
                SizeConstraint = "RelativeYY",
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                FallbackTransparency = 1,
                PlayerUserId = u8.props.fromPlayer.userId,
                Size = UDim2.fromScale(1, 1),
                AnchorPoint = Vector2.new(0.5, 1)
            }, { u3.createElement("UICorner", {
                    CornerRadius = UDim.new(0.1, 0)
                }) }),
            u3.createElement(Empty, {
                SizeConstraint = "RelativeYY",
                Size = UDim2.fromScale(NOTIFICATION_ASPECT_RATIO - 1 - 0.25 - 0.5 - 0.45, 1)
            }, {
                u3.createElement("UIListLayout", {
                    FillDirection = "Vertical",
                    VerticalAlignment = "Center"
                }),
                u3.createElement("UIPadding", {
                    PaddingLeft = UDim.new(0.1, 0),
                    PaddingRight = UDim.new(0.02, 0),
                    PaddingTop = UDim.new(0.1, 0),
                    PaddingBottom = UDim.new(0.04, 0)
                }),
                u3.createElement("TextLabel", {
                    Text = "<b>PARTY INVITE</b>",
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    TextXAlignment = "Left",
                    TextTransparency = 0.3,
                    TextScaled = true,
                    RichText = true,
                    Font = "Roboto",
                    Size = UDim2.fromScale(1, 0.3),
                    TextColor3 = Color3.fromRGB(255, 255, 255),
                    AnchorPoint = Vector2.new(0.5, 0.5)
                }, { u3.createElement("UIPadding", {
                        PaddingTop = UDim.new(0, 0)
                    }) }),
                u3.createElement("TextLabel", {
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    TextXAlignment = "Left",
                    TextScaled = true,
                    RichText = true,
                    Font = "Roboto",
                    AutoLocalize = false,
                    Text = u8.props.message,
                    Size = UDim2.fromScale(1, 0.6),
                    TextColor3 = Color3.fromRGB(255, 255, 255),
                    AnchorPoint = Vector2.new(0.5, 0.5)
                }, { u3.createElement("UIPadding", {
                        PaddingTop = UDim.new(0.08, 0),
                        PaddingBottom = UDim.new(0.08, 0)
                    }) })
            }),
            u3.createElement(Empty, {
                SizeConstraint = "RelativeYY",
                Size = UDim2.fromScale(0.25, 0)
            }),
            u3.createElement("ImageLabel", {
                SizeConstraint = "RelativeYY",
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                Image = ImageId.JOIN,
                Size = UDim2.fromScale(0.5, 0.5)
            }),
            u3.createElement(Empty, {
                SizeConstraint = "RelativeYY",
                Size = UDim2.fromScale(0.45, 0)
            })
        }), u3.createElement(NotificationProgressBar, {
            Size = UDim2.fromScale(0.69, 0.0625),
            AnchorPoint = Vector2.new(0, 1),
            Position = UDim2.fromScale(0.285, 0.96)
        }) });
end;

return {
    PartyInviteNotificationCard = v4.connect(function(p9, p10) -- Line: 134
        local v11 = {};

        for i, v in p10 do
            v11[i] = v;
        end;

        return v11;
    end, function(p12) -- Line: 140
        -- upvalues: Flamework (copy)
        return {
            joinParty = function(p13, p14) -- Line: 142, Name: joinParty
                -- upvalues: Flamework (ref)
                Flamework.resolveDependency("@easy-games/lobby:client/controllers/party-controller@PartyController"):joinParty(p14);
            end
        };
    end)(v5)
};