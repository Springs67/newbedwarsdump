-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = v1.import(script, v1.getModule(script, "@easy-games", "game-core").out);
local DeviceUtil = v2.DeviceUtil;
local Empty = v2.Empty;
local OfflinePlayerUtil = v2.OfflinePlayerUtil;
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v4 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local Players = v1.import(script, v1.getModule(script, "@rbxts", "services")).Players;
local ClientParty = v1.import(script, script.Parent.Parent.Parent.Parent, "lib", "client-party").ClientParty;
local LeavePartyButton = v1.import(script, script.Parent, "leave-party-button").LeavePartyButton;
local PartyMemberList = v1.import(script, script.Parent, "party-member-list").PartyMemberList;

return {
    PartyMembersTab = v4.new(u3)(function(u5, p6) -- Line: 13
        -- upvalues: ClientParty (copy), u3 (copy), PartyMemberList (copy), DeviceUtil (copy), OfflinePlayerUtil (copy), Players (copy), LeavePartyButton (copy), Empty (copy)
        local _ = p6.useState;
        local v7 = ClientParty.new(u5.Party);
        local v9 = { u3.createElement(
                "UIListLayout",
                {
                    FillDirection = "Vertical",
                    HorizontalAlignment = "Left",
                    SortOrder = "LayoutOrder"
                }
            ), u3.createElement(PartyMemberList, {
                LayoutOrder = 2,
                Size = UDim2.fromScale(1, 0.85),
                Party = u5.Party,

                OnRemovePlayer = function(p8) -- Line: 25, Name: OnRemovePlayer
                    -- upvalues: u5 (copy)
                    u5.RemovePlayer(p8);
                end
            }) };
        local v10 = {
            LayoutOrder = 3,
            Size = UDim2.fromScale(1, 0.15)
        };
        local v11 = { u3.createElement(
                "UIListLayout",
                {
                    FillDirection = "Vertical",
                    HorizontalAlignment = "Center",
                    VerticalAlignment = "Bottom"
                }
            ) };
        local v12 = #v11;
        local v13 = not (DeviceUtil.isHoarceKat() or v7:isLeader(OfflinePlayerUtil.getOfflinePlayer(Players.LocalPlayer))) and u3.createElement(LeavePartyButton);

        if v13 then
            v11[v12 + 1] = v13;
        end;

        v11[#v11 + 1] = u3.createElement(Empty, {
            Size = UDim2.fromScale(0, 0.3)
        });
        v9[#v9 + 1] = u3.createElement(Empty, v10, v11);

        return u3.createFragment(v9);
    end)
};