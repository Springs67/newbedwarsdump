-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = v1.import(script, v1.getModule(script, "@easy-games", "game-core").out);
local AutoCanvasScrollingFrame = v2.AutoCanvasScrollingFrame;
local Empty = v2.Empty;
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v4 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local PartyIncomingInviteCard = v1.import(script, script.Parent.Parent.Parent.Parent, "party-invites-ui", "party-incoming-invite-card").PartyIncomingInviteCard;

return {
    PartyInvitesTab = v4.new(u3)(function(p5, p6) -- Line: 9
        -- upvalues: u3 (copy), PartyIncomingInviteCard (copy), Empty (copy), AutoCanvasScrollingFrame (copy)
        local _ = p6.useState;
        local incomingInvites = p5.PartyInvites.incomingInvites;

        local function _(p7, p8) -- Line: 12
            -- upvalues: u3 (ref), PartyIncomingInviteCard (ref)
            return u3.createElement(PartyIncomingInviteCard, {
                Invite = p7,
                LayoutOrder = 2 * p8
            });
        end;

        local v9 = table.create(#incomingInvites);

        for i, v in incomingInvites do
            v9[i] = u3.createElement(PartyIncomingInviteCard, {
                Invite = v,
                LayoutOrder = 2 * (i - 1)
            });
        end;

        local v10 = { u3.createElement("UIListLayout", {
                FillDirection = "Vertical",
                HorizontalAlignment = "Left",
                VerticalAlignment = "Top",
                SortOrder = "LayoutOrder",
                Padding = UDim.new(0, 8)
            }), u3.createElement("UIPadding", {
                PaddingRight = UDim.new(0, 10)
            }) };
        local v11 = #v10;
        local v12 = {
            AdditionalSpace = 8
        };

        for i, v in v9 do
            v10[v11 + i] = v;
        end;

        local v13 = #v10;
        local v14;

        if #p5.PartyInvites.incomingInvites == 0 then
            v14 = u3.createFragment({ u3.createElement(Empty, {
                    Size = UDim2.fromScale(0, 0.1)
                }), u3.createElement("TextLabel", {
                    Text = "You have no incoming party invites :(",
                    TextScaled = true,
                    RichText = true,
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Font = "RobotoMono",
                    Size = UDim2.fromScale(0.8, 0.3),
                    TextColor3 = Color3.fromRGB(255, 255, 255)
                }, { u3.createElement("UIAspectRatioConstraint", {
                        AspectRatio = 13.4
                    }) }), u3.createElement(Empty, {
                    Size = UDim2.fromScale(0, 0.03)
                }) });
        else
            v14 = false;
        end;

        if v14 then
            v10[v13 + 1] = v14;
        end;

        return u3.createElement(AutoCanvasScrollingFrame, v12, v10);
    end)
};