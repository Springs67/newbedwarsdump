-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local OfflinePlayerUtil = v1.import(script, v1.getModule(script, "@easy-games", "game-core").out).OfflinePlayerUtil;
local Players = v1.import(script, v1.getModule(script, "@rbxts", "services")).Players;
local QueueState = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "party", "queue-state").QueueState;
local u2 = {
    queueData = nil,
    leader = not Players.LocalPlayer and {
        name = "SnickTrix",
        userId = 22641473,
        displayName = "SnickTrix"
    } or OfflinePlayerUtil.getOfflinePlayer(Players.LocalPlayer),
    members = {},
    queueState = QueueState.NONE,
    outgoingInviteUserIds = {}
};

return {
    PartyReducer = function(p3, p4) -- Line: 17, Name: PartyReducer
        -- upvalues: u2 (copy)
        if p3 == nil then
            p3 = u2;
        end;

        if p4.type ~= "PartyUpdateAll" then
            return p3;
        end;

        local v5 = {};

        for i, v in p3 do
            v5[i] = v;
        end;

        v5.leader = p4.party.leader;
        local v6 = {};
        local members = p4.party.members;
        table.move(members, 1, #members, #v6 + 1, v6);
        v5.members = v6;
        v5.queueData = p4.party.queueData;
        v5.queueState = p4.party.queueState;
        local v7 = {};
        local outgoingInviteUserIds = p4.party.outgoingInviteUserIds;
        table.move(outgoingInviteUserIds, 1, #outgoingInviteUserIds, #v7 + 1, v7);
        v5.outgoingInviteUserIds = v7;

        return v5;
    end
};