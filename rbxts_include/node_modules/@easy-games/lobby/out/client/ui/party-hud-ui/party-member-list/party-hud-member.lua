-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local PlayerRender = v1.import(script, v1.getModule(script, "@easy-games", "game-core").out).PlayerRender;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local LobbyAppIds = v1.import(script, script.Parent.Parent.Parent, "types", "app-configuration").LobbyAppIds;
local v3 = u2.Component:extend("PartyHudMember");

function v3.init(p4) -- Line: 10
end;

function v3.render(p5) -- Line: 12
    -- upvalues: u2 (copy), Flamework (copy), LobbyAppIds (copy), PlayerRender (copy)
    return u2.createElement("ImageButton", {
        Size = UDim2.fromScale(1, 1),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        AutoButtonColor = false,
        ImageTransparency = 1,
        LayoutOrder = p5.props.LayoutOrder,

        [u2.Event.MouseButton1Click] = function() -- Line: 20
            -- upvalues: Flamework (ref), LobbyAppIds (ref)
            Flamework.resolveDependency("@easy-games/game-core:client/controllers/app-controller@AppController"):openApp(LobbyAppIds.PARTY, {});
        end
    }, { u2.createElement(PlayerRender, {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            PlayerUserId = p5.props.Member.userId,
            Size = UDim2.fromScale(1, 1)
        }, { u2.createElement("UICorner", {
                CornerRadius = UDim.new(1, 0)
            }) }) });
end;

return {
    PartyHudMember = v3
};