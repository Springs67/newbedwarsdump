-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local SyncEvent = v1.import(script, v1.getModule(script, "@easy-games", "sync-event").out).SyncEvent;
local LobbyEvents = v1.import(script, script.Parent.Parent.Parent, "shared", "event", "lobby-events").LobbyEvents;
local LocalPlayerTeleportStartedEvent = v1.import(script, script.Parent, "sync-events", "local-player-teleport-started-event").LocalPlayerTeleportStartedEvent;
local client = LobbyEvents.client;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 10, Name: __tostring
        return "LobbyClientSyncEvents";
    end
});
u2.__index = u2;

function u2.new(...) -- Line: 15
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 19
end;

u2.LocalPlayerTeleportStarted = SyncEvent.new(LocalPlayerTeleportStartedEvent);

return {
    LobbyClientEvents = client,
    LobbyClientSyncEvents = u2
};