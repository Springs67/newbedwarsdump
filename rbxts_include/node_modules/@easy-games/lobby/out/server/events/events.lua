-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local SyncEvent = v1.import(script, v1.getModule(script, "@easy-games", "sync-event").out).SyncEvent;
local LobbyEvents = v1.import(script, script.Parent.Parent.Parent, "shared", "event", "lobby-events").LobbyEvents;
local MatchTeleportEvent = v1.import(script, script.Parent, "sync-events", "match-teleport-event").MatchTeleportEvent;
local PartyLeaveQueueEvent = v1.import(script, script.Parent, "sync-events", "party-leave-queue-event").PartyLeaveQueueEvent;
local PartyJoinQueueEvent = v1.import(script, script.Parent, "sync-events", "player-join-queue-event").PartyJoinQueueEvent;
local server = LobbyEvents.server;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 12, Name: __tostring
        return "LobbyServerSyncEvents";
    end
});
u2.__index = u2;

function u2.new(...) -- Line: 17
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 21
end;

u2.PartyJoinQueue = SyncEvent.new(PartyJoinQueueEvent);
u2.PartyLeaveQueue = SyncEvent.new(PartyLeaveQueueEvent);
u2.MatchTeleportEvent = SyncEvent.new(MatchTeleportEvent);

return {
    LobbyServerEvents = server,
    LobbyServerSyncEvents = u2
};