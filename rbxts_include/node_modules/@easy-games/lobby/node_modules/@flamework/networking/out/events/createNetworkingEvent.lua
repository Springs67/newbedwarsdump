-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "object-utils"));
local RunService = v1.import(script, v1.getModule(script, "@rbxts", "services")).RunService;
local populateInstanceMap = v1.import(script, script.Parent.Parent, "util", "populateInstanceMap").populateInstanceMap;
local createClientHandler = v1.import(script, script.Parent, "createClientHandler").createClientHandler;
local createServerHandler = v1.import(script, script.Parent, "createServerHandler").createServerHandler;

return {
    createNetworkingEvent = function(p3, p4, p5, p6, p7) -- Line: 8, Name: createNetworkingEvent
        -- upvalues: populateInstanceMap (copy), u2 (copy), RunService (copy), createServerHandler (copy), createClientHandler (copy)
        local v8 = {};
        populateInstanceMap("RemoteEvent", "events-" .. p3, u2.keys(p4), v8);
        populateInstanceMap("RemoteEvent", "events-" .. p3, u2.keys(p5), v8);
        local v9 = {};

        for i in v8 do
            v9[i] = {
                eventType = "Event",
                globalName = p3,
                name = i
            };
        end;

        return RunService:IsServer() and {
            client = nil,
            server = createServerHandler(v8, v9, p4, p5, p6)
        } or {
            server = nil,
            client = createClientHandler(v8, v9, p4, p5, p7)
        };
    end
};