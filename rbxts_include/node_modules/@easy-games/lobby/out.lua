-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = {};

for i, v in v1.import(script, script, "client", "lobby-client") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "config", "lobby-client-config") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "custom-match-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "lobby-client-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "lobby-loading-screen-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "lobby-queue-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "party-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "controllers", "event-handlers", "party-handler-controller") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "events", "events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "events", "sync-events", "local-player-teleport-started-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "core") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "lib", "client-party") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "lib", "notifications", "party-invite-notification-card") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-hud-ui", "party-hud-app") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-hud-ui", "party-hud-button") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-hud-ui", "party-member-list", "party-hud-member-list") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-hud-ui", "party-member-list", "party-hud-member") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-invites-ui", "party-incoming-invite-card") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-invites-ui", "party-invites-app.story") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-invites-ui", "party-invites-app") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-invites-ui", "reducers", "party-invites-reducer") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "party-app") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "party-card.story") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "party-card") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-left-side") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-player-search") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-suggested-player") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "right", "leave-party-button") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-invites-tab") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-member-card") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-member-list") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-members-tab") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "party-ui", "reducers", "party-reducer") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "types", "app-configuration") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "types", "app-props") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "client", "ui", "types", "store") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "lobby-server") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "commands", "game-teleport-command") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "config", "lobby-server-config") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "events", "events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "events", "sync-events", "match-teleport-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "events", "sync-events", "party-leave-queue-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "events", "sync-events", "player-join-queue-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "lib", "party") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "device-info-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "game-teleport-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "lobby-server-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "matchmaking-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "party-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "player-location-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "playfab-error-codes") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "playfab-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "queue-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "custom-match", "custom-match-manager-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "device-info", "data", "continents") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "device-info", "data", "sub-regions") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "device-info", "data", "wide-sub-regions") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "event-handlers", "device-info-event-handler-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "event-handlers", "party-event-handler-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "services", "event-handlers", "queue-event-handler-service") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "lobby-core") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "lobby-networking") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "team-config") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "custom-match", "custom-match-types") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "device-info", "events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "event", "lobby-events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "incoming-party-invite-dto") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "party-dto") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "party-invite-notification") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "party-queue-data") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "party-role") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "party") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "party", "queue-state") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "player-location", "server-type") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "queue", "events") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "queue", "queue-metadata") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "sound", "sound-configuration") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "teleport", "game-teleport-data") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "types", "config-types") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "types", "device-info-types") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "types", "flamework") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "types", "generic-types") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "types", "map-types") or {} do
    v2[i] = v;
end;

return v2;