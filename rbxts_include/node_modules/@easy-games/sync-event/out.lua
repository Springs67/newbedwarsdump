-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = {};

for i, v in v1.import(script, script, "async-events", "async-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "event-template") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "events", "cancellable-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "events", "parameterized-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "sync-event-priority") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "sync-events", "parameterized-sync-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "sync-events", "sync-event") or {} do
    v2[i] = v;
end;

return v2;