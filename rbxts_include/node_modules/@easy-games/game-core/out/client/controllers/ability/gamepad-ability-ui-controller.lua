-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Reflect = v1.import(script, v1.getModule(script, "@flamework", "core").out).Reflect;
local Controller = v1.import(script, v1.getModule(script, "@flamework", "core").out).Controller;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local GameCoreClientSyncEvents = v1.import(script, script.Parent.Parent.Parent, "game-core-client-events").GameCoreClientSyncEvents;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 10, Name: __tostring
        return "GamepadAbilityUIController";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 15
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5) -- Line: 19
    p5.mountedAbilities = {};
end;

function u3.onStart(p6) -- Line: 22
end;

function u3.mountAbilityButton(u7, u8, p9, u10, p11) -- Line: 24
    -- upvalues: u2 (copy), GameCoreClientSyncEvents (copy)
    if u7.mountedAbilities[u8] ~= nil then
        return nil;
    end;

    local v12 = u2.new();
    u7.mountedAbilities[u8] = {
        abilityType = u10,
        element = p9
    };

    if p11:getValue() then
        p11:getValue().Size = UDim2.fromScale(1, 0);
    end;

    GameCoreClientSyncEvents.GamepadAbilityMounted:fire(u8, p9, u10);
    v12:GiveTask(function() -- Line: 43
        -- upvalues: u7 (copy), u8 (copy), GameCoreClientSyncEvents (ref), u10 (copy)
        u7.mountedAbilities[u8] = nil;
        GameCoreClientSyncEvents.GamepadAbilityUnmounted:fire(u10);
    end);

    return v12;
end;

Reflect.defineMetadata(u3, "identifier", "@easy-games/game-core:client/controllers/ability/gamepad-ability-ui-controller@GamepadAbilityUIController");
Reflect.defineMetadata(u3, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u3, "$:flamework@Controller", Controller, { {} });

return {
    GamepadAbilityUIController = u3
};