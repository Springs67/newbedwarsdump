local v1 = _G[script].import(script, script.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil
local v2 = v1.isSmallScreen() and 30 or 60
local v3 = UDim.new(nil, v2 * (v1.isSmallScreen() and 1.2 or 2))
local v4 = UDim.new(1) - UDim.new(nil, v2 * 2) - v3
local v5 = UDim.new(1) - UDim.new(nil, v2 * 2.5)
local v6 = UDim2.new(v4, v5)
local v7 = UDim2.new(v3, v5)
local v8 = {}
for _, v9 in {
    { "PrimaryUp", v6 + UDim2.fromOffset(v2, v2 * -1) },
    { "PrimaryDown", v6 + UDim2.fromOffset(v2, v2) },
    { "PrimaryLeft", v6 },
    { "PrimaryRight", v6 + UDim2.fromOffset(v2 * 2) },
    { "SecondaryUp", v7 + UDim2.fromOffset(v2, v2 * -1) },
    { "SecondaryDown", v7 + UDim2.fromOffset(v2, v2) },
    { "SecondaryLeft", v7 },
    { "SecondaryRight", v7 + UDim2.fromOffset(v2 * 2) }
} do
    v8[v9[1]] = v9[2]
end
return {
    ["GAMEPAD_BUTTON_POSITION"] = v8
}