-- Decompiled with Potassium's decompiler.

local DeviceUtil = _G[script].import(script, script.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;
local v1 = DeviceUtil.isSmallScreen() and 30 or 60;
local v2 = UDim.new(nil, v1 * (DeviceUtil.isSmallScreen() and 1.2 or 2));
local v3 = UDim.new(1) - UDim.new(nil, v1 * 2) - v2;
local v4 = UDim.new(1) - UDim.new(nil, v1 * 2.5);
local v5 = UDim2.new(v3, v4);
local v6 = UDim2.new(v2, v4);
local v7 = {};

for _, v in {
    { "PrimaryUp", v5 + UDim2.fromOffset(v1, v1 * -1) },
    { "PrimaryDown", v5 + UDim2.fromOffset(v1, v1) },
    { "PrimaryLeft", v5 },
    { "PrimaryRight", v5 + UDim2.fromOffset(v1 * 2) },
    { "SecondaryUp", v6 + UDim2.fromOffset(v1, v1 * -1) },
    { "SecondaryDown", v6 + UDim2.fromOffset(v1, v1) },
    { "SecondaryLeft", v6 },
    { "SecondaryRight", v6 + UDim2.fromOffset(v1 * 2) }
} do
    v7[v[1]] = v[2];
end;

return {
    GAMEPAD_BUTTON_POSITION = v7
};