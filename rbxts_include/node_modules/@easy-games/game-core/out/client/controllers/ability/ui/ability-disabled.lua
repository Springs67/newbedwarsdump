-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;

return {
    AbilityDisabled = v3.new(u2)(function(p4, p5) -- Line: 7
        -- upvalues: u2 (copy), GameTheme (copy), ColorUtil (copy)
        return u2.createElement("ImageLabel", {
            BorderSizePixel = 0,
            BackgroundTransparency = 0.8,
            Size = GameTheme.actionBarButtonSize,
            BackgroundColor3 = Color3.fromRGB(20, 20, 20)
        }, {
            DisabledButton = u2.createElement("TextLabel", {
                RichText = true,
                Text = "<b>DISABLED</b>",
                Font = "ArialBold",
                TextScaled = true,
                BackgroundTransparency = 1,
                Size = UDim2.fromScale(0.9, 0.9),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.fromScale(0.5, 0.5),
                LayoutOrder = GameTheme.abilityActionBarPriority,
                TextColor3 = ColorUtil.hexColor(16664149)
            }),
            u2.createElement("UIAspectRatioConstraint", {
                AspectRatio = 4
            })
        });
    end)
};