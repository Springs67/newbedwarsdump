-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local MobileButton = v1.import(script, script.Parent.Parent.Parent.Parent, "ui", "lib", "components", "mobile-button", "mobile-button").MobileButton;
local UIUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").UIUtil;

return {
    AbilityMobileButton = v3.new(u2)(function(u4, p5) -- Line: 7
        -- upvalues: UIUtil (copy), u2 (copy), MobileButton (copy)
        local v6 = p5.useMemo(function() -- Line: 8
            -- upvalues: u4 (copy), UIUtil (ref)
            if u4.position == 0 then
                return UIUtil:getActionMobileButtonPosition() + UDim2.fromScale(-0.03, -0.22);
            end;

            if u4.position == 1 then
                return UIUtil:getActionMobileButtonPosition() + UDim2.fromScale(0.06, -0.16);
            end;

            if u4.position == 2 then
                return UIUtil:getActionMobileButtonPosition() + UDim2.fromScale(0.06, -0.34);
            end;

            return UIUtil:getActionMobileButtonPosition() + UDim2.fromScale(-0.085, -0.05);
        end, { u4.position });

        return u2.createElement(MobileButton, {
            BackgroundTransparency = 0.5,
            Image = "rbxassetid://0",
            Position = v6,
            BackgroundColor = Color3.fromRGB(0, 0, 0),

            OnClick = function() -- Line: 32, Name: OnClick
                -- upvalues: u4 (copy)
                u4.activated();
            end
        }, { u2.createElement("UICorner", {
                CornerRadius = UDim.new(1, 0)
            }), u2.createElement("TextLabel", {
                BackgroundTransparency = 1,
                TextScaled = true,
                Size = UDim2.fromScale(0.8, 0.8),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.fromScale(0.5, 0.5),
                Text = string.upper(u4.text),
                TextColor3 = Color3.fromRGB(255, 255, 255)
            }) });
    end)
};