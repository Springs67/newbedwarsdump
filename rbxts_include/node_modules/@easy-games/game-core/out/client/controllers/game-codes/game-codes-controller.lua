-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Reflect = u1.import(script, u1.getModule(script, "@flamework", "core").out).Reflect;
local Controller = u1.import(script, u1.getModule(script, "@flamework", "core").out).Controller;
local Players = u1.import(script, u1.getModule(script, "@rbxts", "services")).Players;
local GameCoreClientConfig = u1.import(script, script.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local v2 = u1.import(script, script.Parent.Parent.Parent.Parent, "shared", "game-core-networking");
local GameCoreNetEvents = v2.GameCoreNetEvents;
local GameCoreNetFunctions = v2.GameCoreNetFunctions;
local ExpireList = u1.import(script, script.Parent.Parent.Parent.Parent, "shared", "object", "expire-list").ExpireList;
local GameTheme = u1.import(script, script.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = u1.import(script, script.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 16, Name: __tostring
        return "GameCodesController";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 21
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5) -- Line: 25
    -- upvalues: ExpireList (copy)
    p5.rateLimiter = ExpireList.new(1);
end;

function u3.onStart(p6) -- Line: 28
    -- upvalues: GameCoreNetEvents (copy), GameCoreClientConfig (copy)
    GameCoreNetEvents.client.sendCodeGenUpdate:connect(function(p7) -- Line: 29
        -- upvalues: GameCoreClientConfig (ref)
        GameCoreClientConfig.Store:dispatch({
            type = "CodeGenUpdate",
            codeGenerators = p7.codeGenerators
        });
    end);
end;

function u3.regenCodes(p8, p9) -- Line: 36
    -- upvalues: Players (copy), u1 (copy), ColorUtil (copy), GameTheme (copy), GameCoreNetFunctions (copy)
    local UserId = Players.LocalPlayer.UserId;

    if p8.rateLimiter:has(UserId) then
        return u1.Promise.new(function(p10) -- Line: 39
            -- upvalues: ColorUtil (ref), GameTheme (ref)
            p10({
                success = false,
                errorMessage = "<font color=\"" .. ColorUtil.richTextColor(GameTheme.backgroundError) .. "\"><b>🚫 Error:</b></font> You\'ve made to many requests. Please slow down a bit!"
            });
        end);
    end;

    p8.rateLimiter:add(UserId);

    return GameCoreNetFunctions.client.regenCodeGenerator:invoke(p9);
end;

function u3.redeemCode(p11, p12) -- Line: 49
    -- upvalues: Players (copy), u1 (copy), ColorUtil (copy), GameTheme (copy), GameCoreNetFunctions (copy)
    local UserId = Players.LocalPlayer.UserId;

    if p11.rateLimiter:has(UserId) then
        return u1.Promise.new(function(p13) -- Line: 52
            -- upvalues: ColorUtil (ref), GameTheme (ref)
            p13({
                success = false,
                errorMessage = "<font color=\"" .. ColorUtil.richTextColor(GameTheme.backgroundError) .. "\"><b>🚫 Error:</b></font> You\'ve made to many requests. Please slow down a bit!"
            });
        end);
    end;

    p11.rateLimiter:add(UserId);

    return GameCoreNetFunctions.client.redeemCode:invoke(p12);
end;

function u3.requestCodeGenUpdate(p14) -- Line: 62
    -- upvalues: GameCoreNetEvents (copy)
    GameCoreNetEvents.client.requestCodeGenUpdate:fire();
end;

Reflect.defineMetadata(u3, "identifier", "@easy-games/game-core:client/controllers/game-codes/game-codes-controller@GameCodesController");
Reflect.defineMetadata(u3, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u3, "$:flamework@Controller", Controller, { {} });

return {
    GameCodesController = u3
};