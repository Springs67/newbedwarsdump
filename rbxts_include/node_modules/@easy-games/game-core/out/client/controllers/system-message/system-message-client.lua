-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Reflect = v1.import(script, v1.getModule(script, "@flamework", "core").out).Reflect;
local Controller = v1.import(script, v1.getModule(script, "@flamework", "core").out).Controller;
local StarterGui = v1.import(script, v1.getModule(script, "@rbxts", "services")).StarterGui;
local GameCoreNetEvents = v1.import(script, script.Parent.Parent.Parent.Parent, "shared", "game-core-networking").GameCoreNetEvents;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 11, Name: __tostring
        return "SystemMessageClient";
    end
});
u2.__index = u2;

function u2.new(...) -- Line: 16
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 20
    p4.chatVersion = game:GetService("TextChatService").ChatVersion;
end;

function u2.onStart(u5) -- Line: 23
    -- upvalues: GameCoreNetEvents (copy), ColorUtil (copy), StarterGui (copy)
    GameCoreNetEvents.client.chatMakeSystemMessage:connect(function(p6) -- Line: 24
        -- upvalues: ColorUtil (ref), u5 (copy), StarterGui (ref)
        if p6.Color then
            p6.Text = "<font color=\"" .. ColorUtil.richTextColor(p6.Color) .. "\">" .. p6.Text .. "</font>";
        end;

        if u5.chatVersion ~= Enum.ChatVersion.TextChatService then
            StarterGui:SetCore("ChatMakeSystemMessage", p6);

            return;
        end;

        local TextChannels = game:GetService("TextChatService"):FindFirstChild("TextChannels");

        if TextChannels ~= nil then
            TextChannels = TextChannels:FindFirstChild("RBXGeneral");
        end;

        if not TextChannels then
            return nil;
        end;

        TextChannels:DisplaySystemMessage(p6.Text);
    end);
end;

Reflect.defineMetadata(u2, "identifier", "@easy-games/game-core:client/controllers/system-message/system-message-client@SystemMessageClient");
Reflect.defineMetadata(u2, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u2, "$:flamework@Controller", Controller, { {} });

return {
    SystemMessageClient = u2
};