-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Action = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "controls", "action").Action;
local GamepadKeycodes = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "controls", "constrained-keycodes").GamepadKeycodes;

return {
    getWrappedBoundFunction = function(u2, u3) -- Line: 9, Name: getWrappedBoundFunction
        -- upvalues: Action (copy), GamepadKeycodes (copy)
        local function _(p4) -- Line: 11
            -- upvalues: u2 (copy)
            return p4 == u2.action;
        end;

        local v5 = nil;

        for i, v in Action.AbilityActions do
            local _ = i - 1;

            if v == u2.action == true then
                v5 = v;
                break;
            end;
        end;

        local u6 = v5 and true or false;

        return function(p7, p8, p9) -- Line: 24
            -- upvalues: GamepadKeycodes (ref), u3 (copy), u6 (copy), u2 (copy)
            if table.find(GamepadKeycodes, p9.KeyCode) ~= nil and u3.Value ~= u6 then
                return Enum.ContextActionResult.Pass;
            end;

            return u2.boundFunction(p7, p8, p9);
        end;
    end
};