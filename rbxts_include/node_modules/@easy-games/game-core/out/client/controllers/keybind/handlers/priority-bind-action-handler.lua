-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local ContextActionService = v1.import(script, v1.getModule(script, "@rbxts", "services")).ContextActionService;
local getWrappedBoundFunction = v1.import(script, script.Parent, "bind-action-handler").getWrappedBoundFunction;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 9, Name: __tostring
        return "PriorityBindActionHandler";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 14
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5) -- Line: 18
    p5.actionIdDisambiguator = "priority-";
end;

function u3.shouldApply(p6, p7, p8) -- Line: 21
    local priority = p7.priority;

    if priority == 0 then
        priority = false;
    elseif priority ~= priority then
        priority = false;
    end;

    return priority and true or false;
end;

function u3.handle(p9, p10, p11, p12) -- Line: 26
    -- upvalues: u2 (copy), ContextActionService (copy), getWrappedBoundFunction (copy)
    local v13 = u2.new();
    local u14 = p9.actionIdDisambiguator .. p10.actionId;
    local v15 = getWrappedBoundFunction(p10, p12);
    local priority = p10.priority;

    local function _(p16) -- Line: 33
        return p16 ~= Enum.KeyCode.ButtonA;
    end;

    local v17 = 0;
    local v18 = {};

    for i, v in p11 do
        local _ = i - 1;

        if v ~= Enum.KeyCode.ButtonA == true then
            v17 = v17 + 1;
            v18[v17] = v;
        end;
    end;

    ContextActionService:BindActionAtPriority(u14, v15, false, priority, unpack(v18));
    v13:GiveTask(function() -- Line: 47
        -- upvalues: ContextActionService (ref), u14 (copy)
        ContextActionService:UnbindAction(u14);
    end);

    return {
        maid = v13,
        actionId = u14
    };
end;

return {
    PriorityBindActionHandler = u3
};