-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local ContextActionService = v1.import(script, v1.getModule(script, "@rbxts", "services")).ContextActionService;
local getWrappedBoundFunction = v1.import(script, script.Parent, "bind-action-handler").getWrappedBoundFunction;
local u3 = Enum.ContextActionPriority.Medium.Value + 10;
local u4 = setmetatable({}, {
    __tostring = function() -- Line: 10, Name: __tostring
        return "DefaultBindActionHandler";
    end
});
u4.__index = u4;

function u4.new(...) -- Line: 15
    -- upvalues: u4 (ref)
    local v5 = setmetatable({}, u4);

    return v5:constructor(...) or v5;
end;

function u4.constructor(p6) -- Line: 19
    p6.actionIdDisambiguator = "default-";
end;

function u4.shouldApply(p7, p8, p9) -- Line: 22
    return p8.priority == nil;
end;

function u4.handle(p10, p11, p12, p13) -- Line: 26
    -- upvalues: u2 (copy), ContextActionService (copy), getWrappedBoundFunction (copy), u3 (copy)
    local v14 = u2.new();
    local u15 = p10.actionIdDisambiguator .. p11.actionId;
    local v16 = getWrappedBoundFunction(p11, p13);

    local function _(p17) -- Line: 32
        return p17 ~= Enum.KeyCode.ButtonA;
    end;

    local v18 = 0;
    local v19 = {};

    for i, v in p12 do
        local _ = i - 1;

        if v ~= Enum.KeyCode.ButtonA == true then
            v18 = v18 + 1;
            v19[v18] = v;
        end;
    end;

    ContextActionService:BindActionAtPriority(u15, v16, false, u3, unpack(v19));
    v14:GiveTask(function() -- Line: 46
        -- upvalues: ContextActionService (ref), u15 (copy)
        return ContextActionService:UnbindAction(u15);
    end);

    return {
        maid = v14,
        actionId = u15
    };
end;

return {
    DEFAULT_BIND_ACTION_PRIORITY = u3,
    DefaultBindActionHandler = u4
};