-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Workspace = v1.import(script, v1.getModule(script, "@rbxts", "services")).Workspace;
local MathExtras = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "math-util").MathExtras;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 8, Name: __tostring
        return "IndicatorColorFunctions";
    end
});
u2.__index = u2;

function u2.new(...) -- Line: 13
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 17
end;

function u2.twoColorTransition(p5, u6, u7, u8, u9) -- Line: 19
    -- upvalues: MathExtras (copy)
    return function(p10) -- Line: 20
        -- upvalues: u8 (copy), u9 (copy), MathExtras (ref), u6 (copy), u7 (copy)
        local v11 = math.clamp(p10, u8, u9);
        local v12 = MathExtras:rangeMap(v11, { u8, u9 }, { u6.R * 255, u7.R * 255 });
        local v13 = MathExtras:rangeMap(v11, { u8, u9 }, { u6.G * 255, u7.G * 255 });
        local v14 = MathExtras:rangeMap(v11, { u8, u9 }, { u6.B * 255, u7.B * 255 });

        return Color3.fromRGB(v12, v13, v14);
    end;
end;

function u2.transitionOverTime(p15, u16, u17, u18) -- Line: 28
    -- upvalues: Workspace (copy), MathExtras (copy)
    local u19 = nil;

    return function(p20) -- Line: 30
        -- upvalues: u19 (ref), Workspace (ref), MathExtras (ref), u18 (copy), u16 (copy), u17 (copy)
        if u19 == 0 or (u19 ~= u19 or not u19) then
            u19 = Workspace:GetServerTimeNow();
        end;

        local v21 = Workspace:GetServerTimeNow() - u19;
        local v22 = MathExtras:rangeMap(v21, { 0, u18 }, { u16.R * 255, u17.R * 255 });
        local v23 = MathExtras:rangeMap(v21, { 0, u18 }, { u16.G * 255, u17.G * 255 });
        local v24 = MathExtras:rangeMap(v21, { 0, u18 }, { u16.B * 255, u17.B * 255 });

        return Color3.fromRGB(v22, v23, v24);
    end;
end;

return {
    IndicatorColorFunctions = u2
};