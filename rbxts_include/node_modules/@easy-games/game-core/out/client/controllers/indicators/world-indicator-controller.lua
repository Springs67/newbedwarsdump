-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Reflect = v1.import(script, v1.getModule(script, "@flamework", "core").out).Reflect;
local Controller = v1.import(script, v1.getModule(script, "@flamework", "core").out).Controller;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "make"));
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "services"));
local HttpService = v3.HttpService;
local Players = v3.Players;
local Workspace = v3.Workspace;
local CircleIndicator = v1.import(script, script.Parent, "indicator-renderers", "circle-indicator").CircleIndicator;
local CustomIndicator = v1.import(script, script.Parent, "indicator-renderers", "custom-indicator").CustomIndicator;
local IconIndicator = v1.import(script, script.Parent, "indicator-renderers", "icon-indicator").IconIndicator;
local IndicatorUIType = v1.import(script, script.Parent, "indicator-types").IndicatorUIType;
local CurrentCamera = Workspace.CurrentCamera;
local u4 = setmetatable({}, {
    __tostring = function() -- Line: 19, Name: __tostring
        return "WorldIndicatorController";
    end
});
u4.__index = u4;

function u4.new(...) -- Line: 24
    -- upvalues: u4 (ref)
    local v5 = setmetatable({}, u4);

    return v5:constructor(...) or v5;
end;

function u4.constructor(p6) -- Line: 28
    p6.activeIndicators = {};
end;

function u4.onStart(u7) -- Line: 31
    -- upvalues: CurrentCamera (copy)
    u7.uiContainer = u7:createUiContainer();
    CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(function() -- Line: 33
        -- upvalues: u7 (copy)
        return u7:updateUiContainer();
    end);
    u7:updateUiContainer();
end;

function u4.addIndicator(p8, p9) -- Line: 38
    -- upvalues: HttpService (copy), IndicatorUIType (copy), CircleIndicator (copy), IconIndicator (copy), CustomIndicator (copy)
    local v10 = HttpService:GenerateGUID(false);
    local uiType = p9.uiType;
    local v11;

    if uiType == IndicatorUIType.Circle then
        v11 = CircleIndicator.new(v10, p8.uiContainer, p9);
    elseif uiType == IndicatorUIType.Icon then
        v11 = IconIndicator.new(v10, p8.uiContainer, p9);
    elseif uiType == IndicatorUIType.Custom then
        v11 = CustomIndicator.new(v10, p8.uiContainer, p9);
    else
        v11 = CircleIndicator.new(v10, p8.uiContainer, p9);
    end;

    p8.activeIndicators[v10] = v11;

    return v11;
end;

function u4.getIndicator(p12, p13) -- Line: 62
    local _ = p12.activeIndicators[p13];
end;

function u4.removeIndicator(p14, p15) -- Line: 67
    p14.activeIndicators[p15] = nil;
end;

function u4.onRender(p16) -- Line: 72
    for _, v in p16.activeIndicators do
        v:update();
    end;
end;

function u4.updateUiContainer(p17) -- Line: 77
    -- upvalues: CurrentCamera (copy)
    local ViewportSize = CurrentCamera.ViewportSize;
    local v18 = math.min(ViewportSize.X, ViewportSize.Y) * 0.065;
    p17.uiContainer.Position = UDim2.fromOffset(v18, v18);
    p17.uiContainer.Size = UDim2.new(1, -v18 * 2, 1, -v18 * 2);
end;

function u4.createUiContainer(p19) -- Line: 83
    -- upvalues: u2 (copy), Players (copy)
    local v20 = u2("Frame", {
        BackgroundTransparency = 1
    });
    u2("ScreenGui", {
        Name = "WorldIndicators",
        DisplayOrder = 100,
        ResetOnSpawn = false,
        Parent = Players.LocalPlayer:FindFirstChildOfClass("PlayerGui"),
        Children = { v20 }
    });

    return v20;
end;

Reflect.defineMetadata(u4, "identifier", "@easy-games/game-core:client/controllers/indicators/world-indicator-controller@WorldIndicatorController");
Reflect.defineMetadata(u4, "flamework:implements", { "$:flamework@OnRender", "$:flamework@OnStart" });
Reflect.decorate(u4, "$:flamework@Controller", Controller, { {} });

return {
    WorldIndicatorController = u4
};