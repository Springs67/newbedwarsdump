-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Workspace = v1.import(script, v1.getModule(script, "@rbxts", "services")).Workspace;
local MathExtras = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "math-util").MathExtras;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 8, Name: __tostring
        return "IndicatorTransparencyFunctions";
    end
});
u2.__index = u2;

function u2.new(...) -- Line: 13
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 17
end;

function u2.fadeOutDistance(p5, u6, u7, p8, p9) -- Line: 19
    -- upvalues: MathExtras (copy)
    local u10 = p8 == nil and 0 or p8;
    local u11 = p9 == nil and 1 or p9;

    return function(p12) -- Line: 26
        -- upvalues: MathExtras (ref), u6 (copy), u7 (copy), u10 (ref), u11 (ref)
        local v13 = { u6, u7 };
        local v14 = { u10, u11 };

        return MathExtras:rangeMap(math.clamp(p12, u6, u7), v13, v14);
    end;
end;

function u2.fadeOverTime(p15, u16, u17, p18, p19) -- Line: 30
    -- upvalues: Workspace (copy), MathExtras (copy)
    local u20 = p18 == nil and 0 or p18;
    local u21 = p19 == nil and 1 or p19;
    local u22 = nil;

    return function(p23) -- Line: 38
        -- upvalues: u22 (ref), Workspace (ref), MathExtras (ref), u16 (copy), u17 (copy), u20 (ref), u21 (ref)
        if u22 == 0 or (u22 ~= u22 or not u22) then
            u22 = Workspace:GetServerTimeNow();
        end;

        local v24 = Workspace:GetServerTimeNow() - u22;

        return MathExtras:rangeMap(math.clamp(v24, u16, u16 + u17), { u16, u16 + u17 }, { u20, u21 });
    end;
end;

function u2.combine(p25, ...) -- Line: 45
    local u26 = { ... };

    return function(u27) -- Line: 47
        -- upvalues: u26 (copy)
        local v28 = u26;

        local function _(p29) -- Line: 49
            -- upvalues: u27 (copy)
            return p29(u27);
        end;

        local v30 = table.create(#v28);

        for i, v in v28 do
            local _ = i - 1;
            v30[i] = v(u27);
        end;

        return math.max(unpack(v30));
    end;
end;

return {
    IndicatorTransparencyFunctions = u2
};