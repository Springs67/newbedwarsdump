-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "make"));
local ActiveWorldIndicator = v1.import(script, script.Parent.Parent, "active-indicator").ActiveWorldIndicator;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 9, Name: __tostring
        return "CircleIndicator";
    end,

    __index = ActiveWorldIndicator
});
u3.__index = u3;

function u3.new(...) -- Line: 15
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5, ...) -- Line: 19
    -- upvalues: ActiveWorldIndicator (copy)
    ActiveWorldIndicator.constructor(p5, ...);
end;

function u3.updateProperties(p6, p7, p8, p9) -- Line: 22
    -- upvalues: ActiveWorldIndicator (copy)
    local v10;

    if type(p6.config.uiConfig.transparency) == "function" then
        v10 = p6.config.uiConfig.transparency(p7);
    else
        v10 = p6.config.uiConfig.transparency;
    end;

    local v11;

    if type(p6.config.uiConfig.color3) == "function" then
        v11 = p6.config.uiConfig.color3(p7);
    else
        v11 = p6.config.uiConfig.color3;
    end;

    if v10 == nil then
        v10 = p8;
    end;

    local v12 = math.max(p8, v10);
    local v13 = (p6.config.uiConfig.scaleOnFade ~= nil and not p6.config.uiConfig.scaleOnFade and true or false) and 0 or p9;
    local v14 = p6.config.size or ActiveWorldIndicator.BASE_INDICATOR_SIZE;
    local ui = p6.ui;
    local v15 = UDim2.new();

    if p6.config.uiConfig.scaleWithAlpha and p8 < v12 then
        v13 = v12;
    end;

    ui.Size = v14:Lerp(v15, v13);
    p6.ui.BackgroundColor3 = v11;
    p6.ui.Transparency = v12;

    if p6.arrow then
        p6.arrow.BackgroundColor3 = v11;
        local ImageLabel = p6.arrow:WaitForChild("ImageLabel");

        if ImageLabel then
            ImageLabel.ImageTransparency = v12;
            ImageLabel.ImageColor3 = v11;
        end;
    end;
end;

function u3.mountUi(p16, p17) -- Line: 49
    -- upvalues: u2 (copy)
    local v18;

    if type(p16.config.uiConfig.color3) == "function" then
        v18 = p16.config.uiConfig.color3(0);
    else
        v18 = p16.config.uiConfig.color3;
    end;

    p17.BackgroundColor3 = v18;
    p17.BackgroundTransparency = 0;
    u2("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = p17
    });

    return p17;
end;

return {
    CircleIndicator = u3
};