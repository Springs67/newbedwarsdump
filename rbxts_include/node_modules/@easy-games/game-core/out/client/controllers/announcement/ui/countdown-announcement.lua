-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Flamework = u1.import(script, u1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local v3 = u1.import(script, u1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = u1.import(script, u1.getModule(script, "@rbxts", "services")).TweenService;
local Countdown = u1.import(script, script.Parent.Parent.Parent.Parent, "ui", "lib", "components", "countdown", "countdown").Countdown;
local Empty = u1.import(script, script.Parent.Parent.Parent.Parent, "ui", "lib", "components", "empty").Empty;
local AnnouncementUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "announcement", "announcement-util").AnnouncementUtil;
local GameTheme = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local DeviceUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;

return {
    CountdownAnnouncement = v3.new(u2)(function(u4, p5) -- Line: 13
        -- upvalues: u2 (copy), TweenService (copy), u1 (copy), AnnouncementUtil (copy), DeviceUtil (copy), Flamework (copy), ColorUtil (copy), GameTheme (copy), Countdown (copy), Empty (copy)
        local useEffect = p5.useEffect;
        local u6 = u2.createRef();
        local u7 = u2.createRef();
        local u8 = u2.createRef();
        local u9 = u2.createRef();
        local themeColor = u4.Announcement.themeColor;

        if themeColor == nil then
            themeColor = Color3.fromHex("#54FFFF");
        end;

        useEffect(function() -- Line: 24
            -- upvalues: u6 (copy), u9 (copy), u7 (copy), u8 (copy), TweenService (ref), u1 (ref), u4 (copy), AnnouncementUtil (ref), DeviceUtil (ref), Flamework (ref)
            local u10 = u6:getValue();
            local u11 = u9:getValue();
            local u12 = u7:getValue();
            local u13 = u8:getValue();

            if not (u10 and (u12 and u11)) then
                return nil;
            end;

            local function v21() -- Line: 68
                -- upvalues: TweenService (ref), u10 (copy), u11 (copy), u1 (ref), u4 (ref), AnnouncementUtil (ref), u12 (copy), u13 (copy), DeviceUtil (ref), Flamework (ref)
                local function u15(p14) -- Line: 71
                    -- upvalues: TweenService (ref)
                    return TweenService:Create(p14, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                        ImageTransparency = 1
                    });
                end;

                local function u17(p16) -- Line: 76
                    -- upvalues: TweenService (ref)
                    return TweenService:Create(p16, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                        Size = UDim2.fromScale(0, 1)
                    });
                end;

                local u18 = TweenService:Create(u10, TweenInfo.new(0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                    TextTransparency = 1,
                    Position = UDim2.fromScale(0.5, 0.25)
                });
                local u19 = TweenService:Create(u11, TweenInfo.new(0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                    TextTransparency = 1,
                    Position = UDim2.fromScale(0.5, 0.9)
                });
                local duration = u4.Announcement.duration;

                if duration == nil then
                    duration = AnnouncementUtil.DEFAULT_DURATION;
                end;

                u1.Promise.delay(duration):andThen(function() -- Line: 97
                    -- upvalues: u18 (copy), u19 (copy), u15 (copy), u12 (ref), u17 (copy), u13 (ref), DeviceUtil (ref), Flamework (ref), u4 (ref)
                    u18:Play();
                    u19:Play();
                    u15(u12):Play();
                    local v20 = u17(u12);
                    v20:Play();

                    if u13 then
                        u15(u13):Play();
                        u17(u13):Play();
                    end;

                    if DeviceUtil.isHoarceKat() then
                        return nil;
                    end;

                    v20.Completed:Connect(function() -- Line: 110
                        -- upvalues: Flamework (ref), u4 (ref)
                        Flamework.resolveDependency("@easy-games/game-core:client/controllers/announcement/announcement-controller@AnnouncementController"):endAnnouncement(u4.Announcement.important);
                    end);
                end);
            end;

            (function() -- Line: 32
                -- upvalues: TweenService (ref), u10 (copy), u11 (copy), u12 (copy), u13 (copy)
                local function _(p22) -- Line: 33
                    -- upvalues: TweenService (ref)
                    local v23 = TweenService:Create(p22, TweenInfo.new(0.45), {
                        ImageTransparency = 0
                    });
                    v23:Play();

                    return v23;
                end;

                local function v26(p24) -- Line: 40
                    -- upvalues: TweenService (ref)
                    local v25 = TweenService:Create(p24, TweenInfo.new(0.4, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                        Size = UDim2.fromScale(0.5, 1)
                    });
                    v25:Play();

                    return v25;
                end;

                local function u27() -- Line: 47
                    -- upvalues: TweenService (ref), u10 (ref), u11 (ref)
                    TweenService:Create(u10, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut), {
                        TextTransparency = 0,
                        Position = UDim2.fromScale(0.5, 0.25)
                    }):Play();
                    TweenService:Create(u11, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut), {
                        TextTransparency = 0,
                        Position = UDim2.fromScale(0.5, 1)
                    }):Play();
                end;

                TweenService:Create(u12, TweenInfo.new(0.45), {
                    ImageTransparency = 0
                }):Play();
                local v28 = v26(u12);

                if u13 then
                    TweenService:Create(u13, TweenInfo.new(0.45), {
                        ImageTransparency = 0
                    }):Play();
                    v26(u13);
                end;

                v28.Completed:Connect(function() -- Line: 64
                    -- upvalues: u27 (copy)
                    u27();
                end);
            end)();
            v21();
        end, {});

        local function v33() -- Line: 120
            -- upvalues: u2 (ref), ColorUtil (ref), GameTheme (ref), DeviceUtil (ref), u4 (copy), u6 (copy)
            local createFragment = u2.createFragment;
            local v29 = {};
            local createElement = u2.createElement;
            local v30 = {
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.fromScale(0.5, 0.35),
                Size = UDim2.fromScale(0.9, 0.6)
            };
            local v31 = ColorUtil.richTextColor(GameTheme.Gray);
            local v32 = DeviceUtil.isSmallScreen() and 1.5 or 2.5;
            v30.Text = "<stroke color=\"" .. v31 .. "\" thickness=\"" .. tostring(v32 * 2) .. "\">" .. string.upper(u4.Announcement.message) .. "</stroke>";
            v30.TextColor3 = ColorUtil.WHITE;
            v30.TextScaled = true;
            v30.RichText = true;
            v30.Font = Enum.Font.GothamBlack;
            v30.SizeConstraint = "RelativeXY";
            v30.BackgroundTransparency = 1;
            v30.TextTransparency = 1;
            v30[u2.Ref] = u6;
            v30.ZIndex = 4;
            v29.AnnouncementMessage = createElement("TextLabel", v30);

            return createFragment(v29);
        end;

        local v34 = {
            ResetOnSpawn = false
        };
        local v35 = {};
        local backgroundImage = u4.Announcement.backgroundImage;
        local v36;

        if backgroundImage == "" or not backgroundImage then
            v36 = u2.createFragment({
                AnnouncementBGStroke = u2.createFragment({
                    AnnouncementBGStroke = u2.createElement("ImageLabel", {
                        Image = "rbxassetid://9338854959",
                        ImageTransparency = 1,
                        AnchorPoint = Vector2.new(0.5, 0.5),
                        Position = UDim2.fromScale(0.5, 0.5),
                        Size = UDim2.fromScale(0, 1),
                        BackgroundTransparency = 1,
                        ImageColor3 = themeColor,
                        [u2.Ref] = u8,
                        ScaleType = "Slice",
                        SliceCenter = Rect.new(162, 13, 162, 13),
                        SliceScale = 4,
                        ZIndex = 2
                    }, { u2.createElement("UIAspectRatioConstraint", {
                            AspectRatio = 12.617647058823529
                        }), u2.createElement(v33) })
                }),
                AnnouncementBackground = u2.createFragment({
                    AnnouncementBackground = u2.createElement("ImageLabel", {
                        Image = "rbxassetid://9338855112",
                        ImageTransparency = 1,
                        AnchorPoint = Vector2.new(0.5, 0.5),
                        Position = UDim2.fromScale(0.5, 0.5),
                        Size = UDim2.fromScale(0, 1),
                        BackgroundTransparency = 1,
                        [u2.Ref] = u7,
                        ScaleType = "Slice",
                        SliceCenter = Rect.new(162, 13, 162, 13),
                        SliceScale = 4,
                        ZIndex = 3
                    }, { u2.createElement("UIAspectRatioConstraint", {
                            AspectRatio = 12.617647058823529
                        }) })
                })
            });
        else
            local v37 = {};
            local backgroundImage2 = u4.Announcement.backgroundImage;
            v37.Image = backgroundImage2 == nil and "rbxassetid://9290915178" or backgroundImage2;
            v37.ImageTransparency = 1;
            v37.AnchorPoint = Vector2.new(0.5, 0.5);
            v37.Position = UDim2.fromScale(0.5, 0.5);
            v37.Size = UDim2.fromScale(0, 1);
            v37.BackgroundTransparency = 1;
            v37.ImageColor3 = themeColor;
            v37[u2.Ref] = u7;
            v36 = u2.createFragment({
                AnnouncementBackground = u2.createElement("ImageLabel", v37, { u2.createElement("UIAspectRatioConstraint", {
                        AspectRatio = 12.617647058823529
                    }), u2.createElement(v33) })
            });
        end;

        local v38 = {
            AnchorPoint = Vector2.new(0.5, 0),
            Position = u4.Announcement.position or UDim2.fromScale(0.5, 0.125),
            Size = UDim2.new(0.65, 0, 0.1, 0)
        };
        local v39 = {
            [#v39 + 1] = v36
        };
        local createElement = u2.createElement;
        local v40 = {
            PostText = "</stroke>",
            EndTime = u4.Announcement.endTime,
            CountdownConfig = {
                days = false,
                hours = false
            },
            TextLabel = {
                TextTransparency = 1,
                Size = UDim2.fromScale(1, 0.5),
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.fromScale(0.5, 0.9),
                Ref = u9
            }
        };
        local v41 = ColorUtil.richTextColor(GameTheme.Gray);
        local v42 = DeviceUtil.isSmallScreen() and 1.5 or 2.5;
        v40.PreText = "<stroke color=\"" .. v41 .. "\" thickness=\"" .. tostring(v42 * 2) .. "\">";
        v39[#v39 + 1] = createElement(Countdown, v40);
        v35[#v35 + 1] = u2.createElement(Empty, v38, v39);

        return u2.createFragment({
            CountdownAnnouncement = u2.createElement("ScreenGui", v34, v35)
        });
    end)
};