-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Flamework = u1.import(script, u1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local v3 = u1.import(script, u1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = u1.import(script, u1.getModule(script, "@rbxts", "services")).TweenService;
local Empty = u1.import(script, script.Parent.Parent.Parent.Parent, "ui", "lib", "components", "empty").Empty;
local AnnouncementUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "announcement", "announcement-util").AnnouncementUtil;
local ColorUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local DeviceUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;

return {
    TextAnnouncementBordered = v3.new(u2)(function(u4, p5) -- Line: 11
        -- upvalues: ColorUtil (copy), TweenService (copy), u1 (copy), AnnouncementUtil (copy), DeviceUtil (copy), Flamework (copy), u2 (copy), Empty (copy)
        local useEffect = p5.useEffect;
        local v6 = u4.Announcement.themeColor or ColorUtil.hexColor(16773227);
        local u7 = nil;
        local u8 = nil;
        local u9 = nil;
        local u10 = nil;
        useEffect(function() -- Line: 15
            -- upvalues: u7 (ref), u8 (ref), u4 (copy), u9 (ref), u10 (ref), TweenService (ref), u1 (ref), AnnouncementUtil (ref), DeviceUtil (ref), Flamework (ref)
            local u11 = u7:getValue();
            local u12 = u8:getValue();
            local backgroundImage = u4.Announcement.backgroundImage;
            local v13;

            if backgroundImage == "" or not backgroundImage then
                v13 = u10:getValue();
            else
                v13 = u9:getValue();
            end;

            if not (u11 and (v13 and u12)) then
                return nil;
            end;

            local v14 = TweenService:Create(v13, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                Size = UDim2.fromScale(0.5, 1)
            });
            local backgroundImage2 = u4.Announcement.backgroundImage;
            local v15 = (backgroundImage2 == "" or not backgroundImage2) and {
                BackgroundTransparency = 0
            } or {
                ImageTransparency = 0
            };
            local backgroundImage3 = u4.Announcement.backgroundImage;
            local v16 = (backgroundImage3 == "" or not backgroundImage3) and {
                BackgroundTransparency = 1
            } or {
                ImageTransparency = 1
            };
            TweenService:Create(v13, TweenInfo.new(0.3), v15):Play();
            v14:Play();
            v14.Completed:Connect(function() -- Line: 42
                -- upvalues: TweenService (ref), u11 (copy), u12 (copy)
                TweenService:Create(u11, TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                    TextTransparency = 0
                }):Play();
                TweenService:Create(u12, TweenInfo.new(0.4, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                    Transparency = 0
                }):Play();
            end);
            local u17 = TweenService:Create(v13, TweenInfo.new(0.3), v16);
            local u18 = TweenService:Create(v13, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                Size = UDim2.fromScale(0, 1)
            });
            local u19 = TweenService:Create(u11, TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                TextTransparency = 1
            });
            local u20 = TweenService:Create(u12, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                Transparency = 1
            });
            local duration = u4.Announcement.duration;

            if duration == nil then
                duration = AnnouncementUtil.DEFAULT_DURATION;
            end;

            u1.Promise.delay(duration):andThen(function() -- Line: 71
                -- upvalues: u19 (copy), u20 (copy), u17 (copy), u18 (copy), DeviceUtil (ref), Flamework (ref)
                u19:Play();
                u20:Play();
                u17:Play();
                u18:Play();

                if DeviceUtil.isHoarceKat() then
                    return nil;
                end;

                u19.Completed:Connect(function() -- Line: 79
                    -- upvalues: Flamework (ref)
                    Flamework.resolveDependency("@easy-games/game-core:client/controllers/announcement/announcement-controller@AnnouncementController"):endAnnouncement();
                end);
            end);
        end, {});
        u7 = u2.createRef();
        u10 = u2.createRef();
        u9 = u2.createRef();
        u8 = u2.createRef();
        local v21 = {
            ResetOnSpawn = false
        };
        local v22 = {};
        local backgroundImage = u4.Announcement.backgroundImage;
        local v23 = {
            AnchorPoint = Vector2.new(0.5, 0),
            Position = u4.Announcement.position or UDim2.fromScale(0.5, 0.05),
            Size = UDim2.fromScale(0.9, 0.07)
        };
        local v24 = {};
        local v25;

        if backgroundImage == "" or not backgroundImage then
            v25 = u2.createFragment({
                AnnouncementBackground = u2.createElement("Frame", {
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    Position = UDim2.fromScale(0.5, 0.5),
                    Size = UDim2.fromScale(0, 1),
                    BackgroundTransparency = 1,
                    [u2.Ref] = u10
                }, {
                    u2.createElement("UIGradient", {
                        Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, ColorUtil.BLACK), ColorSequenceKeypoint.new(1, ColorUtil.BLACK) }),
                        Transparency = NumberSequence.new({
                            NumberSequenceKeypoint.new(0, 1),
                            NumberSequenceKeypoint.new(0.1, 0.7),
                            NumberSequenceKeypoint.new(0.2, 0.5),
                            NumberSequenceKeypoint.new(0.8, 0.5),
                            NumberSequenceKeypoint.new(0.9, 0.7),
                            NumberSequenceKeypoint.new(1, 1)
                        })
                    }),
                    TopBorder = u2.createElement("Frame", {
                        BackgroundTransparency = 0,
                        BorderSizePixel = 0,
                        AnchorPoint = Vector2.new(0, 0),
                        Position = UDim2.fromScale(0, -0.03),
                        Size = UDim2.fromScale(1, 0.03),
                        BackgroundColor3 = ColorUtil.WHITE
                    }, { u2.createElement("UIGradient", {
                            Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, v6), ColorSequenceKeypoint.new(1, v6) }),
                            Transparency = NumberSequence.new({
                                NumberSequenceKeypoint.new(0, 1),
                                NumberSequenceKeypoint.new(0.1, 0.6),
                                NumberSequenceKeypoint.new(0.25, 0.1),
                                NumberSequenceKeypoint.new(0.75, 0.1),
                                NumberSequenceKeypoint.new(0.9, 0.6),
                                NumberSequenceKeypoint.new(1, 1)
                            })
                        }) }),
                    BottomBorder = u2.createElement("Frame", {
                        BackgroundTransparency = 0,
                        BorderSizePixel = 0,
                        AnchorPoint = Vector2.new(0, 1),
                        Position = UDim2.fromScale(0, 1.03),
                        Size = UDim2.fromScale(1, 0.03),
                        BackgroundColor3 = ColorUtil.WHITE
                    }, { u2.createElement("UIGradient", {
                            Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, v6), ColorSequenceKeypoint.new(1, v6) }),
                            Transparency = NumberSequence.new({
                                NumberSequenceKeypoint.new(0, 1),
                                NumberSequenceKeypoint.new(0.1, 0.6),
                                NumberSequenceKeypoint.new(0.25, 0.1),
                                NumberSequenceKeypoint.new(0.75, 0.1),
                                NumberSequenceKeypoint.new(0.9, 0.6),
                                NumberSequenceKeypoint.new(1, 1)
                            })
                        }) })
                })
            });
        else
            v25 = u2.createFragment({
                AnnouncementBackground = u2.createElement("ImageLabel", {
                    Image = u4.Announcement.backgroundImage,
                    ImageTransparency = 0,
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    Position = UDim2.fromScale(0.5, 0.5),
                    Size = UDim2.fromScale(0, 1),
                    BackgroundTransparency = 1,
                    [u2.Ref] = u9
                })
            });
        end;

        v24[#v24 + 1] = v25;
        local _ = #v24;
        local v26 = {
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.fromScale(0.5, 0.5),
            Size = UDim2.fromScale(0.9, 0.6),
            Text = string.upper(u4.Announcement.message),
            TextColor3 = ColorUtil.WHITE,
            Font = Enum.Font.GothamBlack,
            TextScaled = true,
            RichText = true,
            SizeConstraint = "RelativeXY",
            BackgroundTransparency = 1,
            TextTransparency = 1,
            [u2.Ref] = u7
        };
        local textlabel = u4.Announcement.textlabel;

        if textlabel ~= nil then
            textlabel = textlabel.props;
        end;

        if textlabel then
            for i, v in textlabel do
                v26[i] = v;
            end;
        end;

        v24.AnnouncementMessage = u2.createElement("TextLabel", v26, { u2.createElement("UIStroke", {
                Color = ColorUtil.BLACK,
                Thickness = 2,
                [u2.Ref] = u8,
                Transparency = 1
            }) });
        v22[#v22 + 1] = u2.createElement(Empty, v23, v24);

        return u2.createFragment({
            TextAnnouncementBordered = u2.createElement("ScreenGui", v21, v22)
        });
    end)
};