-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Flamework = u1.import(script, u1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local v3 = u1.import(script, u1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = u1.import(script, u1.getModule(script, "@rbxts", "services")).TweenService;
local Empty = u1.import(script, script.Parent.Parent.Parent.Parent, "ui", "lib", "components", "empty").Empty;
local AnnouncementUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "announcement", "announcement-util").AnnouncementUtil;
local SoundManager = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local DeviceUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;

return {
    CenterImageAnnouncement = v3.new(u2)(function(u4, p5) -- Line: 11
        -- upvalues: TweenService (copy), SoundManager (copy), u1 (copy), AnnouncementUtil (copy), DeviceUtil (copy), Flamework (copy), u2 (copy), Empty (copy)
        local u6 = nil;
        p5.useEffect(function() -- Line: 14
            -- upvalues: u6 (ref), TweenService (ref), SoundManager (ref), u1 (ref), u4 (copy), AnnouncementUtil (ref), DeviceUtil (ref), Flamework (ref)
            local v7 = u6:getValue();

            if not v7 then
                return nil;
            end;

            TweenService:Create(v7, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                Size = UDim2.fromScale(0.5, 1)
            }):Play();
            TweenService:Create(v7, TweenInfo.new(0.3), {
                ImageTransparency = 0
            }):Play();
            SoundManager:playSound("rbxassetid://8537464850");
            local u8 = TweenService:Create(v7, TweenInfo.new(0.3), {
                ImageTransparency = 1
            });
            local u9 = TweenService:Create(v7, TweenInfo.new(0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.InOut), {
                Size = UDim2.fromScale(0, 1)
            });
            local duration = u4.Announcement.duration;

            if duration == nil then
                duration = AnnouncementUtil.DEFAULT_DURATION;
            end;

            u1.Promise.delay(duration):andThen(function() -- Line: 42
                -- upvalues: u8 (copy), u9 (copy), DeviceUtil (ref), Flamework (ref), u4 (ref)
                u8:Play();
                u9:Play();

                if DeviceUtil.isHoarceKat() then
                    return nil;
                end;

                u9.Completed:Connect(function() -- Line: 48
                    -- upvalues: Flamework (ref), u4 (ref)
                    Flamework.resolveDependency("@easy-games/game-core:client/controllers/announcement/announcement-controller@AnnouncementController"):endAnnouncement(u4.Announcement.important);
                end);
            end);
        end, {});
        u6 = u2.createRef();

        return u2.createFragment({
            ImageAnnouncement = u2.createElement("ScreenGui", {
                ResetOnSpawn = false
            }, { u2.createElement(Empty, {
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    Position = u4.Announcement.position or UDim2.fromScale(0.5, 0.4),
                    Size = UDim2.fromScale(0.6, 0.5)
                }, {
                    AnnouncementBackground = u2.createElement("ImageLabel", {
                        Image = u4.Announcement.backgroundImage,
                        ImageTransparency = 1,
                        AnchorPoint = Vector2.new(0.5, 0.5),
                        Position = UDim2.fromScale(0.5, 0.5),
                        Size = UDim2.fromScale(0, 1),
                        BackgroundTransparency = 1,
                        ScaleType = "Fit",
                        [u2.Ref] = u6
                    })
                }) })
        });
    end)
};