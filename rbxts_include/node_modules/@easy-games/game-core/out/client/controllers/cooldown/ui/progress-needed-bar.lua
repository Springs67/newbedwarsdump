-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local ProgressBar = v1.import(script, script.Parent, "progress-bar").ProgressBar;
local v4 = u3.Component:extend("ProgressNeededBar");

function v4.init(p5, p6) -- Line: 9
    -- upvalues: u2 (copy)
    p5.maid = u2.new();
    p5.state = {
        progress = p6.progress.Value
    };
end;

function v4.didMount(u7) -- Line: 15
    u7.maid:GiveTask(u7.props.progress.Changed:Connect(function(p8) -- Line: 16
        -- upvalues: u7 (copy)
        u7:setState({
            progress = p8
        });
    end));
end;

function v4.willUnmount(p9) -- Line: 22
    p9.maid:DoCleaning();
end;

function v4.render(p10) -- Line: 25
    -- upvalues: u3 (copy), ProgressBar (copy)
    local v11 = math.clamp(p10.state.progress / p10.props.progressNeeded, 0.04, 1);
    local v12 = {
        BarColor = p10.props.BarColor,
        BarGradient = p10.props.BarGradient,
        GradientRotation = p10.props.GradientRotation,
        Position = p10.props.Position,
        Size = p10.props.Size,
        AnchorPoint = p10.props.AnchorPoint,
        Pills = p10.props.Pills,
        DividerColor = p10.props.DividerColor,
        DividerTransparency = p10.props.DividerTransparency
    };
    local v13 = {
        LayoutOrder = p10.props.LayoutOrder
    };

    for i, v in v12 do
        v13[i] = v;
    end;

    v13.Size = UDim2.fromScale(0.5, 0.04);
    v13.Progress = v11;

    return u3.createElement(ProgressBar, v13);
end;

return {
    ProgressNeededBar = v4
};