-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local UIUtil = v1.import(script, script.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").UIUtil;

return {
    ActionBar = v3.new(u2)(function(p4, p5) -- Line: 8
        -- upvalues: UIUtil (copy), GameTheme (copy), Flamework (copy), u2 (copy)
        local _ = p5.useState;
        local useEffect = p5.useEffect;
        local _ = p5.useMemo;
        local Position = p4.Position;

        if Position ~= nil then
            Position = Position.Y.Scale;
        end;

        if Position == nil then
            Position = UIUtil:getActionBarPosition().Y.Scale;
        end;

        local v6 = Position - GameTheme.centerTopAnnouncementCardSize.Y.Scale;
        useEffect(function() -- Line: 21
            -- upvalues: Flamework (ref)
            local v7 = Flamework.resolveDependency("@easy-games/game-core:client/controllers/action-bar/action-bar-controller@ActionBarController").actionBarRef:getValue();

            if v7 then
                Flamework.resolveDependency("@easy-games/game-core:client/controllers/action-bar/action-bar-controller@ActionBarController").actionBarMounted:Fire(Flamework.resolveDependency("@easy-games/game-core:client/controllers/action-bar/action-bar-controller@ActionBarController").actionBarRef, v7);
            end;
        end, {});

        return u2.createFragment({
            ActionBar = u2.createElement("Frame", {
                Position = p4.Position or UIUtil:getActionBarPosition(),
                AnchorPoint = p4.AnchorPoint or Vector2.new(0.5, 1),
                Size = p4.Size or UDim2.fromScale(0.35, v6),
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                [u2.Ref] = Flamework.resolveDependency("@easy-games/game-core:client/controllers/action-bar/action-bar-controller@ActionBarController").actionBarRef,
                LayoutOrder = p4.LayoutOrder
            }, { u2.createElement("UIListLayout", {
                    FillDirection = Enum.FillDirection.Vertical,
                    HorizontalAlignment = Enum.HorizontalAlignment.Center,
                    VerticalAlignment = Enum.VerticalAlignment.Bottom,
                    SortOrder = Enum.SortOrder.LayoutOrder,
                    Padding = UDim.new(0.02, 0)
                }) })
        });
    end)
};