-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local Flamework = u1.import(script, u1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local StoreProvider = u1.import(script, u1.getModule(script, "@rbxts", "roact-rodux").src).StoreProvider;
local u3 = u1.import(script, u1.getModule(script, "@rbxts", "rodux").src);
local GameCoreReducers = u1.import(script, script.Parent.Parent, "core").GameCoreReducers;
local NotificationApp = u1.import(script, script.Parent, "notification-app").NotificationApp;

return {
    renderNotificationAppStory = function(p4) -- Line: 9
        -- upvalues: u3 (copy), GameCoreReducers (copy), u2 (copy), StoreProvider (copy), NotificationApp (copy), u1 (copy), Flamework (copy)
        local v5 = u3.Store.new(GameCoreReducers);
        local u6 = u2.mount(u2.createElement(StoreProvider, {
            store = v5
        }, { u2.createElement(NotificationApp, {
                AppId = "1"
            }) }));
        local u7 = true;
        u1.Promise.defer(function() -- Line: 19
            -- upvalues: Flamework (ref)
            wait(0.1);
            Flamework.resolveDependency("@easy-games/game-core:client/controllers/notification-controller@NotificationController"):sendErrorNotification({
                message = "Only party leaders can join the queue."
            });
        end);
        u1.Promise.defer(function() -- Line: 25
            -- upvalues: Flamework (ref)
            wait(0.05);
            Flamework.resolveDependency("@easy-games/game-core:client/controllers/notification-controller@NotificationController"):sendInfoNotification({
                message = "This is an info notification."
            });
        end);

        return function() -- Line: 31
            -- upvalues: u7 (ref), u2 (ref), u6 (copy)
            u7 = false;
            u2.unmount(u6);
        end;
    end
};