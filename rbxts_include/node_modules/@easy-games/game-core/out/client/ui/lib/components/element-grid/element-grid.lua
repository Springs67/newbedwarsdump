-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local AutoCanvasScrollingFrame = v1.import(script, script.Parent.Parent, "auto-canvas-scrolling-frame").AutoCanvasScrollingFrame;

return {
    ElementGrid = v3.new(u2)(function(p4, p5) -- Line: 6
        -- upvalues: u2 (copy), AutoCanvasScrollingFrame (copy)
        local DefaultElement = p4.DefaultElement;
        local u6, u7 = p5.useState(DefaultElement == nil and 0 or DefaultElement);
        local v8 = {};

        for i, v in p4 do
            v8[i] = v;
        end;

        local ElementSize = v8.ElementSize;

        if ElementSize == nil then
            ElementSize = UDim2.new(0.317, 0, 0, 90);
        end;

        local ElementPadding = v8.ElementPadding;

        if ElementPadding == nil then
            ElementPadding = UDim2.fromOffset(8, 8);
        end;

        local u9 = {};
        local v10 = p4[u2.Children];

        if v10 ~= nil then
            local function _(p11, p12) -- Line: 30
                -- upvalues: u6 (copy), u7 (copy), u9 (copy), u2 (ref)
                p11.props.CurrentIndex = u6;
                p11.props.SetSelectedIndex = u7;
                local v13 = u2.createElement(p11.component, p11.props);
                table.insert(u9, v13);
            end;

            for _, v in v10 do
                v.props.CurrentIndex = u6;
                v.props.SetSelectedIndex = u7;
                local v14 = u2.createElement(v.component, v.props);
                table.insert(u9, v14);
            end;
        end;

        local v15 = {};
        local v16 = {};
        local ZIndex = p4.ZIndex;
        v16.ZIndex = ZIndex == nil and 1 or ZIndex;
        v16.BackgroundTransparency = 1;
        v16.BorderSizePixel = 0;
        v16.ScrollBarThickness = 8;
        v16.ScrollBarImageColor3 = Color3.fromRGB(172, 176, 255);
        local ScrollingFrameProps = p4.ScrollingFrameProps;

        if type(ScrollingFrameProps) == "table" then
            for i, v in ScrollingFrameProps do
                v16[i] = v;
            end;
        end;

        v15.ScrollingFrameProps = v16;
        local v17 = {};
        local v18 = #v17;
        local v19 = {
            PaddingRight = UDim.new(0, 8)
        };
        local UIPaddingProps = p4.UIPaddingProps;

        if UIPaddingProps then
            for i, v in UIPaddingProps do
                v19[i] = v;
            end;
        end;

        v17[v18 + 1] = u2.createElement("UIPadding", v19);
        local v20 = {
            CellSize = ElementSize,
            CellPadding = ElementPadding,
            FillDirection = Enum.FillDirection.Horizontal,
            HorizontalAlignment = Enum.HorizontalAlignment.Left,
            VerticalAlignment = Enum.VerticalAlignment.Top,
            SortOrder = Enum.SortOrder.LayoutOrder
        };
        local UIGridLayoutProps = p4.UIGridLayoutProps;

        if UIGridLayoutProps then
            for i, v in UIGridLayoutProps do
                v20[i] = v;
            end;
        end;

        v17[v18 + 2] = u2.createElement("UIGridLayout", v20);

        for i, v in u9 do
            v17[v18 + 2 + i] = v;
        end;

        return u2.createElement(AutoCanvasScrollingFrame, v15, v17);
    end)
};