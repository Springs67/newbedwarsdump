-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;

return {
    LineButton = v3.new(u2)(function(u4, p5) -- Line: 9
        -- upvalues: u2 (copy), SoundManager (copy), GameCoreClientConfig (copy), TweenService (copy), ColorUtil (copy)
        local _ = p5.useState;
        local u6 = u2.createRef();
        local u7 = u2.createRef();
        local createElement = u2.createElement;
        local v15 = {
            Size = u4.Size,
            Position = UDim2.fromScale(0.5, 0.5),
            AnchorPoint = Vector2.new(0.5, 0.5),

            [u2.Event.MouseButton1Click] = function() -- Line: 17
                -- upvalues: u4 (copy)
                if not u4.Enabled then
                    return nil;
                end;

                u4.OnClick();
            end,

            [u2.Event.MouseEnter] = function() -- Line: 23
                -- upvalues: u4 (copy), SoundManager (ref), GameCoreClientConfig (ref), u7 (copy), u6 (copy), TweenService (ref)
                if not u4.Enabled then
                    return nil;
                end;

                SoundManager:playSound(GameCoreClientConfig.Sounds.UI_HOVER);

                if u7:getValue() then
                    u7:getValue().Text = "<u><b>" .. u4.Text .. "</b></u>";
                end;

                local v8 = TweenInfo.new(0.09, Enum.EasingStyle.Linear);
                local v9 = u6:getValue();
                local v10, v11, v12 = u4.Color3:ToHSV();

                if v9 then
                    TweenService:Create(v9, v8, {
                        ImageColor3 = Color3.fromHSV(v10, v11 * 0.95, v12 * 0.95)
                    }):Play();
                end;
            end,

            [u2.Event.MouseLeave] = function() -- Line: 40
                -- upvalues: u4 (copy), u7 (copy), u6 (copy), TweenService (ref)
                if not u4.Enabled then
                    return nil;
                end;

                if u7:getValue() then
                    u7:getValue().Text = "<b>" .. u4.Text .. "</b>";
                end;

                local v13 = TweenInfo.new(0.09, Enum.EasingStyle.Linear);
                local v14 = u6:getValue();

                if v14 then
                    TweenService:Create(v14, v13, {
                        ImageColor3 = u4.Color3
                    }):Play();
                end;
            end,

            AutoButtonColor = false,
            BackgroundTransparency = 1,
            Image = "rbxassetid://7287834372"
        };
        local v16;

        if u4.Enabled then
            v16 = u4.Color3;
        else
            v16 = ColorUtil.hexColor(8882055);
        end;

        v15.ImageColor3 = v16;
        v15[u2.Ref] = u6;

        return createElement("ImageButton", v15, { u2.createElement("TextLabel", {
                Font = "RobotoCondensed",
                Text = "<b>" .. u4.Text .. "</b>",
                RichText = true,
                TextColor3 = ColorUtil.hexColor(16776956),
                TextScaled = true,
                TextXAlignment = "Center",
                TextYAlignment = "Center",
                BackgroundTransparency = 1,
                Size = UDim2.fromScale(0.9, 0.6),
                Position = UDim2.fromScale(0.5, 0.5),
                AnchorPoint = Vector2.new(0.5, 0.5),
                [u2.Ref] = u7
            }) });
    end)
};