-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local ImageId = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "images", "image-id").ImageId;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local v4 = u3.Component:extend("WindowHeader");

function v4.init(p5) -- Line: 14
    -- upvalues: u2 (copy)
    p5.minimizeHoverMaid = u2.new();
end;

function v4.render(u6) -- Line: 17
    -- upvalues: u3 (copy), GameTheme (copy), ImageId (copy), SoundManager (copy), GameCoreClientConfig (copy), Flamework (copy), TweenService (copy)
    local v7 = {};

    for i, v in u6.props do
        v7[i] = v;
    end;

    v7[u3.Children] = nil;
    v7.LeftText = nil;
    v7.AppId = nil;
    local v8 = {
        BorderSizePixel = 0,
        BackgroundColor3 = GameTheme.backgroundSecondary
    };

    for i, v in v7 do
        v8[i] = v;
    end;

    local v9 = {};
    local v10 = #v9;
    local v11 = u6.props[u3.Children];

    if v11 then
        for i, v in v11 do
            if type(i) == "number" then
                v9[v10 + i] = v;
            else
                v9[i] = v;
            end;
        end;
    end;

    local v12 = #v9;
    local v13;

    if u6.props.LeftText == nil then
        v13 = false;
    else
        v13 = u3.createElement("TextLabel", {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            RichText = true,
            TextScaled = true,
            TextXAlignment = "Left",
            Text = u6.props.LeftText,
            Size = UDim2.fromScale(1, 0.5),
            Position = UDim2.fromScale(0.5, 0.5),
            AnchorPoint = Vector2.new(0.5, 0.5),
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }, { u3.createElement("UIPadding", {
                PaddingLeft = UDim.new(0.03, 0)
            }) });
    end;

    if v13 then
        v9[v12 + 1] = v13;
    end;

    v9[#v9 + 1] = u3.createElement("ImageButton", {
        Image = ImageId.MINIMIZE,
        Size = UDim2.fromScale(0.7, 0.7),
        SizeConstraint = "RelativeYY",
        Position = UDim2.fromScale(0.97, 0.5),
        AnchorPoint = Vector2.new(1, 0.5),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,

        [u3.Event.MouseButton1Click] = function() -- Line: 74
            -- upvalues: SoundManager (ref), GameCoreClientConfig (ref), Flamework (ref), u6 (copy)
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
            Flamework.resolveDependency("@easy-games/game-core:client/controllers/app-controller@AppController"):closeApp(u6.props.AppId);
        end,

        [u3.Event.MouseEnter] = function(p14) -- Line: 78
            -- upvalues: TweenService (ref), u6 (copy)
            local u15 = TweenService:Create(p14, TweenInfo.new(0.12), {
                ImageTransparency = 0.4
            });
            u15:Play();
            u6.minimizeHoverMaid:GiveTask(function() -- Line: 83
                -- upvalues: u15 (copy)
                u15:Cancel();
            end);
        end,

        [u3.Event.MouseLeave] = function(p16) -- Line: 87
            -- upvalues: TweenService (ref), u6 (copy)
            local u17 = TweenService:Create(p16, TweenInfo.new(0.12), {
                ImageTransparency = 0
            });
            u17:Play();
            u6.minimizeHoverMaid:GiveTask(function() -- Line: 92
                -- upvalues: u17 (copy)
                u17:Cancel();
            end);
        end
    });

    return u3.createElement("Frame", v8, v9);
end;

return {
    WindowHeader = v4
};