-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-rodux").src);
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local GetTarmacAsset = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "tarmac", "tarmac-helpers").GetTarmacAsset;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local DeviceUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;
local DynamicCalcContainerSize = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").DynamicCalcContainerSize;
local CornerFiller = v1.import(script, script.Parent, "corner-filler").CornerFiller;
local EmptyButton = v1.import(script, script.Parent, "empty-button").EmptyButton;
local v4 = v1.import(script, script.Parent, "tabs", "tabs-component");
local TabComponentVariant = v4.TabComponentVariant;
local TabsComponent = v4.TabsComponent;
local u5 = GetTarmacAsset("CloseIcon");
local v6 = u2.PureComponent:extend("WidgetComponentBase");

function v6.init(u7) -- Line: 24
    -- upvalues: SoundManager (copy), GameCoreClientConfig (copy), Flamework (copy), u2 (copy)
    u7.defaultProps = {
        CornerRadius = 10,

        OnClose = function() -- Line: 27, Name: OnClose
            -- upvalues: SoundManager (ref), GameCoreClientConfig (ref), Flamework (ref), u7 (copy)
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
            Flamework.resolveDependency("@easy-games/game-core:client/controllers/app-controller@AppController"):closeApp(u7.props.AppId);
        end
    };
    u7.containerRef = u2.createRef();
    u7.contentRef = u2.createRef();
end;

function v6.didMount(p8) -- Line: 35
    -- upvalues: DynamicCalcContainerSize (copy)
    local AutomaticContainerSize = p8.props.AutomaticContainerSize;

    if AutomaticContainerSize == nil then
        AutomaticContainerSize = false;
    end;

    local v9 = p8.containerRef:getValue();
    local v10 = p8.contentRef:getValue();

    if AutomaticContainerSize then
        p8.cleanUp = DynamicCalcContainerSize(v9, v10:FindFirstChildWhichIsA("UIGridStyleLayout"), 70);
    end;
end;

function v6.willUnmount(p11) -- Line: 47
    if p11.cleanUp then
        p11.cleanUp();
    end;
end;

function v6.render(u12) -- Line: 52
    -- upvalues: u2 (copy), GameTheme (copy), CornerFiller (copy), DeviceUtil (copy), TabComponentVariant (copy), TabsComponent (copy), u5 (copy), EmptyButton (copy)
    local props = u12.props;
    local AutomaticSize = props.AutomaticSize;

    if AutomaticSize == nil then
        AutomaticSize = Enum.AutomaticSize.None;
    end;

    local CornerRadius = props.CornerRadius;
    local v13 = CornerRadius == nil and 10 or CornerRadius;
    local Title = props.Title;
    local SubTitle = props.SubTitle;
    local AspectRatio = props.AspectRatio;
    local _ = props.HideEventThemeing == nil;
    local _ = props.CornerFillerProps;
    local v14 = {
        BackgroundTransparency = 1,
        Modal = true,
        AnchorPoint = props.AnchorPoint,
        Position = props.Position,
        Size = props.Size,
        AutomaticSize = AutomaticSize,
        ObjRef = u12.containerRef
    };
    local v15 = {};
    local v16 = #v15;
    local v17;

    if AspectRatio == nil then
        v17 = nil;
    else
        v17 = u2.createElement("UIAspectRatioConstraint", {
            AspectRatio = AspectRatio
        });
    end;

    if v17 then
        v15[v16 + 1] = v17;
    end;

    local _ = #v15;
    local v18 = {
        BorderSizePixel = 0,
        ZIndex = 4,
        LayoutOrder = 1,
        Size = UDim2.new(1, 0, 0, 40),
        BackgroundColor3 = GameTheme.backgroundPrimary
    };
    local v19 = { u2.createElement("UICorner", {
            CornerRadius = UDim.new(0, v13)
        }), u2.createElement(CornerFiller, {
            BottomLeft = true,
            BottomRight = true,
            ZIndex = 5
        }) };
    local v20 = #v19;
    local v21 = {
        BackgroundTransparency = 1,
        ZIndex = 4,
        Size = UDim2.fromScale(0.2, 1)
    };
    local v22 = { u2.createElement("UIListLayout", {
            FillDirection = Enum.FillDirection.Horizontal,
            HorizontalAlignment = Enum.HorizontalAlignment.Left,
            VerticalAlignment = Enum.VerticalAlignment.Center,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Padding = UDim.new(0, 10)
        }), u2.createElement("UIPadding", {
            PaddingTop = UDim.new(0, 0),
            PaddingBottom = UDim.new(0, 0),
            PaddingLeft = UDim.new(0, 15),
            PaddingRight = UDim.new(0, 15)
        }) };
    local v23 = #v22;
    local v24;

    if u12.props.TitleIcon == nil then
        v24 = false;
    else
        v24 = u2.createElement("ImageLabel", {
            ScaleType = "Fit",
            SizeConstraint = "RelativeYY",
            BackgroundTransparency = 1,
            ZIndex = 4,
            Image = u12.props.TitleIcon,
            Size = UDim2.fromScale(0.5, 0.5)
        });
    end;

    if v24 then
        v22[v23 + 1] = v24;
    end;

    local _ = #v22;
    local v25 = {
        AutomaticSize = "X",
        BackgroundTransparency = 1,
        ZIndex = 4,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.fromScale(0, 0.5),
        Size = UDim2.new(0, 0, 1, 0)
    };
    local v26 = {};
    local createElement = u2.createElement;
    local v27 = {
        FillDirection = Enum.FillDirection.Horizontal,
        HorizontalAlignment = Enum.HorizontalAlignment.Left
    };
    local v28;

    if DeviceUtil.isSmallScreen() then
        v28 = Enum.VerticalAlignment.Bottom;
    else
        v28 = Enum.VerticalAlignment.Center;
    end;

    v27.VerticalAlignment = v28;
    v27.SortOrder = Enum.SortOrder.LayoutOrder;
    v27.Padding = UDim.new(0, 10);
    v26[1] = createElement("UIListLayout", v27);
    v26.Title = u2.createElement("TextLabel", {
        BackgroundTransparency = 1,
        RichText = true,
        TextSize = 18,
        ZIndex = 4,
        Size = UDim2.fromScale(0, 1),
        AutomaticSize = Enum.AutomaticSize.X,
        TextXAlignment = Enum.TextXAlignment.Left,
        Text = "<b>" .. Title .. "</b>",
        TextColor3 = Color3.new(1, 1, 1),
        Font = Enum.Font.Roboto
    });
    local v29 = #v26;
    local v30;

    if SubTitle == nil then
        v30 = false;
    else
        v30 = u2.createFragment({
            SubTitle = u2.createElement("TextLabel", {
                BackgroundTransparency = 1,
                RichText = true,
                TextTransparency = 0.3,
                TextSize = 12,
                ZIndex = 4,
                AutomaticSize = Enum.AutomaticSize.XY,
                TextXAlignment = Enum.TextXAlignment.Left,
                Text = SubTitle,
                TextColor3 = Color3.new(1, 1, 1),
                Font = Enum.Font.Roboto
            })
        });
    end;

    if v30 then
        v26[v29 + 1] = v30;
    end;

    v22.WidgetTitles = u2.createElement("Frame", v25, v26);
    v19.WidgetInfo = u2.createElement("Frame", v21, v22);
    local Navbar = u12.props.Navbar;

    if Navbar then
        local v31 = {
            FrameProps = {
                ZIndex = 100,
                Size = UDim2.fromScale(0.5, 1),
                Position = UDim2.fromScale(0.5, 0.5),
                AnchorPoint = Vector2.new(0.5, 0.5)
            },
            Variant = TabComponentVariant.UNDERLINE
        };

        for i, v in u12.props.Navbar do
            v31[i] = v;
        end;

        Navbar = u2.createElement(TabsComponent, v31);
    end;

    if Navbar then
        v19[v20 + 1] = Navbar;
    end;

    local v32 = #v19;
    local HeaderChildren = u12.props.HeaderChildren;

    if HeaderChildren then
        v19[v32 + 1] = HeaderChildren;
    end;

    v19[#v19 + 1] = u2.createElement("TextButton", {
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -15, 0.5, 0),
        Size = UDim2.fromOffset(28, 28),
        BackgroundColor3 = GameTheme.backgroundPrimary,
        Text = "",

        [u2.Event.Activated] = function() -- Line: 222
            -- upvalues: u12 (copy)
            if u12.props.OnClose then
                return u12.props.OnClose();
            end;

            return u12.defaultProps.OnClose();
        end,

        ZIndex = 4
    }, { u2.createElement("UICorner", {
            CornerRadius = UDim.new(1, 0)
        }), u2.createElement("ImageLabel", {
            BackgroundTransparency = 1,
            ZIndex = 4,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.fromScale(0.5, 0.5),
            Size = UDim2.fromOffset(10, 10),
            Image = u5.Image,
            ImageRectOffset = u5.ImageRectOffset,
            ImageRectSize = u5.ImageRectSize
        }) });
    v15.WidgetHeader = u2.createElement("Frame", v18, v19);
    local v33 = {
        BorderSizePixel = 0,
        LayoutOrder = 2,
        ZIndex = 1,
        Size = UDim2.new(1, 0, 1, -40),
        Position = UDim2.new(0, 0, 0, 40),
        BackgroundColor3 = GameTheme.backgroundSecondary,
        ClipsDescendants = u12.props.ClipsDescendents
    };
    local v34 = { u2.createElement("UICorner", {
            CornerRadius = UDim.new(0, v13)
        }) };
    local v35 = {
        TopLeft = true,
        TopRight = true
    };
    local CornerFillerProps = u12.props.CornerFillerProps;

    if CornerFillerProps then
        for i, v in CornerFillerProps do
            v35[i] = v;
        end;
    end;

    v35.ZIndex = 1;
    v34[#v34 + 1] = u2.createElement(CornerFiller, v35);
    local ContentUIPadding = u12.props.ContentUIPadding;

    if ContentUIPadding == nil then
        ContentUIPadding = u2.createElement("UIPadding", {
            PaddingTop = UDim.new(0, 15),
            PaddingBottom = UDim.new(0, 15),
            PaddingLeft = UDim.new(0, 15),
            PaddingRight = UDim.new(0, 15)
        });
    end;

    local v36 = {
        Size = UDim2.fromScale(1, 1),
        BackgroundTransparency = 1,
        [u2.Ref] = u12.contentRef,
        ZIndex = 1
    };
    local v37 = {};
    local v38 = #v37;

    if type(ContentUIPadding) == "table" then
        if ContentUIPadding.elements == nil and (ContentUIPadding.props == nil or ContentUIPadding.component == nil) then
            for i, v in ContentUIPadding do
                if type(i) == "number" then
                    v37[v38 + i] = v;
                else
                    v37[i] = v;
                end;
            end;
        else
            v37[v38 + 1] = ContentUIPadding;
        end;
    end;

    local v39 = #v37;
    local v40 = u12.props[u2.Children];

    if v40 then
        for i, v in v40 do
            if type(i) == "number" then
                v37[v39 + i] = v;
            else
                v37[i] = v;
            end;
        end;
    end;

    v34.WidgetContent = u2.createElement("Frame", v36, v37);
    v15.WidgetContentFrame = u2.createElement("Frame", v33, v34);

    return u2.createFragment({
        WidgetComponent = u2.createElement(EmptyButton, v14, v15)
    });
end;

return {
    WidgetComponentBase = v6,
    WidgetComponent = v3.connect(function(p41, p42) -- Line: 317
        local v43 = {};

        for i, v in p42 do
            v43[i] = v;
        end;

        return v43;
    end)(v6)
};