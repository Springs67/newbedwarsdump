-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local LineComponent = v1.import(script, script.Parent, "line-component").LineComponent;

return {
    LineFrame = v3.new(u2)(function(p4, p5) -- Line: 6
        -- upvalues: u2 (copy), LineComponent (copy)
        local v6 = {
            BackgroundTransparency = 1,
            Size = p4.Size,
            Position = p4.Position,
            AnchorPoint = p4.AnchorPoint
        };
        local v7 = {
            u2.createElement(LineComponent, {
                Corner1 = UDim2.fromScale(0, 0),
                Corner2 = UDim2.fromScale(0, 1),
                WidthPixels = p4.BorderWidthPixels,
                Color3 = p4.BorderColor3,
                Transparency = p4.BorderTransparency
            }),
            u2.createElement(LineComponent, {
                Corner1 = UDim2.fromScale(0, 1),
                Corner2 = UDim2.fromScale(1, 1),
                WidthPixels = p4.BorderWidthPixels,
                Color3 = p4.BorderColor3,
                Transparency = p4.BorderTransparency
            }),
            u2.createElement(LineComponent, {
                Corner1 = UDim2.fromScale(1, 1),
                Corner2 = UDim2.fromScale(1, 0),
                WidthPixels = p4.BorderWidthPixels,
                Color3 = p4.BorderColor3,
                Transparency = p4.BorderTransparency
            }),
            u2.createElement(LineComponent, {
                Corner1 = UDim2.fromScale(1, 0),
                Corner2 = UDim2.fromScale(0, 0),
                WidthPixels = p4.BorderWidthPixels,
                Color3 = p4.BorderColor3,
                Transparency = p4.BorderTransparency
            })
        };
        local v8 = {
            Size = UDim2.fromScale(1, 1) - UDim2.fromOffset(p4.BorderWidthPixels, p4.BorderWidthPixels),
            Position = UDim2.fromOffset(p4.BorderWidthPixels / 2, p4.BorderWidthPixels / 2),
            BackgroundTransparency = 1
        };
        local v9 = {};
        local v10 = #v9;
        local v11 = p4[u2.Children];

        if v11 then
            for i, v in v11 do
                if type(i) == "number" then
                    v9[v10 + i] = v;
                else
                    v9[i] = v;
                end;
            end;
        end;

        v7[#v7 + 1] = u2.createElement("Frame", v8, v9);

        return u2.createElement("Frame", v6, v7);
    end)
};