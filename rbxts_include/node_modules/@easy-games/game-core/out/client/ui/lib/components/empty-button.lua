-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);

return {
    EmptyButton = function(p3) -- Line: 4, Name: EmptyButton
        -- upvalues: u2 (copy)
        local v4 = {};

        for i, v in p3 do
            v4[i] = v;
        end;

        v4[u2.Children] = nil;
        v4.ObjRef = nil;
        local v5 = {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            ImageTransparency = 1,
            AutoButtonColor = false,
            Selectable = false,
            [u2.Ref] = p3.ObjRef
        };

        for i, v in v4 do
            v5[i] = v;
        end;

        local v6 = {};
        local v7 = #v6;
        local v8 = p3[u2.Children];

        if v8 then
            for i, v in v8 do
                if type(i) == "number" then
                    v6[v7 + i] = v;
                else
                    v6[i] = v;
                end;
            end;
        end;

        return u2.createElement("ImageButton", v5, v6);
    end
};