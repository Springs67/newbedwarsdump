-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local ImageId = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "images", "image-id").ImageId;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local DeviceUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;
local UIUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").UIUtil;
local u4 = nil;

local function v25(u5, p6) -- Line: 13
    -- upvalues: u2 (copy), u4 (ref), TweenService (copy), DeviceUtil (copy), UIUtil (copy), GameTheme (copy), SoundManager (copy), ImageId (copy)
    local useEffect = p6.useEffect;
    local u7 = u2.createRef();
    local v8 = {};

    for i, v in u5 do
        v8[i] = v;
    end;

    v8[u2.Children] = nil;
    v8.Text = nil;
    v8.CornerRadius = nil;
    v8.OnClick = nil;
    v8.OnEnter = nil;
    v8.OnLeave = nil;
    v8.Loading = nil;
    v8.Active = nil;
    v8.CreateDeactivatedTween = nil;
    v8.Disabled = nil;
    v8.ClickSound = nil;
    v8.noBold = nil;
    v8.DisableDefaultSound = nil;
    v8.GamepadShouldAutoSelect = nil;
    useEffect(function() -- Line: 36
        -- upvalues: u7 (copy), u5 (copy)
        local v9 = u7:getValue();
        local v10;

        if v9 == nil then
            v10 = v9;
        else
            v10 = v9.Name;
        end;

        if v10 == "1" then
            v9.Name = "ButtonComponent";

            return nil;
        end;

        if u5.Active == false and (u5.CreateDeactivatedTween and v9) then
            u5.CreateDeactivatedTween(v9):Play();
        end;
    end, { u5.Active });
    local u11 = u2.createRef();
    useEffect(function() -- Line: 53
        -- upvalues: u5 (copy), u4 (ref), TweenService (ref), u11 (copy)
        if u5.Loading then
            u4 = TweenService:Create(u11:getValue(), TweenInfo.new(4, Enum.EasingStyle.Linear, Enum.EasingDirection.In, -1), {
                Rotation = 360
            });
            u4:Play();
        end;
    end, { u5.Loading });
    useEffect(function() -- Line: 61
        -- upvalues: u5 (copy), DeviceUtil (ref), u7 (copy), UIUtil (ref)
        if u5.GamepadShouldAutoSelect and DeviceUtil.isGamepadControls() then
            UIUtil:selectGui((u7:getValue()));
        end;
    end, {});

    local function _() -- Line: 67
        -- upvalues: u5 (copy)
        local Text = u5.Text;

        if Text == "" or not Text then
            return "";
        end;

        if u5.Loading then
            return "";
        end;

        if u5.noBold then
            return u5.Text;
        end;

        return "<b>" .. u5.Text .. "</b>";
    end;

    local v12 = {
        AnchorPoint = u5.AnchorPoint,
        Position = u5.Position,
        Size = u5.Size,
        BackgroundTransparency = u5.Disabled and 0.5 or 0,
        BorderColor3 = Color3.new(1, 1, 1),
        BorderMode = Enum.BorderMode.Inset,
        BorderSizePixel = u5.Active and 1 or 0,
        BackgroundColor3 = u5.BackgroundColor3 or GameTheme.backgroundSuccess
    };
    local Text = u5.Text;
    local v13;

    if Text == "" or not Text or u5.Loading then
        v13 = "";
    elseif u5.noBold then
        v13 = u5.Text;
    else
        v13 = "<b>" .. u5.Text .. "</b>";
    end;

    v12.Text = v13;
    v12.RichText = true;
    v12.Font = Enum.Font.Roboto;
    v12.TextSize = u5.TextSize;
    v12.TextScaled = u5.TextScaled;
    v12.TextTransparency = u5.Disabled and 0.5 or 0;
    v12.TextColor3 = Color3.new(1, 1, 1);
    v12.LayoutOrder = u5.LayoutOrder;
    v12.AutoButtonColor = not u5.Disabled;

    v12[u2.Event.Activated] = function(p14, p15) -- Line: 98
        -- upvalues: u5 (copy), SoundManager (ref), GameTheme (ref)
        if u5.Disabled then
            return nil;
        end;

        local ClickSound = u5.ClickSound;

        if ClickSound == "" or not ClickSound then
            if not u5.DisableDefaultSound then
                SoundManager:playSound(GameTheme.uiHover);
            end;
        else
            SoundManager:playSound(u5.ClickSound);
        end;

        u5.OnClick(p14, p15);
    end;

    v12[u2.Event.MouseEnter] = function(p16) -- Line: 112
        -- upvalues: u5 (copy), SoundManager (ref), GameTheme (ref)
        local OnEnter = u5.OnEnter;

        if OnEnter ~= nil then
            OnEnter(p16);
        end;

        if not u5.DisableDefaultSound then
            SoundManager:playSound(GameTheme.uiHover);
        end;
    end;

    v12[u2.Event.MouseLeave] = function(p17) -- Line: 121
        -- upvalues: u5 (copy)
        local OnLeave = u5.OnLeave;

        if OnLeave ~= nil then
            OnLeave(p17);
        end;
    end;

    v12[u2.Ref] = u7;

    for i, v in v8 do
        v12[i] = v;
    end;

    local Loading = u5.Loading;

    if Loading then
        local v18 = {
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.fromScale(0.5, 0.5),
            Size = UDim2.new(0, 16, 0, 16),
            Image = ImageId.LOADING_SPINNER,
            ScaleType = "Fit",
            BackgroundTransparency = 1,
            [u2.Ref] = u11
        };
        local ZIndex = v8.ZIndex;
        local v19;

        if ZIndex == 0 or (ZIndex ~= ZIndex or not ZIndex) then
            v19 = 1;
        else
            local v20 = tonumber(v8.ZIndex);
            v19 = (v20 == nil and 0 or v20) + 1;
        end;

        v18.ZIndex = v19;
        Loading = u2.createElement("ImageLabel", v18);
    end;

    local v21 = { u2.createElement("UICorner", {
            CornerRadius = u5.CornerRadius
        }) };
    local v22 = #v21;

    if Loading then
        v21[v22 + 1] = Loading;
    end;

    local v23 = #v21;
    local v24 = u5[u2.Children];

    if v24 then
        for i, v in v24 do
            if type(i) == "number" then
                v21[v23 + i] = v;
            else
                v21[i] = v;
            end;
        end;
    end;

    return u2.createElement("TextButton", v12, v21);
end;

return {
    ButtonComponent = v3.new(u2)(v25, {
        defaultProps = {
            TextSize = 12,
            Size = UDim2.fromOffset(120, 28),
            ClickSound = GameCoreClientConfig.Sounds.UI_CLICK
        }
    })
};