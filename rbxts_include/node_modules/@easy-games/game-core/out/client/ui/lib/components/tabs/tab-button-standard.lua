-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TextService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TextService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;

return {
    TabButton = v3.new(u2)(function(u4, p5) -- Line: 9
        -- upvalues: TextService (copy), ColorUtil (copy), u2 (copy), SoundManager (copy), GameCoreClientConfig (copy)
        local v6, u7 = p5.useState(false);
        local ButtonProps = u4.ButtonProps;

        if ButtonProps ~= nil then
            ButtonProps = ButtonProps.TextSize;
        end;

        local v8 = ButtonProps == nil and 15 or ButtonProps;
        local v9 = TextService:GetTextSize(u4.TabDetail.text, v8, Enum.Font.Roboto, Vector2.new(1000, 1000));
        local v10 = {
            AutomaticSize = "X",
            Size = UDim2.new(0, v9.X + 1, 1, 0)
        };
        local v11;

        if u4.Active then
            v11 = u4.UIConfig;

            if v11 ~= nil then
                v11 = v11.ActiveColor;
            end;

            if v11 == nil then
                v11 = Color3.fromRGB(107, 111, 191);
            end;
        else
            v11 = ColorUtil.BLACK;
        end;

        v10.BackgroundColor3 = v11;
        local v12;

        if u4.Active then
            local UIConfig = u4.UIConfig;

            if UIConfig ~= nil then
                UIConfig = UIConfig.ActiveBGTransparency;
            end;

            v12 = UIConfig == nil and 0 or UIConfig;
        elseif v6 then
            v12 = 0.9;
        else
            local UIConfig = u4.UIConfig;

            if UIConfig ~= nil then
                UIConfig = UIConfig.InactiveBGTransparency;
            end;

            v12 = UIConfig == nil and 0.9 or UIConfig;
        end;

        v10.BackgroundTransparency = v12;
        v10.Font = "Roboto";
        v10.Text = "<b>" .. u4.TabDetail.text .. "</b>";
        local v13;

        if u4.Active then
            v13 = u4.UIConfig;

            if v13 ~= nil then
                v13 = v13.ActiveTextColor;
            end;

            if v13 == nil then
                v13 = ColorUtil.WHITE;
            end;
        else
            v13 = ColorUtil.WHITE;
        end;

        v10.TextColor3 = v13;
        v10.TextSize = v8;
        v10.RichText = true;
        v10.TextTransparency = (v6 or u4.Active) and 0 or 0.3;
        v10.TextXAlignment = "Center";
        v10.TextYAlignment = "Center";

        v10[u2.Event.Activated] = function() -- Line: 92
            -- upvalues: u4 (copy), SoundManager (ref), GameCoreClientConfig (ref)
            u4.OnChange(u4.TabDetail);
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
        end;

        v10[u2.Event.MouseEnter] = function(p14) -- Line: 96
            -- upvalues: u7 (copy)
            u7(true);
        end;

        v10[u2.Event.MouseLeave] = function(p15) -- Line: 99
            -- upvalues: u7 (copy)
            u7(false);
        end;

        v10.AutoButtonColor = false;
        v10.LayoutOrder = u4.TabDetail.layoutOrder;
        local ButtonProps2 = u4.ButtonProps;

        if ButtonProps2 then
            for i, v in ButtonProps2 do
                v10[i] = v;
            end;
        end;

        local v16 = { u2.createElement("UICorner", {
                CornerRadius = UDim.new(0, 5)
            }) };
        local v17 = #v16;
        local v18 = {
            PaddingTop = UDim.new(0, 4),
            PaddingBottom = UDim.new(0, 4),
            PaddingLeft = UDim.new(0, 6),
            PaddingRight = UDim.new(0, 6)
        };
        local UIConfig = u4.UIConfig;

        if UIConfig ~= nil then
            UIConfig = UIConfig.Padding;
        end;

        if UIConfig then
            for i, v in UIConfig do
                v18[i] = v;
            end;
        end;

        v16[v17 + 1] = u2.createElement("UIPadding", v18);
        local v19 = {};
        local UIConfig2 = u4.UIConfig;

        if UIConfig2 ~= nil then
            UIConfig2 = UIConfig2.MinTextSize;
        end;

        v19.MinTextSize = UIConfig2;
        local UIConfig3 = u4.UIConfig;

        if UIConfig3 ~= nil then
            UIConfig3 = UIConfig3.MaxTextSize;
        end;

        v19.MaxTextSize = UIConfig3 == nil and 18 or UIConfig3;
        v16[v17 + 2] = u2.createElement("UITextSizeConstraint", v19);

        return u2.createElement("TextButton", v10, v16);
    end)
};