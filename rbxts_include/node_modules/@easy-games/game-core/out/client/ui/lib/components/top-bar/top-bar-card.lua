-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local Padding = v1.import(script, script.Parent.Parent, "padding").Padding;

return {
    TopBarCard = v3.new(u2)(function(p4, p5) -- Line: 8
        -- upvalues: u2 (copy), GameTheme (copy), ColorUtil (copy), Padding (copy)
        local _ = p5.useState;
        local v6 = {};

        for i, v in p4 do
            v6[i] = v;
        end;

        v6[u2.Children] = nil;
        local BackgroundTransparency = p4.BackgroundTransparency;

        if BackgroundTransparency == nil then
            BackgroundTransparency = GameTheme.topBarBGTransparency;
        end;

        local v7 = {
            Size = UDim2.new(0, 0, 0, GameTheme.topBarGuiSize),
            AutomaticSize = Enum.AutomaticSize.X,
            BackgroundColor3 = ColorUtil.BLACK,
            BackgroundTransparency = BackgroundTransparency
        };

        for i, v in v6 do
            v7[i] = v;
        end;

        local v8 = { u2.createElement("UIListLayout", {
                FillDirection = Enum.FillDirection.Horizontal,
                VerticalAlignment = Enum.VerticalAlignment.Center,
                HorizontalAlignment = Enum.HorizontalAlignment.Center,
                SortOrder = Enum.SortOrder.LayoutOrder,
                Padding = UDim.new(0, 6)
            }), u2.createElement("UICorner", {
                CornerRadius = UDim.new(0, 6)
            }), u2.createElement(Padding, {
                Padding = UDim.new(0, 7)
            }) };
        local v9 = #v8;
        local v10 = p4[u2.Children];

        if v10 then
            for i, v in v10 do
                if type(i) == "number" then
                    v8[v9 + i] = v;
                else
                    v8[i] = v;
                end;
            end;
        end;

        return u2.createElement("Frame", v7, v8);
    end)
};