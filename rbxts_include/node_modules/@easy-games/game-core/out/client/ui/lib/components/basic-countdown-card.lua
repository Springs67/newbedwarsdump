-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local Workspace = u1.import(script, u1.getModule(script, "@rbxts", "services")).Workspace;
local GameTheme = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local StringUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "string-util").StringUtil;
local v3 = u2.Component:extend("BasicCountdownCard");

function v3.init(p4, p5) -- Line: 10
    p4.alive = true;
    p4:setState({
        time = p4:getTime()
    });
end;

function v3.didMount(u6) -- Line: 16
    -- upvalues: u1 (copy)
    u1.Promise.defer(function() -- Line: 17
        -- upvalues: u6 (copy)
        while { wait(0.2) } and u6.alive do
            u6:setState({
                time = u6:getTime()
            });
        end;
    end);
end;

function v3.didUpdate(p7, p8) -- Line: 25
    if p8.EndOSTime ~= p7.props.EndOSTime or p8.EndTime ~= p7.props.EndTime then
        p7:setState({
            time = p7:getTime()
        });
    end;
end;

function v3.willUnmount(p9) -- Line: 32
    p9.alive = false;
end;

function v3.getTime(p10) -- Line: 35
    -- upvalues: StringUtil (copy), Workspace (copy)
    if p10.props.EndOSTime ~= nil then
        local v11 = p10.props.EndOSTime - os.time();
        local v12 = math.max(0, v11);

        return StringUtil.formatCountdownTime(v12);
    end;

    if p10.props.EndTime == nil then
        return "";
    end;

    local v13 = p10.props.EndTime - Workspace:GetServerTimeNow();
    local v14 = math.max(0, v13);

    return StringUtil.formatCountdownTime(v14);
end;

function v3.render(p15) -- Line: 45
    -- upvalues: u2 (copy), GameTheme (copy)
    local v16 = {};

    for i, v in p15.props do
        v16[i] = v;
    end;

    v16[u2.Children] = nil;
    v16.Text = nil;
    v16.EndOSTime = nil;
    v16.EndTime = nil;
    local v17 = string.gsub(p15.props.Text, "{0}", p15.state.time);
    local v18 = {
        BorderSizePixel = 0,
        BackgroundTransparency = 0,
        BackgroundColor3 = GameTheme.backgroundPrimary
    };

    for i, v in v16 do
        v18[i] = v;
    end;

    return u2.createElement("Frame", v18, { u2.createElement("UIAspectRatioConstraint", {
            AspectRatio = 7.408163265306122,
            DominantAxis = "Height"
        }), u2.createElement("UICorner", {
            CornerRadius = UDim.new(0.1, 0)
        }), u2.createElement("TextLabel", {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Font = "RobotoMono",
            RichText = true,
            TextScaled = true,
            Text = v17,
            Size = UDim2.fromScale(0.75, 0.65),
            Position = UDim2.fromScale(0.5, 0.5),
            AnchorPoint = Vector2.new(0.5, 0.5),
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }) });
end;

return {
    BasicCountdownCard = v3
};