-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local u2 = u1.import(script, u1.getModule(script, "@rbxts", "maid").Maid);
local u3 = u1.import(script, u1.getModule(script, "@rbxts", "roact").src);
local v4 = u1.import(script, u1.getModule(script, "@rbxts", "roact-hooks").src);
local ColorUtil = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local DynamicCalcCanvasSize = u1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").DynamicCalcCanvasSize;

return {
    AutoCanvasScrollingFrame = v4.new(u3)(function(u5, p6) -- Line: 8
        -- upvalues: u3 (copy), u2 (copy), u1 (copy), DynamicCalcCanvasSize (copy), ColorUtil (copy)
        local useEffect = p6.useEffect;
        local u7 = u5.ScrollingFrameRef or u3.createRef();

        local function v18() -- Line: 12
            -- upvalues: u7 (copy), u2 (ref), u1 (ref), u5 (copy), DynamicCalcCanvasSize (ref)
            local u8 = u7:getValue();

            if not u8 then
                print("Missing scrollingFrameRef");

                return nil;
            end;

            local u9 = u2.new();
            u1.Promise.defer(function() -- Line: 20
                -- upvalues: u5 (ref), u8 (copy), DynamicCalcCanvasSize (ref), u9 (copy)
                if u5.WaitForAbsoluteSize then
                    local v10 = u8:GetDescendants();

                    local function v12(p11) -- Line: 23
                        if p11:IsA("GuiObject") then
                            while p11.AbsoluteSize.X == 0 and p11.AbsoluteSize.Y == 0 do
                                wait(0.1);
                            end;
                        end;
                    end;

                    local v13 = 0;
                    local v14 = {};

                    for i, v in v10 do
                        local v15 = v12(v, i - 1, v10);

                        if v15 ~= nil then
                            v13 = v13 + 1;
                            v14[v13] = v15;
                        end;
                    end;
                end;

                local v17 = DynamicCalcCanvasSize(u8, function(p16) -- Line: 42
                    -- upvalues: u5 (ref)
                    if u5.OnCalc then
                        u5.OnCalc(p16);
                    end;
                end, {
                    additionalSpace = u5.AdditionalSpace
                });

                if v17 then
                    u9:GiveTask(v17);
                end;
            end);

            return function() -- Line: 53
                -- upvalues: u9 (copy)
                u9:DoCleaning();
            end;
        end;

        local v19 = u5[u3.Children];

        if v19 ~= nil then
            v19 = 0;

            for _ in v19 do
                v19 = v19 + 1;
            end;
        end;

        useEffect(v18, { v19 });
        local v20 = {
            Size = UDim2.new(1, 0, 1, 0),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            ScrollingDirection = Enum.ScrollingDirection.Y,
            ScrollBarThickness = 4,
            ScrollBarImageTransparency = 0.3,
            ScrollBarImageColor3 = ColorUtil.WHITE,
            [u3.Ref] = u7
        };
        local ScrollingFrameProps = u5.ScrollingFrameProps;

        if ScrollingFrameProps then
            for i, v in ScrollingFrameProps do
                v20[i] = v;
            end;
        end;

        local v21 = {};
        local v22 = #v21;
        local v23 = u5[u3.Children];

        if v23 then
            for i, v in v23 do
                if type(i) == "number" then
                    v21[v22 + i] = v;
                else
                    v21[i] = v;
                end;
            end;
        end;

        return u3.createFragment({
            AutoCanvasScrollingFrame = u3.createElement("ScrollingFrame", v20, v21)
        });
    end)
};