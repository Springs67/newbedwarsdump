-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;

return {
    DividerComponent = function(p3) -- Line: 5, Name: DividerComponent
        -- upvalues: u2 (copy), GameTheme (copy)
        local LengthScale = p3.LengthScale;
        local v4 = LengthScale == nil and 1 or LengthScale;
        local Thickness = p3.Thickness;
        local v5 = Thickness == nil and 1 or Thickness;
        local Margin = p3.Margin;
        local v6 = Margin == nil and 6 or Margin;
        local BarPosition = p3.BarPosition;
        local v7 = BarPosition == nil and {
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.fromScale(0.5, 0.5)
        } or BarPosition;
        local AutomaticSize = p3.AutomaticSize;

        if AutomaticSize == nil then
            AutomaticSize = Enum.AutomaticSize.None;
        end;

        local MinSize = p3.MinSize;

        if MinSize == nil then
            MinSize = Vector2.new(0, 0);
        end;

        local MaxSize = p3.MaxSize;

        if MaxSize == nil then
            MaxSize = Vector2.new((1 / 0), (1 / 0));
        end;

        local v8;

        if p3.Direction == "Y" then
            v8 = p3.Size or UDim2.new(0, 12 + v6, v4, 0);
        else
            v8 = p3.Size or UDim2.new(v4, 0, 0, 12 + v6);
        end;

        local v9;

        if p3.Direction == "Y" then
            v9 = UDim2.new(0, v5, v4, 0);
        else
            v9 = UDim2.new(v4, 0, 0, v5);
        end;

        local v10 = {
            BackgroundTransparency = 1,
            AnchorPoint = p3.AnchorPoint,
            Position = p3.Position,
            Size = v8,
            LayoutOrder = p3.LayoutOrder,
            ZIndex = p3.ZIndex
        };
        local v11 = { u2.createElement("UISizeConstraint", {
                MinSize = MinSize,
                MaxSize = MaxSize
            }) };
        local v12 = {
            [u2.Ref] = p3.Ref,
            AnchorPoint = v7.AnchorPoint,
            Position = v7.Position,
            Size = v9,
            AutomaticSize = AutomaticSize
        };
        local BorderSizePixel = p3.BorderSizePixel;
        v12.BorderSizePixel = BorderSizePixel == nil and 0 or BorderSizePixel;
        local BarColor = p3.BarColor;

        if BarColor ~= nil then
            BarColor = BarColor.Color;
        end;

        if BarColor == nil then
            BarColor = Color3.new(1, 1, 1);
        end;

        v12.BackgroundColor3 = BarColor;
        local BarColor2 = p3.BarColor;

        if BarColor2 ~= nil then
            BarColor2 = BarColor2.Transparency;
        end;

        v12.BackgroundTransparency = BarColor2 == nil and 0.6 or BarColor2;
        v12.ZIndex = p3.ZIndex;
        local v13;

        if p3.Text == nil then
            v13 = nil;
        else
            local v14 = {
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.fromScale(0.5, 0.5),
                AutomaticSize = Enum.AutomaticSize.XY
            };
            local TextLabel = p3.TextLabel;

            if TextLabel ~= nil then
                TextLabel = TextLabel.BackgroundColor;
            end;

            v14.BackgroundColor3 = TextLabel or GameTheme.backgroundSecondary;
            v14.BorderSizePixel = 0;
            v14.Text = "<b>" .. p3.Text .. "</b>";
            v14.RichText = true;
            v14.TextSize = 12;
            v14.Font = Enum.Font.RobotoMono;
            local TextLabel2 = p3.TextLabel;

            if TextLabel2 ~= nil then
                TextLabel2 = TextLabel2.Color;
            end;

            if TextLabel2 == nil then
                TextLabel2 = Color3.new(1, 1, 1);
            end;

            v14.TextColor3 = TextLabel2;
            local TextLabel3 = p3.TextLabel;

            if TextLabel3 ~= nil then
                TextLabel3 = TextLabel3.Transparency;
            end;

            v14.TextTransparency = TextLabel3 == nil and 0.6 or TextLabel3;
            v14.ZIndex = p3.ZIndex;
            v13 = u2.createElement("TextLabel", v14, { u2.createElement("UIPadding", {
                    PaddingLeft = UDim.new(0, 8),
                    PaddingRight = UDim.new(0, 8)
                }) });
        end;

        local v15 = {};
        local v16 = #v15;

        if v13 then
            v15[v16 + 1] = v13;
        end;

        v11[#v11 + 1] = u2.createElement("Frame", v12, v15);

        return u2.createFragment({
            DividerComponent = u2.createElement("Frame", v10, v11)
        });
    end
};