-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local Padding = v1.import(script, script.Parent, "padding").Padding;

return {
    TextInputComponent = v3.new(u2)(function(u4, p5) -- Line: 7
        -- upvalues: GameTheme (copy), u2 (copy), Padding (copy)
        local _ = p5.useState;
        local TextBox = u4.TextBox;

        if TextBox ~= nil then
            TextBox = TextBox.Text;
        end;

        local u6, u7 = p5.useBinding(TextBox == nil and "" or TextBox);
        local v12 = {
            Size = UDim2.fromOffset(120, 34),
            Font = Enum.Font.Roboto,
            TextColor3 = Color3.new(1, 1, 1),
            Text = "",
            TextSize = 12,
            BackgroundColor3 = GameTheme.interactionPrimary,

            [u2.Event.FocusLost] = function(p8, p9, p10) -- Line: 26
                -- upvalues: u4 (copy)
                if p9 and u4.OnEnter then
                    u4.OnEnter(p8.Text);
                    p8:CaptureFocus();

                    if u4.ClearTextOnEnter then
                        p8.Text = "";
                    end;
                end;

                if u4.OnFocusLost then
                    u4.OnFocusLost(p8.Text, p10);
                end;
            end,

            [u2.Change.Text] = function(p11) -- Line: 38
                -- upvalues: u4 (copy), u6 (copy), u7 (copy)
                local MaxCharacters = u4.MaxCharacters;

                if MaxCharacters ~= 0 and (MaxCharacters == MaxCharacters and MaxCharacters) then
                    MaxCharacters = #p11.Text > u4.MaxCharacters;
                end;

                if MaxCharacters == 0 or (MaxCharacters ~= MaxCharacters or not MaxCharacters) then
                    u7(p11.Text);

                    return;
                end;

                p11.Text = tostring(u6:getValue());
            end
        };
        local TextBox2 = u4.TextBox;

        if TextBox2 then
            for i, v in TextBox2 do
                v12[i] = v;
            end;
        end;

        v12[u2.Ref] = u4.Ref;
        local v13 = { u2.createElement("UICorner", {
                CornerRadius = UDim.new(0, 5)
            }), u2.createElement(Padding, {
                Padding = {
                    Horizontal = 8,
                    Vertical = 4
                }
            }) };
        local v14 = #v13;
        local v15 = u4[u2.Children];

        if v15 then
            for i, v in v15 do
                if type(i) == "number" then
                    v13[v14 + i] = v;
                else
                    v13[i] = v;
                end;
            end;
        end;

        return u2.createElement("TextBox", v12, v13);
    end)
};