-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);

return {
    LineComponent = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src).new(u3)(function(p4, p5) -- Line: 6, Name: Component
        -- upvalues: u3 (copy), u2 (copy)
        local useEffect = p5.useEffect;
        local v6, u7 = p5.useState({
            position = Vector2.new(0, 0),
            size = Vector2.new(0, 0)
        });
        local u8 = u3.createRef();
        useEffect(function() -- Line: 14
            -- upvalues: u2 (ref), u8 (copy), u7 (copy)
            local u9 = u2.new();
            u9:GiveTask(u8:getValue():GetPropertyChangedSignal("AbsolutePosition"):Connect(function() -- Line: 16
                -- upvalues: u8 (ref), u7 (ref)
                if u8:getValue() then
                    u7({
                        position = u8:getValue().AbsolutePosition,
                        size = u8:getValue().AbsoluteSize
                    });
                end;
            end));

            return function() -- Line: 24
                -- upvalues: u9 (copy)
                u9:DoCleaning();
            end;
        end);
        local v10 = UDim2.new(math.min(p4.Corner1.X.Scale, p4.Corner2.X.Scale), math.min(p4.Corner1.X.Offset, p4.Corner2.X.Offset), math.min(p4.Corner1.Y.Scale, p4.Corner2.Y.Scale), (math.min(p4.Corner1.Y.Offset, p4.Corner2.Y.Offset)));
        local v11 = p4.Corner1 - p4.Corner2;
        local v12 = UDim2.new(math.abs(v11.X.Scale), math.abs(v11.X.Offset), math.abs(v11.Y.Scale), (math.abs(v11.Y.Offset)));
        local position = v6.position;
        local size = v6.size;
        local v13 = Vector2.new(position.X, position.Y);
        local v14 = Vector2.new(position.X + size.X, position.Y + size.Y) - position;
        local v15 = v13 - position;
        local v16 = (v14 + v15) / 2;
        local v17 = v14 - v15;
        local v18 = Vector2.new(math.ceil(v17.X), (math.ceil(v17.Y)));
        local v19;

        if v18.Y == 0 then
            v19 = 0;
        else
            local v20 = math.atan2(v18.Y, v18.X);
            v19 = math.deg(v20);
        end;

        return u3.createElement("Frame", {
            Size = v12,
            BackgroundTransparency = 1,
            Position = v10,
            [u3.Ref] = u8
        }, { u3.createElement("Frame", {
                BorderSizePixel = 0,
                BackgroundColor3 = p4.Color3,
                Transparency = p4.Transparency,
                Position = UDim2.fromOffset(v16.X - v18.Magnitude / 2, v16.Y),
                Rotation = v19,
                Size = UDim2.fromOffset(v18.Magnitude, p4.WidthPixels)
            }) });
    end)
};