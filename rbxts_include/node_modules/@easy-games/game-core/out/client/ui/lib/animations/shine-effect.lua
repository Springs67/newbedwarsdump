-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local v4 = {};
local u5 = setmetatable({}, {
    __index = v4
});
u5.gradient = 0;
v4[0] = "gradient";
u5.gradientLarge = 1;
v4[1] = "gradientLarge";
u5.solid = 2;
v4[2] = "solid";
u5.trailing = 3;
v4[3] = "trailing";

local function v20(u6, p7) -- Line: 22
    -- upvalues: u2 (copy), u5 (ref), TweenService (copy), ColorUtil (copy)
    local useEffect = p7.useEffect;
    local u8 = u2.createRef();
    local v9 = {};

    for i, v in u6 do
        v9[i] = v;
    end;

    local Speed = v9.Speed;
    local u10 = Speed == nil and 0.7 or Speed;
    local Loop = v9.Loop;
    local u11 = Loop == nil and true or Loop;
    local DelayTime = v9.DelayTime;
    local u12 = DelayTime == nil and 4 or DelayTime;
    local Color = v9.Color;

    if Color == nil then
        Color = Color3.fromRGB(255, 255, 255);
    end;

    local Variation = v9.Variation;

    if Variation == nil then
        Variation = u5.gradient;
    end;

    useEffect(function() -- Line: 51
        -- upvalues: u8 (copy), Variation (ref), u5 (ref), Color (ref), u10 (ref), u11 (ref), u12 (ref), u6 (copy), TweenService (ref)
        local v13 = u8:getValue();

        if Variation == u5.gradient then
            v13.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.35, 1),
                NumberSequenceKeypoint.new(0.5, 0.25),
                NumberSequenceKeypoint.new(0.65, 1),
                NumberSequenceKeypoint.new(1, 1)
            });
        elseif Variation == u5.gradientLarge then
            v13.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.35, 0.75),
                NumberSequenceKeypoint.new(0.5, 0.5),
                NumberSequenceKeypoint.new(0.75, 0.75),
                NumberSequenceKeypoint.new(1, 1)
            });
        elseif Variation == u5.solid then
            v13.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.34, 1),
                NumberSequenceKeypoint.new(0.35, 0.25),
                NumberSequenceKeypoint.new(0.45, 0.25),
                NumberSequenceKeypoint.new(0.46, 0.5),
                NumberSequenceKeypoint.new(0.51, 0.5),
                NumberSequenceKeypoint.new(0.52, 1),
                NumberSequenceKeypoint.new(1, 1)
            });
        elseif Variation == u5.trailing then
            v13.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.35, 0.75),
                NumberSequenceKeypoint.new(0.5, 0),
                NumberSequenceKeypoint.new(0.51, 1),
                NumberSequenceKeypoint.new(1, 1)
            });
        else
            v13.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.35, 0.1),
                NumberSequenceKeypoint.new(0.5, 0.1),
                NumberSequenceKeypoint.new(0.65, 1),
                NumberSequenceKeypoint.new(1, 1)
            });
        end;

        v13.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color), ColorSequenceKeypoint.new(1, Color) });
        local v14 = TweenInfo.new(u10, Enum.EasingStyle.Circular, Enum.EasingDirection.Out, u11 and -1 or 1, false, u12);
        local v15 = {
            Offset = Vector2.new(1.1, 0)
        };

        if not u6.OnHover then
            TweenService:Create(u8:getValue(), v14, v15):Play();

            return;
        end;

        local u16 = TweenService:Create(u8:getValue(), TweenInfo.new(u10, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), v15);
        local u17 = TweenService:Create(u8:getValue(), TweenInfo.new(u10, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), {
            Offset = Vector2.new(-1, 0)
        });
        v13.Parent.MouseEnter:Connect(function() -- Line: 84
            -- upvalues: u16 (copy)
            u16:Play();
        end);
        v13.Parent.MouseLeave:Connect(function() -- Line: 87
            -- upvalues: u17 (copy)
            u17:Play();
        end);
    end, {});
    local v18 = {};

    for i, v in u6 do
        v18[i] = v;
    end;

    v18.Speed = nil;
    v18.Loop = nil;
    v18.DelayTime = nil;
    v18.Color = nil;
    v18.Variation = nil;
    v18.OnHover = nil;
    local v19 = {
        BorderSizePixel = 0,
        Size = UDim2.fromScale(1, 1),
        BackgroundColor3 = ColorUtil.WHITE
    };

    for i, v in v18 do
        v19[i] = v;
    end;

    return u2.createElement("Frame", v19, { u2.createElement("UIGradient", {
            Rotation = 30,
            [u2.Ref] = u8,
            Offset = Vector2.new(-1, 0)
        }) });
end;

return {
    ShineEffectVariation = u5,
    ShineEffect = v3.new(u2)(v20)
};