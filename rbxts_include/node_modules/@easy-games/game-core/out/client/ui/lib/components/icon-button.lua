-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local AutoSizedText = v1.import(script, script.Parent, "tooltip", "autosized-text").AutoSizedText;
local TooltipContainer = v1.import(script, script.Parent, "tooltip", "tooltip-container").TooltipContainer;
local v4 = u3.Component:extend("IconButton");

function v4.init(p5) -- Line: 14
    -- upvalues: u2 (copy)
    p5.hoverMaid = u2.new();
end;

function v4.render(u6) -- Line: 17
    -- upvalues: u3 (copy), ColorUtil (copy), SoundManager (copy), GameCoreClientConfig (copy), TweenService (copy), TooltipContainer (copy), AutoSizedText (copy)
    local u7 = {};

    for i, v in u6.props do
        u7[i] = v;
    end;

    u7.Image = nil;
    u7.OnClick = nil;
    u7.IconProps = nil;
    u7.CornerRadiusScale = nil;
    u7.Tooltip = nil;
    u7[u3.Children] = nil;
    local v14 = {
        BackgroundColor3 = ColorUtil.BLACK,
        BackgroundTransparency = 1,
        ImageTransparency = 1,

        [u3.Event.MouseButton1Click] = function() -- Line: 33
            -- upvalues: SoundManager (ref), GameCoreClientConfig (ref), u6 (copy)
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
            local OnClick = u6.props.OnClick;

            if OnClick ~= nil then
                OnClick();
            end;
        end,

        [u3.Event.MouseEnter] = function(p8) -- Line: 40
            -- upvalues: SoundManager (ref), GameCoreClientConfig (ref), u6 (copy), TweenService (ref)
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_HOVER);
            u6.hoverMaid:DoCleaning();
            local u9 = TweenService:Create(p8, TweenInfo.new(0.12), {
                BackgroundTransparency = 0.6
            });
            u9:Play();
            u6.hoverMaid:GiveTask(function() -- Line: 47
                -- upvalues: u9 (copy)
                u9:Cancel();
            end);
        end,

        [u3.Event.MouseLeave] = function(p10) -- Line: 51
            -- upvalues: u6 (copy), TweenService (ref), u7 (copy)
            u6.hoverMaid:DoCleaning();
            local v11 = TweenInfo.new(0.12);
            local v12 = {};
            local BackgroundTransparency = u7.BackgroundTransparency;
            v12.BackgroundTransparency = BackgroundTransparency == nil and 1 or BackgroundTransparency;
            local u13 = TweenService:Create(p10, v11, v12);
            u13:Play();
            u6.hoverMaid:GiveTask(function() -- Line: 65
                -- upvalues: u13 (copy)
                u13:Cancel();
            end);
        end
    };

    for i, v in u7 do
        v14[i] = v;
    end;

    local v15 = {};
    local v16 = #v15;
    local v17;

    if u6.props.Tooltip == nil then
        v17 = false;
    else
        v17 = u3.createElement(TooltipContainer, {}, { u3.createElement(AutoSizedText, {
                TextSize = 16,
                Text = u6.props.Tooltip,
                Font = Enum.Font.SourceSansBold,
                Limits = Vector2.new(300, 60)
            }) });
    end;

    if v17 then
        v15[v16 + 1] = v17;
    end;

    local v18 = #v15;
    local v19 = {};
    local CornerRadiusScale = u6.props.CornerRadiusScale;
    v19.CornerRadius = UDim.new(CornerRadiusScale == nil and 1 or CornerRadiusScale, 0);
    v15[v18 + 1] = u3.createElement("UICorner", v19);
    v15[v18 + 2] = u3.createElement("UIPadding", {
        PaddingTop = UDim.new(0.1, 0),
        PaddingBottom = UDim.new(0.1, 0),
        PaddingLeft = UDim.new(0.1, 0),
        PaddingRight = UDim.new(0.1, 0)
    });
    local v20 = {
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Size = UDim2.fromScale(1, 1),
        Position = UDim2.fromScale(0.5, 0.5),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Image = u6.props.Image,
        ZIndex = u7.ZIndex
    };
    local IconProps = u6.props.IconProps;

    if IconProps then
        for i, v in IconProps do
            v20[i] = v;
        end;
    end;

    local v21 = {};
    local v22 = #v21;
    local v23 = u6.props[u3.Children];

    if v23 then
        for i, v in v23 do
            if type(i) == "number" then
                v21[v22 + i] = v;
            else
                v21[i] = v;
            end;
        end;
    end;

    v15.Icon = u3.createElement("ImageLabel", v20, v21);

    return u3.createFragment({
        IconButtonWrapper = u3.createElement("ImageButton", v14, v15)
    });
end;

function v4.willUnmount(p24) -- Line: 132
    p24.hoverMaid:DoCleaning();
end;

return {
    IconButton = v4
};