-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local Empty = v1.import(script, script.Parent.Parent.Parent.Parent, "components", "empty").Empty;
local SpriteSheet = v1.import(script, script.Parent.Parent, "sprite-sheet").SpriteSheet;

return {
    ChineseLantern = v3.new(u2)(function(p4, p5) -- Line: 7
        -- upvalues: u2 (copy), Empty (copy), SpriteSheet (copy)
        local _ = p5.useState;
        local v6 = {
            Size = UDim2.fromScale(1, 1)
        };

        for i, v in p4 do
            v6[i] = v;
        end;

        return u2.createElement(Empty, v6, {
            Lantern = u2.createElement(SpriteSheet, {
                ScaleType = "Fit",
                Size = UDim2.fromScale(1, 1),
                SpriteSheet = {
                    image = "rbxassetid://8659128085",
                    imageWidth = 1024,
                    imageHeight = 512,
                    rows = 2,
                    columns = 5,
                    sprites = 10,
                    config = {
                        framesPerSecond = 10
                    }
                }
            }),
            FireFlies = u2.createElement(SpriteSheet, {
                ScaleType = "Fit",
                Size = UDim2.fromScale(1, 1),
                SpriteSheet = {
                    image = "rbxassetid://8659686244",
                    imageWidth = 1024,
                    imageHeight = 512,
                    rows = 2,
                    columns = 5,
                    sprites = 10,
                    config = {
                        framesPerSecond = 10
                    }
                }
            })
        });
    end)
};