-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TextService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TextService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;

return {
    TabButtonUnderline = v3.new(u2)(function(u4, p5) -- Line: 9
        -- upvalues: TextService (copy), ColorUtil (copy), u2 (copy), SoundManager (copy), GameCoreClientConfig (copy)
        local v6, u7 = p5.useState(false);
        local ButtonProps = u4.ButtonProps;

        if ButtonProps ~= nil then
            ButtonProps = ButtonProps.TextSize;
        end;

        local v8 = TextService:GetTextSize(u4.TabDetail.text, ButtonProps == nil and 15 or ButtonProps, Enum.Font.Roboto, Vector2.new(1000, 1000));
        local v9 = {
            Size = UDim2.new(0, v8.X + 1, 1, 0)
        };
        local v10;

        if u4.Active then
            v10 = u4.UIConfig;

            if v10 ~= nil then
                v10 = v10.ActiveColor;
            end;

            if v10 == nil then
                v10 = ColorUtil.hexColor(7040959);
            end;
        else
            v10 = ColorUtil.BLACK;
        end;

        v9.BackgroundColor3 = v10;
        v9.BackgroundTransparency = 1;
        v9.Text = u4.TabDetail.text;
        v9.FontFace = Font.fromName("Roboto", Enum.FontWeight.Bold);
        local v11;

        if u4.Active then
            v11 = u4.UIConfig;

            if v11 ~= nil then
                v11 = v11.ActiveTextColor;
            end;

            if v11 == nil then
                v11 = ColorUtil.hexColor(12172543);
            end;
        else
            v11 = ColorUtil.WHITE;
        end;

        v9.TextColor3 = v11;
        v9.TextSize = 16;
        v9.TextTransparency = (v6 or u4.Active) and 0 or 0.3;
        v9.TextXAlignment = "Center";
        v9.TextYAlignment = "Center";

        v9[u2.Event.Activated] = function() -- Line: 62
            -- upvalues: u4 (copy), SoundManager (ref), GameCoreClientConfig (ref)
            u4.OnChange(u4.TabDetail);
            SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
        end;

        v9[u2.Event.MouseEnter] = function(p12) -- Line: 66
            -- upvalues: u7 (copy)
            u7(true);
        end;

        v9[u2.Event.MouseLeave] = function(p13) -- Line: 69
            -- upvalues: u7 (copy)
            u7(false);
        end;

        v9.AutoButtonColor = false;
        v9.LayoutOrder = u4.TabDetail.layoutOrder;
        local ButtonProps2 = u4.ButtonProps;

        if ButtonProps2 then
            for i, v in ButtonProps2 do
                v9[i] = v;
            end;
        end;

        local Active = u4.Active;

        if Active then
            local v14 = {
                Size = UDim2.new(1, 0, 0.08, 0),
                AnchorPoint = Vector2.new(0.5, 1),
                Position = UDim2.fromScale(0.5, 1)
            };
            local UIConfig = u4.UIConfig;

            if UIConfig ~= nil then
                UIConfig = UIConfig.ActiveTextColor;
            end;

            if UIConfig == nil then
                UIConfig = ColorUtil.hexColor(12172543);
            end;

            v14.BackgroundColor3 = UIConfig;
            v14.BorderSizePixel = 0;
            local ButtonProps3 = u4.ButtonProps;

            if ButtonProps3 ~= nil then
                ButtonProps3 = ButtonProps3.ZIndex;
            end;

            v14.ZIndex = (ButtonProps3 == nil and 1 or ButtonProps3) + 1;
            Active = u2.createFragment({
                Underline = u2.createElement("Frame", v14)
            });
        end;

        local v15 = { u2.createElement("UICorner", {
                CornerRadius = UDim.new(0, 5)
            }), u2.createElement("UITextSizeConstraint", {
                MaxTextSize = 18
            }) };
        local v16 = #v15;

        if Active then
            v15[v16 + 1] = Active;
        end;

        return u2.createElement("TextButton", v9, v15);
    end)
};