-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local preloadImages = v1.import(script, script.Parent.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").preloadImages;
local Empty = v1.import(script, script.Parent.Parent, "empty").Empty;
local Padding = v1.import(script, script.Parent.Parent, "padding").Padding;
local AutoSizedText = v1.import(script, script.Parent.Parent, "tooltip", "autosized-text").AutoSizedText;
local TooltipContainer = v1.import(script, script.Parent.Parent, "tooltip", "tooltip-container").TooltipContainer;

return {
    TopBarButton = v3.new(u2)(function(u4, p5) -- Line: 13
        -- upvalues: GameTheme (copy), ColorUtil (copy), preloadImages (copy), u2 (copy), TooltipContainer (copy), AutoSizedText (copy), TweenService (copy), Padding (copy), Empty (copy)
        local useEffect = p5.useEffect;
        local Selected = u4.Selected;

        if Selected == nil then
            Selected = false;
        end;

        local u6, u7 = p5.useState(Selected);
        local Image = u4.Image;
        local BackgroundTransparency = u4.BackgroundTransparency;

        if BackgroundTransparency == nil then
            BackgroundTransparency = GameTheme.topBarBGTransparency;
        end;

        local u8 = u4.BackgroundColor or ColorUtil.BLACK;
        useEffect(function() -- Line: 29
            -- upvalues: u4 (copy), u7 (copy)
            if u4.Selected ~= nil then
                u7(u4.Selected);
            end;
        end, { u4.Selected });
        useEffect(function() -- Line: 34
            -- upvalues: u4 (copy), preloadImages (ref)
            local SelectedState = u4.SelectedState;

            if SelectedState ~= nil then
                SelectedState = SelectedState.Image;
            end;

            if SelectedState ~= "" and SelectedState then
                preloadImages({ u4.SelectedState.Image });
            end;
        end, { u4.SelectedState });
        local v9 = {
            AutomaticSize = "X",
            Size = UDim2.new(0, GameTheme.topBarGuiSize, 0, GameTheme.topBarGuiSize),
            LayoutOrder = u4.LayoutOrder
        };
        local v10 = {};
        local v11 = #v10;
        local v12;

        if u4.ToolTip == nil then
            v12 = false;
        else
            v12 = u2.createElement(TooltipContainer, {}, { u2.createElement(AutoSizedText, {
                    TextSize = 14,
                    Text = u4.ToolTip,
                    Font = Enum.Font.SourceSans,
                    TextColor3 = Color3.fromRGB(255, 255, 255),
                    Limits = Vector2.new(300, 60)
                }) });
        end;

        if v12 then
            v10[v11 + 1] = v12;
        end;

        local v13 = {};
        local Text = u4.Text;
        v13.Text = (Text == "" or not Text) and "" or "<b>" .. u4.Text .. "</b>";
        v13.Size = UDim2.new(0, GameTheme.topBarGuiSize, 0, GameTheme.topBarGuiSize);
        v13.AutomaticSize = "X";
        v13.RichText = true;
        v13.TextColor3 = ColorUtil.WHITE;
        local v14;

        if u6 then
            v14 = u4.SelectedState;

            if v14 ~= nil then
                v14 = v14.BackgroundColor;
            end;

            if v14 == nil then
                v14 = ColorUtil.brighten(u8, 0.2);
            end;
        else
            v14 = u8;
        end;

        v13.BackgroundColor3 = v14;

        if u6 then
            BackgroundTransparency = BackgroundTransparency + 0.1;
        end;

        v13.BackgroundTransparency = BackgroundTransparency;
        v13.BorderSizePixel = 0;
        v13.AutoButtonColor = false;

        v13[u2.Event.Activated] = function(p15) -- Line: 88
            -- upvalues: u4 (copy), u6 (copy), u7 (copy)
            u4.OnClick(u6, u7);
        end;

        v13[u2.Event.MouseEnter] = function(p16) -- Line: 91
            -- upvalues: TweenService (ref), ColorUtil (ref)
            TweenService:Create(p16, TweenInfo.new(0.1, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), {
                BackgroundColor3 = ColorUtil.brighten(p16.BackgroundColor3, 0.2)
            }):Play();
        end;

        v13[u2.Event.MouseLeave] = function(p17) -- Line: 97
            -- upvalues: TweenService (ref), u4 (copy), ColorUtil (ref)
            TweenService:Create(p17, TweenInfo.new(0.1, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), {
                TextTransparency = 0,
                BackgroundColor3 = u4.BackgroundColor or ColorUtil.BLACK
            }):Play();
        end;

        v13[u2.Event.MouseButton1Down] = function(p18) -- Line: 104
            -- upvalues: TweenService (ref), ColorUtil (ref), u8 (copy)
            TweenService:Create(p18, TweenInfo.new(0.05, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), {
                TextTransparency = 0.4,
                BackgroundColor3 = ColorUtil.darken(u8, 0.8)
            }):Play();
        end;

        v13[u2.Event.MouseButton1Up] = function(p19) -- Line: 111
            -- upvalues: TweenService (ref), ColorUtil (ref), u8 (copy)
            TweenService:Create(p19, TweenInfo.new(0.05, Enum.EasingStyle.Circular, Enum.EasingDirection.Out), {
                TextTransparency = 0,
                BackgroundColor3 = ColorUtil.brighten(u8, 0.2)
            }):Play();
        end;

        local v20 = u4.Image ~= nil;

        if v20 then
            local v21 = {};
            local ImageScale = u4.ImageScale;
            local ImageScale2 = u4.ImageScale;
            v21.Size = UDim2.fromScale(ImageScale == nil and 1 or ImageScale, ImageScale2 == nil and 1 or ImageScale2);
            v21.Position = UDim2.fromScale(0.5, 0.5);
            v21.AnchorPoint = Vector2.new(0.5, 0.5);
            v21.BackgroundTransparency = 1;
            local v22;

            if u6 then
                v22 = u4.SelectedState;

                if v22 ~= nil then
                    v22 = v22.Image;
                end;

                if v22 == nil then
                    v22 = u4.Image;
                end;
            else
                v22 = u4.Image;
            end;

            v21.Image = v22;
            v21.ScaleType = "Fit";
            v20 = u2.createElement("ImageLabel", v21);
        end;

        local v23 = { u2.createElement("UICorner", {
                CornerRadius = UDim.new(0, 6)
            }), u2.createElement(Padding, {
                Padding = UDim.new(0, (Image == "" or not Image) and 7 or 6)
            }) };
        local v24 = #v23;

        if v20 then
            v23[v24 + 1] = v20;
        end;

        local v25 = #v23;
        local v26 = u4[u2.Children];

        if v26 then
            for i, v in v26 do
                if type(i) == "number" then
                    v23[v25 + i] = v;
                else
                    v23[i] = v;
                end;
            end;
        end;

        v10[#v10 + 1] = u2.createElement("TextButton", v13, v23);

        return u2.createElement(Empty, v9, v10);
    end)
};