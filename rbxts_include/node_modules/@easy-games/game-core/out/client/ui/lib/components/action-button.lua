-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local ContextActionService = v1.import(script, v1.getModule(script, "@rbxts", "services")).ContextActionService;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local DeviceUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;
local UIUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").UIUtil;

return {
    ActionButton = v3.new(u2)(function(u4, p5) -- Line: 9
        -- upvalues: ContextActionService (copy), DeviceUtil (copy), GameTheme (copy), UIUtil (copy), u2 (copy)
        local _ = p5.useState;
        p5.useEffect(function() -- Line: 12
            -- upvalues: u4 (copy), ContextActionService (ref)
            local v6 = {};

            if u4.interactionKey then
                table.insert(v6, u4.interactionKey);
            end;

            if u4.gamepadInteractionKey then
                table.insert(v6, u4.gamepadInteractionKey);
            end;

            local function v10(p7, p8, p9) -- Line: 24
                -- upvalues: u4 (ref)
                if p8 == Enum.UserInputState.Begin then
                    u4.onActivated();
                end;
            end;

            local priority = u4.priority;
            ContextActionService:BindActionAtPriority(u4.actionName, v10, false, priority == nil and 1 or priority, unpack(v6));

            return function() -- Line: 34
                -- upvalues: ContextActionService (ref), u4 (ref)
                ContextActionService:UnbindAction(u4.actionName);
            end;
        end, {});
        local text = u4.text;

        if DeviceUtil.isMobileControls() then
            text = u4.text;
        elseif DeviceUtil.isGamepadControls() and u4.gamepadInteractionKey then
            text = u4.text .. " [" .. u4.gamepadInteractionKey.Name .. "]";
        else
            local interactionKey = u4.interactionKey;

            if interactionKey ~= nil then
                interactionKey = interactionKey.Name;
            end;

            if interactionKey then
                text = u4.text .. " [" .. u4.interactionKey.Name .. "]";
            end;
        end;

        local v11 = {};
        local LayoutOrder = u4.LayoutOrder;

        if LayoutOrder == nil then
            LayoutOrder = GameTheme.abilityActionBarPriority;
        end;

        v11.LayoutOrder = LayoutOrder;
        v11.Size = GameTheme.actionBarButtonSize;
        v11.Position = UIUtil:getActionBarPosition();
        v11.AnchorPoint = Vector2.new(0.5, 1);
        v11.BackgroundColor3 = Color3.fromRGB(0, 0, 0);
        v11.BackgroundTransparency = 0.6;
        v11.BorderSizePixel = 0;

        v11[u2.Event.Activated] = function() -- Line: 64
            -- upvalues: u4 (copy)
            u4.onActivated();
        end;

        return u2.createFragment({
            ActionButton = u2.createElement("ImageButton", v11, { u2.createElement("UIAspectRatioConstraint", {
                    AspectRatio = 4.675675675675675
                }), u2.createElement("TextLabel", {
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Font = "Roboto",
                    RichText = true,
                    TextScaled = true,
                    Size = UDim2.fromScale(0.8, 0.5),
                    Position = UDim2.fromScale(0.5, 0.5),
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    Text = "<b>" .. text .. "</b>",
                    TextColor3 = Color3.fromRGB(255, 255, 255)
                }) })
        });
    end)
};