-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local v3 = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local ImageId = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "images", "image-id").ImageId;
local Empty = v1.import(script, script.Parent, "empty").Empty;

return {
    CircularSpinner = v3.new(u2)(function(p4, p5) -- Line: 8
        -- upvalues: u2 (copy), TweenService (copy), ImageId (copy), Empty (copy)
        local useEffect = p5.useEffect;
        local u6 = u2.createRef();
        useEffect(function() -- Line: 11
            -- upvalues: u6 (copy), TweenService (ref)
            local v7 = u6:getValue();

            if v7 then
                TweenService:Create(v7, TweenInfo.new(4, Enum.EasingStyle.Linear, Enum.EasingDirection.In, -1), {
                    Rotation = 360
                }):Play();
            end;
        end, {});
        local v8 = {};

        for i, v in p4 do
            v8[i] = v;
        end;

        v8[u2.Children] = nil;
        local v9 = {};

        for i, v in v8 do
            v9[i] = v;
        end;

        local v10 = { u2.createElement("ImageLabel", {
                ZIndex = 1000,
                Image = ImageId.LOADING_SPINNER,
                BackgroundTransparency = 1,
                Size = UDim2.fromScale(1, 1),
                Position = UDim2.fromScale(0.5, 0.5),
                AnchorPoint = Vector2.new(0.5, 0.5),
                [u2.Ref] = u6
            }) };
        local v11 = #v10;
        local v12 = p4[u2.Children];

        if v12 then
            for i, v in v12 do
                if type(i) == "number" then
                    v10[v11 + i] = v;
                else
                    v10[i] = v;
                end;
            end;
        end;

        return u2.createElement(Empty, v9, v10);
    end)
};