-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local GameCoreClientConfig = v1.import(script, script.Parent.Parent.Parent.Parent, "config", "game-core-config").GameCoreClientConfig;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local Empty = v1.import(script, script.Parent, "empty").Empty;
local v4 = u3.Component:extend("Checkbox");

function v4.init(p5) -- Line: 14
    -- upvalues: u2 (copy)
    p5.hoverMaid = u2.new();
end;

function v4.render(u6) -- Line: 17
    -- upvalues: u3 (copy), TweenService (copy), SoundManager (copy), GameCoreClientConfig (copy), Empty (copy), ColorUtil (copy), GameTheme (copy)
    local v7 = {};

    for i, v in u6.props do
        v7[i] = v;
    end;

    v7[u3.Children] = nil;
    v7.Value = nil;
    v7.SetValue = nil;
    v7.Text = nil;
    v7.AspectRatio = nil;
    v7.BoxColor = nil;
    local v8 = {
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ImageTransparency = 1,
        AutoButtonColor = false,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    };

    for i, v in v7 do
        v8[i] = v;
    end;

    v8[u3.Event.MouseEnter] = function(p9) -- Line: 39
        -- upvalues: u6 (copy), TweenService (ref)
        u6.hoverMaid:DoCleaning();
        local u10 = TweenService:Create(p9, TweenInfo.new(0.12), {
            BackgroundTransparency = 0.85
        });
        u6.hoverMaid:GiveTask(function() -- Line: 44
            -- upvalues: u10 (copy)
            u10:Cancel();
        end);
        u10:Play();
    end;

    v8[u3.Event.MouseLeave] = function(p11) -- Line: 49
        -- upvalues: u6 (copy), TweenService (ref)
        u6.hoverMaid:DoCleaning();
        local u12 = TweenService:Create(p11, TweenInfo.new(0.12), {
            BackgroundTransparency = 1
        });
        u6.hoverMaid:GiveTask(function() -- Line: 54
            -- upvalues: u12 (copy)
            u12:Cancel();
        end);
        u12:Play();
    end;

    v8[u3.Event.MouseButton1Click] = function() -- Line: 59
        -- upvalues: SoundManager (ref), GameCoreClientConfig (ref), u6 (copy)
        SoundManager:playSound(GameCoreClientConfig.Sounds.UI_CLICK);
        u6.props.SetValue(not u6.props.Value);
    end;

    local createElement = u3.createElement;
    local v13 = { u3.createElement("UICorner", {
            CornerRadius = UDim.new(0.08, 0)
        }), (u3.createElement("UIListLayout", {
            FillDirection = "Horizontal",
            VerticalAlignment = "Center"
        })) };
    local createElement2 = u3.createElement;
    local v14 = {
        SizeConstraint = "RelativeYY",
        Size = UDim2.fromScale(1, 1)
    };
    local v15 = {};
    local createElement3 = u3.createElement;
    local v16 = {
        BorderSizePixel = 0,
        Size = UDim2.fromScale(0.78, 0.78),
        Position = UDim2.fromScale(0.5, 0.5),
        AnchorPoint = Vector2.new(0.5, 0.5)
    };
    local v17;

    if u6.props.Value then
        v17 = ColorUtil.hexColor(5635993);
    else
        v17 = u6.props.BoxColor or GameTheme.backgroundPrimary;
    end;

    v16.BackgroundColor3 = v17;
    v15[1] = createElement3("Frame", v16);
    v13.Checkbox = createElement2(Empty, v14, v15);
    v13.CheckboxText = u3.createElement("TextLabel", {
        SizeConstraint = "RelativeYY",
        TextXAlignment = "Left",
        TextYAlignment = "Center",
        Font = "Roboto",
        TextTransparency = 0.18,
        RichText = true,
        TextScaled = true,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Text = u6.props.Text,
        Size = UDim2.fromScale(u6.props.AspectRatio - 1, 0.7),
        TextColor3 = Color3.fromRGB(255, 255, 255)
    }, { u3.createElement("UIPadding", {
            PaddingLeft = UDim.new(0.06, 0)
        }) });

    return createElement("ImageButton", v8, v13);
end;

return {
    Checkbox = v4
};