-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local CarouselImage = v1.import(script, script, "image").CarouselImage;
local v3 = u2.Component:extend("CarouselComponent");

function v3.init(p4, p5) -- Line: 8
    -- upvalues: u2 (copy)
    p4.pageRef = u2.createRef();
    p4:setState({
        currentIndex = 0
    });
end;

function v3.render(u6) -- Line: 14
    -- upvalues: u2 (copy), CarouselImage (copy)
    local v7 = {
        BackgroundTransparency = 1,
        ClipsDescendants = true,
        AnchorPoint = u6.props.AnchorPoint,
        Position = u6.props.Position,
        Size = u6.props.Size
    };
    local v8 = {};
    local Images = u6.props.Images;

    local function _(p9, p10) -- Line: 25
        -- upvalues: u2 (ref), CarouselImage (ref), u6 (copy)
        return u2.createElement(CarouselImage, {
            TarmacData = p9,
            Index = p10,
            CurrentIndex = u6.state.currentIndex,
            PageRef = u6.pageRef,
            AspectRatio = u6.props.ImageAspectRatio
        });
    end;

    local v11 = table.create(#Images);

    for i, v in Images do
        v11[i] = u2.createElement(CarouselImage, {
            TarmacData = v,
            Index = i - 1,
            CurrentIndex = u6.state.currentIndex,
            PageRef = u6.pageRef,
            AspectRatio = u6.props.ImageAspectRatio
        });
    end;

    local v12 = {
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0),
        Position = UDim2.fromScale(0.5, 0),
        Size = UDim2.fromScale(u6.props.ImageContentWidth, 1)
    };
    local v18 = { u2.createElement("UIPageLayout", {
            Animated = true,
            Circular = true,
            EasingDirection = Enum.EasingDirection.InOut,
            EasingStyle = Enum.EasingStyle.Quad,
            Padding = UDim.new(0, 0),
            TweenTime = 0.2,
            FillDirection = Enum.FillDirection.Horizontal,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            VerticalAlignment = Enum.VerticalAlignment.Center,
            SortOrder = Enum.SortOrder.LayoutOrder,
            [u2.Ref] = u6.pageRef,

            [u2.Event.Stopped] = function(p13, u14) -- Line: 59
                -- upvalues: u6 (copy)
                if not (u14 and u14:IsA("ImageButton")) then
                    return nil;
                end;

                local function _(p15) -- Line: 67
                    -- upvalues: u14 (copy)
                    return p15.Image == u14.Image;
                end;

                local v16 = {};
                local v17 = -1;

                for i, v in u6.props.Images do
                    local _ = i - 1;

                    if v.Image == u14.Image == true then
                        v17 = i - 1;
                        break;
                    end;
                end;

                v16.currentIndex = v17;
                u6:setState(v16);
            end
        }) };
    local v19 = #v18;

    for i, v in v11 do
        v18[v19 + i] = v;
    end;

    v8[#v8 + 1] = u2.createElement("Frame", v12, v18);

    return u2.createFragment({
        Carousel = u2.createElement("Frame", v7, v8)
    });
end;

v3.defaultProps = {
    ImageAspectRatio = 1.5,
    ImageContentWidth = 0.65
};

return {
    CarouselComponent = v3
};