-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "maid").Maid);
local u3 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local SoundManager = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "sound", "sound-manager").SoundManager;
local GameTheme = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "ui", "game-theme").GameTheme;
local ColorUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "color-util").ColorUtil;
local DeviceUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "device-util").DeviceUtil;
local UIUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "util", "ui-utils").UIUtil;
local v4 = u3.Component:extend("Button");

function v4.init(p5) -- Line: 14
    -- upvalues: u2 (copy), u3 (copy)
    p5.hoverMaid = u2.new();
    p5.buttonRef = u3.createRef();
end;

function v4.didMount(p6) -- Line: 18
    -- upvalues: DeviceUtil (copy), UIUtil (copy)
    if p6.props.GamepadShouldAutoSelect and DeviceUtil.isGamepadControls() then
        local Ref = p6.props.Ref;

        if Ref ~= nil then
            Ref = Ref:getValue();
        end;

        if Ref == nil then
            Ref = p6.buttonRef:getValue();
        end;

        UIUtil:selectGui(Ref);
    end;
end;

function v4.render(u7) -- Line: 32
    -- upvalues: u3 (copy), GameTheme (copy), ColorUtil (copy), TweenService (copy), SoundManager (copy)
    local v8 = {};

    for i, v in u7.props do
        v8[i] = v;
    end;

    v8[u3.Children] = nil;
    v8.Text = nil;
    v8.CornerRadius = nil;
    v8.OnClick = nil;
    v8.hoverBrightness = nil;
    v8.TextLabel = nil;
    v8.DisableDefaultSound = nil;
    v8.GamepadShouldAutoSelect = nil;
    local u9 = u7.props.BackgroundColor3 or GameTheme.backgroundSecondary;
    local hoverBrightness = u7.props.hoverBrightness;
    local u10 = ColorUtil.brighten(u9, hoverBrightness == nil and 0.05 or hoverBrightness);
    local v15 = {
        BorderSizePixel = 0,
        AutoButtonColor = false,
        BackgroundColor3 = u9,

        [u3.Event.MouseEnter] = function(p11) -- Line: 57
            -- upvalues: u7 (copy), TweenService (ref), u10 (copy), SoundManager (ref), GameTheme (ref)
            u7.hoverMaid:DoCleaning();
            local u12 = TweenService:Create(p11, TweenInfo.new(0.12), {
                BackgroundColor3 = u10
            });
            u12:Play();
            u7.hoverMaid:GiveTask(function() -- Line: 63
                -- upvalues: u12 (copy)
                u12:Cancel();
            end);

            if not u7.props.DisableDefaultSound then
                SoundManager:playSound(GameTheme.uiHover);
            end;
        end,

        [u3.Event.MouseLeave] = function(p13) -- Line: 70
            -- upvalues: u7 (copy), TweenService (ref), u9 (copy)
            u7.hoverMaid:DoCleaning();
            local u14 = TweenService:Create(p13, TweenInfo.new(0.12), {
                BackgroundColor3 = u9
            });
            u14:Play();
            u7.hoverMaid:GiveTask(function() -- Line: 76
                -- upvalues: u14 (copy)
                u14:Cancel();
            end);
        end,

        [u3.Event.MouseButton1Click] = function() -- Line: 80
            -- upvalues: u7 (copy), SoundManager (ref), GameTheme (ref)
            u7.props.OnClick();

            if not u7.props.DisableDefaultSound then
                SoundManager:playSound(GameTheme.uiClick);
            end;
        end
    };

    for i, v in v8 do
        v15[i] = v;
    end;

    v15[u3.Ref] = v8.Ref or u7.buttonRef;
    local v16 = { u3.createElement("UICorner", {
            CornerRadius = u7.props.CornerRadius or UDim.new(0.08, 0)
        }) };
    local v17 = #v16;
    local v18 = u7.props[u3.Children];

    if v18 then
        for i, v in v18 do
            if type(i) == "number" then
                v16[v17 + i] = v;
            else
                v16[i] = v;
            end;
        end;
    end;

    local v19 = #v16;
    local v20 = u7.props.Text ~= nil;

    if v20 then
        local v21 = {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            TextScaled = true,
            RichText = true,
            Font = "Roboto",
            Text = u7.props.Text,
            Size = UDim2.fromScale(0.8, 0.45),
            Position = UDim2.fromScale(0.5, 0.5),
            AnchorPoint = Vector2.new(0.5, 0.5),
            TextColor3 = Color3.fromRGB(255, 255, 255)
        };
        local TextLabel = u7.props.TextLabel;

        if TextLabel then
            for i, v in TextLabel do
                v21[i] = v;
            end;
        end;

        v20 = u3.createElement("TextLabel", v21);
    end;

    if v20 then
        v16[v19 + 1] = v20;
    end;

    return u3.createElement("ImageButton", v15, v16);
end;

return {
    Button = v4
};