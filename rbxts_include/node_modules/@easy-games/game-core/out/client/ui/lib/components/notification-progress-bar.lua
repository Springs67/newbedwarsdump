-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local NOTIFICATION_TIME = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "notification", "notification-const").NOTIFICATION_TIME;
local v3 = u2.Component:extend("NotificationProgressBar");

function v3.init(p4) -- Line: 9
    -- upvalues: u2 (copy)
    p4.progressRef = u2.createRef();
end;

function v3.didMount(p5) -- Line: 12
    -- upvalues: TweenService (copy), NOTIFICATION_TIME (copy)
    local v6 = p5.progressRef:getValue();
    local ProgressBarTime = p5.props.ProgressBarTime;

    if ProgressBarTime == nil then
        ProgressBarTime = NOTIFICATION_TIME;
    end;

    TweenService:Create(v6, TweenInfo.new(ProgressBarTime, Enum.EasingStyle.Linear), {
        Size = UDim2.fromScale(0, 1)
    }):Play();
end;

function v3.render(p7) -- Line: 24
    -- upvalues: u2 (copy)
    local v8 = {};

    for i, v in p7.props do
        v8[i] = v;
    end;

    v8.ForegroundColor3 = nil;
    v8.ProgressBarTime = nil;
    v8[u2.Children] = nil;
    local v9 = {
        BackgroundTransparency = 0.58,
        BorderSizePixel = 0,
        BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    };

    for i, v in v8 do
        v9[i] = v;
    end;

    local v10 = { u2.createElement("UICorner", {
            CornerRadius = UDim.new(100, 0)
        }) };
    local v11 = #v10;
    local v12 = p7.props[u2.Children];

    if v12 then
        for i, v in v12 do
            if type(i) == "number" then
                v10[v11 + i] = v;
            else
                v10[i] = v;
            end;
        end;
    end;

    v10[#v10 + 1] = u2.createElement("Frame", {
        [u2.Ref] = p7.progressRef,
        Size = UDim2.fromScale(1, 1),
        BackgroundColor3 = p7.props.ForegroundColor3 or Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 0.2,
        BorderSizePixel = 0
    }, { u2.createElement("UICorner", {
            CornerRadius = UDim.new(100, 0)
        }) });

    return u2.createElement("Frame", v9, v10);
end;

return {
    NotificationProgressBar = v3
};