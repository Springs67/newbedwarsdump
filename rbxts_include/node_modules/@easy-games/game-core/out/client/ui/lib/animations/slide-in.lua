-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local TweenService = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local v3 = u2.Component:extend("SlideIn");

function v3.init(p4) -- Line: 8
    -- upvalues: u2 (copy)
    p4.wrapperRef = u2.createRef();
end;

function v3.didMount(p5) -- Line: 11
    -- upvalues: TweenService (copy)
    TweenService:Create(p5.wrapperRef:getValue(), p5.props.TweenInfo or TweenInfo.new(0.08), {
        Position = p5.props.EndPosition or UDim2.fromScale(0, 0)
    }):Play();
end;

function v3.render(p6) -- Line: 16
    -- upvalues: u2 (copy)
    local v7 = {
        [u2.Ref] = p6.wrapperRef,
        Position = p6.props.StartPosition or UDim2.fromScale(0, 0.05),
        Size = UDim2.fromScale(1, 1),
        BackgroundTransparency = 1,
        BorderSizePixel = 0
    };
    local v8 = {};
    local v9 = #v8;
    local v10 = p6.props[u2.Children];

    if v10 then
        for i, v in v10 do
            if type(i) == "number" then
                v8[v9 + i] = v;
            else
                v8[i] = v;
            end;
        end;
    end;

    return u2.createElement("Frame", v7, v8);
end;

return {
    SlideIn = v3
};