-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local u2 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);

return {
    Padding = function(p3) -- Line: 4, Name: Padding
        -- upvalues: u2 (copy)
        if typeof(p3.Padding) == "UDim" then
            return u2.createElement("UIPadding", {
                PaddingBottom = p3.Padding,
                PaddingLeft = p3.Padding,
                PaddingTop = p3.Padding,
                PaddingRight = p3.Padding
            });
        end;

        local Padding = p3.Padding;
        local Left = Padding.Left;
        local Right = Padding.Right;
        local Top = Padding.Top;
        local Bottom = Padding.Bottom;
        local Vertical = Padding.Vertical;
        local v4 = Vertical == nil and 0 or Vertical;
        local Horizontal = Padding.Horizontal;
        local v5 = Horizontal == nil and 0 or Horizontal;

        return u2.createElement("UIPadding", {
            PaddingBottom = UDim.new(0, (Bottom == nil and 0 or Bottom) + v4),
            PaddingTop = UDim.new(0, (Top == nil and 0 or Top) + v4),
            PaddingRight = UDim.new(0, (Right == nil and 0 or Right) + v5),
            PaddingLeft = UDim.new(0, (Left == nil and 0 or Left) + v5)
        });
    end
};