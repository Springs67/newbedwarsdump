-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = v1.import(script, v1.getModule(script, "@rbxts", "rodux").src);
local AppInfoReducer = v1.import(script, script.Parent, "apps", "reducers", "apps").AppInfoReducer;
local FriendsReducer = v1.import(script, script.Parent, "apps", "reducers", "friends-reducer").FriendsReducer;
local GameCodesReducer = v1.import(script, script.Parent, "apps", "reducers", "game-codes-reducer").GameCodesReducer;
local v3 = {
    Notifications = v1.import(script, script.Parent, "notifications", "reducers", "notification").NotificationReducer,
    Template = v1.import(script, script.Parent, "template", "reducers", "template").TemplateReducer,
    AppInfo = AppInfoReducer,
    Friends = FriendsReducer,
    GameCodes = GameCodesReducer
};

return {
    GameCoreReducerMap = v3,
    GameCoreReducers = v2.combineReducers(v3)
};