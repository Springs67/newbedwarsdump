-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Reflect = v1.import(script, v1.getModule(script, "@flamework", "core").out).Reflect;
local Service = v1.import(script, v1.getModule(script, "@flamework", "core").out).Service;
local v2 = v1.import(script, v1.getModule(script, "@rbxts", "services"));
local Players = v2.Players;
local Workspace = v2.Workspace;
local GameCoreServerSyncEvents = v1.import(script, script.Parent.Parent, "game-core-server-events").GameCoreServerSyncEvents;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 12, Name: __tostring
        return "TeleportService";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 17
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5) -- Line: 21
end;

function u3.onStart(p6) -- Line: 23
end;

function u3.teleportInstance(p7, p8, p9) -- Line: 25
    -- upvalues: Players (copy), GameCoreServerSyncEvents (copy), Workspace (copy)
    local v10 = p8:GetPivot();

    if typeof(p9) ~= "CFrame" then
        p9 = CFrame.new(p9);
    end;

    local v11 = Players:GetPlayerFromCharacter(p8);

    if GameCoreServerSyncEvents.InstanceTeleportEvent:fire(p8, v10, p9, v11):isCancelled() then
        return nil;
    end;

    if v11 then
        v11:SetAttribute("LastTeleported", Workspace:GetServerTimeNow());
    end;

    p8:PivotTo(p9);
end;

Reflect.defineMetadata(u3, "identifier", "@easy-games/game-core:server/services/teleport-service@TeleportService");
Reflect.defineMetadata(u3, "flamework:implements", { "$:flamework@OnStart" });
Reflect.decorate(u3, "$:flamework@Service", Service, { {} });

return {
    TeleportService = u3
};