-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local PermissionGroupBuilder = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "permission", "permissions").PermissionGroupBuilder;
local OfflinePlayerUtil = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "player", "offline-player-util").OfflinePlayerUtil;
local ChatCommand = v1.import(script, script.Parent.Parent, "chat-command").ChatCommand;
local CommandArgumentType = v1.import(script, script.Parent.Parent, "command-types").CommandArgumentType;
local CommandUtil = v1.import(script, script.Parent.Parent, "command-util").CommandUtil;
local u2 = setmetatable({}, {
    __tostring = function() -- Line: 13, Name: __tostring
        return "TestCommand";
    end,

    __index = ChatCommand
});
u2.__index = u2;

function u2.new(...) -- Line: 19
    -- upvalues: u2 (ref)
    local v3 = setmetatable({}, u2);

    return v3:constructor(...) or v3;
end;

function u2.constructor(p4) -- Line: 23
    -- upvalues: ChatCommand (copy), PermissionGroupBuilder (copy), CommandArgumentType (copy)
    ChatCommand.constructor(p4, { "test" }, "testing purposes", PermissionGroupBuilder.everyone(), {
        {
            {
                name = "target",
                type = CommandArgumentType.Player
            }
        }
    });
end;

function u2.onRun(p5, p6, p7, p8) -- Line: 29
    -- upvalues: CommandUtil (copy), CommandArgumentType (copy), Flamework (copy)
    local v9 = CommandUtil.validateCommandArguments(p6, p8, {
        {
            name = "target",
            type = CommandArgumentType.Player
        }
    });

    if p6 then
        if v9.success == true then
            Flamework.resolveDependency("@easy-games/game-core:server/services/notification-service@NotificationService"):sendInfoNotification({ p6 }, {
                message = "test !! " .. v9.value.target[1].DisplayName
            });

            return;
        end;

        Flamework.resolveDependency("@easy-games/game-core:server/services/notification-service@NotificationService"):sendErrorNotification({ p6 }, {
            message = "invalid arguments for test command"
        });
    end;
end;

function u2.canUseCommandOverride(p10, p11) -- Line: 46
    -- upvalues: OfflinePlayerUtil (copy)
    if p11 ~= nil then
        p11 = p11.UserId;
    end;

    return p11 == OfflinePlayerUtil.Dummy.Asen.userId;
end;

return {
    TestCommand = u2
};