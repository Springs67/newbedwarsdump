-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local CollectionService = v1.import(script, v1.getModule(script, "@rbxts", "services")).CollectionService;

local function WatchCollectionTag(p2, u3) -- Line: 8
    -- upvalues: CollectionService (copy)
    local v5 = CollectionService:GetInstanceAddedSignal(p2):Connect(function(p4) -- Line: 9
        -- upvalues: u3 (copy)
        u3(p4);
    end);

    for _, v in CollectionService:GetTagged(p2) do
        task.spawn(function() -- Line: 13
            -- upvalues: u3 (copy), v (copy)
            u3(v);
        end);
    end;

    return v5;
end;

return {
    WatchCollectionTag = WatchCollectionTag,

    CollectionTagAdded = function(p6, p7) -- Line: 24, Name: CollectionTagAdded
        -- upvalues: WatchCollectionTag (copy)
        return WatchCollectionTag(p6, p7);
    end
};