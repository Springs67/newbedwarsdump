-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local Flamework = v1.import(script, v1.getModule(script, "@flamework", "core").out).Flamework;
local RunService = v1.import(script, v1.getModule(script, "@rbxts", "services")).RunService;
local GameCoreSharedConfig = v1.import(script, script.Parent.Parent, "config", "game-core-shared-config").GameCoreSharedConfig;
local v2 = {};
local v3 = setmetatable({}, {
    __index = v2
});
v3.DISABLED = "disabled";
v2.disabled = "DISABLED";
v3.SILENCED = "silenced";
v2.silenced = "SILENCED";
v3.PROGRESSING = "progressing";
v2.progressing = "PROGRESSING";
v3.PARTIALLY_CHARGED = "partially_charged";
v2.partially_charged = "PARTIALLY_CHARGED";
v3.READY = "ready";
v2.ready = "READY";
v3.ON_COOLDOWN = "on_cooldown";
v2.on_cooldown = "ON_COOLDOWN";

return {
    getAbilityMeta = function(p4) -- Line: 25, Name: getAbilityMeta
        -- upvalues: GameCoreSharedConfig (copy), RunService (copy), Flamework (copy)
        local v5 = GameCoreSharedConfig.AbilityMetatable[p4];

        if v5 then
            return v5;
        end;

        if RunService:IsServer() then
            return Flamework.resolveDependency("@easy-games/game-core:server/services/ability/ability-service@AbilityService"):getCustomAbilityMeta(p4);
        end;

        return Flamework.resolveDependency("@easy-games/game-core:client/controllers/ability/ability-controller@AbilityController"):getCustomAbilityMeta(p4);
    end,

    AbilityState = v3
};