-- Decompiled with Potassium's decompiler.

local u1 = _G[script].import(script, script.Parent);

return {
    GetTarmacAsset = function(p2, p3) -- Line: 8, Name: GetTarmacAsset
        -- upvalues: u1 (copy)
        if p3 == nil then
            p3 = u1;
        end;

        local v4 = p3[p2];

        if not v4 then
            error(string.format("No asset by name %s", p2));
        end;

        if type(v4) == "string" then
            return {
                Image = v4
            };
        end;

        if type(v4) == "function" then
            return v4(2);
        end;

        return v4;
    end,

    GetTarmacAssetFromPath = function(p5) -- Line: 27, Name: GetTarmacAssetFromPath
        -- upvalues: u1 (copy)
        local v6 = u1;

        for _, v in p5 do
            v6 = v6[v];
        end;

        return v6;
    end
};