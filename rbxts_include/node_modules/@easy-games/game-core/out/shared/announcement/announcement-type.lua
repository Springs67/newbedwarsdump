-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = v1.import(script, v1.getModule(script, "@rbxts", "variant").out);
local fields = v2.fields;

return {
    AnnouncementVariant = v2.variantModule({
        text = fields(),
        image = fields(),
        countdown = fields()
    })
};