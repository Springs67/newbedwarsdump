-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local v2 = u1.import(script, script.Parent.Parent, "internal");
local getRemoteOrThrow = v2.getRemoteOrThrow;
local IS_SERVER = v2.IS_SERVER;
local waitForRemote = v2.waitForRemote;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 18, Name: __tostring
        return "ClientEvent";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 23
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5, p6) -- Line: 27
    -- upvalues: getRemoteOrThrow (copy), IS_SERVER (copy)
    p5.instance = getRemoteOrThrow("RemoteEvent", p6);
    assert(not IS_SERVER, "Cannot fetch NetClientEvent on the server!");
end;

function u3.GetInstance(p7) -- Line: 32
    return p7.instance;
end;

function u3.Wait(p8, u9) -- Line: 35
    -- upvalues: u1 (copy), waitForRemote (copy), u3 (ref)
    return u1.Promise.defer(u1.async(function(p10) -- Line: 36
        -- upvalues: u1 (ref), waitForRemote (ref), u9 (copy), u3 (ref)
        u1.await(waitForRemote("RemoteEvent", u9, 60));
        p10(u3.new(u9));
    end));
end;

function u3.SendToServer(p11, ...) -- Line: 41
    p11.instance:FireServer(unpack({ ... }));
end;

function u3.Connect(p12, p13) -- Line: 45
    return p12.instance.OnClientEvent:Connect(p13);
end;

return {
    default = u3
};