-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local v2 = u1.import(script, script.Parent.Parent, "internal");
local findOrCreateRemote = v2.findOrCreateRemote;
local IS_SERVER = v2.IS_SERVER;
local default = u1.import(script, script.Parent, "MiddlewareFunction").default;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 11, Name: __tostring
        return "ServerFunction";
    end,

    __index = default
});
u3.__index = u3;

function u3.new(...) -- Line: 17
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5, p6, p7) -- Line: 21
    -- upvalues: default (copy), findOrCreateRemote (copy), u3 (ref), IS_SERVER (copy)
    default.constructor(p5, p7 == nil and {} or p7);
    p5.instance = findOrCreateRemote("RemoteFunction", p6, function(p8) -- Line: 26
        -- upvalues: u3 (ref)
        p8.OnServerInvoke = u3.DefaultFunctionHook;
    end);
    assert(IS_SERVER, "Cannot create a Net.ServerFunction on the Client!");
end;

function u3.GetInstance(p9) -- Line: 32
    return p9.instance;
end;

function u3.SetCallback(u10, u11) -- Line: 35
    -- upvalues: u1 (copy)
    function u10.instance.OnServerInvoke(p12, ...) -- Line: 36
        -- upvalues: u10 (copy), u11 (copy), u1 (ref)
        local v13 = { ... };
        local v14 = u10:_processMiddleware(u11);

        if v14 ~= nil then
            v14 = v14(p12, unpack(v13));
        end;

        if u1.Promise.is(v14) then
            warn("[rbx-net] WARNING: Promises should be used with an AsyncFunction!");
            local v15, v16 = v14:await();

            if v15 then
                return v16;
            end;

            error(v16);
        end;

        return v14;
    end;
end;

function u3.DefaultFunctionHook() -- Line: 55
    return nil;
end;

return {
    default = u3
};