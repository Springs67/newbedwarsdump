-- Decompiled with Potassium's decompiler.

local u1 = _G[script];
local v2 = u1.import(script, script.Parent.Parent, "internal");
local getRemoteOrThrow = v2.getRemoteOrThrow;
local IS_SERVER = v2.IS_SERVER;
local waitForRemote = v2.waitForRemote;
local u3 = setmetatable({}, {
    __tostring = function() -- Line: 10, Name: __tostring
        return "ClientFunction";
    end
});
u3.__index = u3;

function u3.new(...) -- Line: 15
    -- upvalues: u3 (ref)
    local v4 = setmetatable({}, u3);

    return v4:constructor(...) or v4;
end;

function u3.constructor(p5, p6) -- Line: 19
    -- upvalues: getRemoteOrThrow (copy), IS_SERVER (copy)
    p5.instance = getRemoteOrThrow("RemoteFunction", p6);
    assert(not IS_SERVER, "Cannot create a Net.ClientFunction on the Server!");
end;

function u3.Wait(p7, u8) -- Line: 24
    -- upvalues: u1 (copy), waitForRemote (copy), u3 (ref)
    return u1.Promise.defer(u1.async(function(p9) -- Line: 25
        -- upvalues: u1 (ref), waitForRemote (ref), u8 (copy), u3 (ref)
        u1.await(waitForRemote("RemoteFunction", u8, 60));
        p9(u3.new(u8));
    end));
end;

function u3.CallServer(p10, ...) -- Line: 30
    return p10.instance:InvokeServer(unpack({ ... }));
end;

u3.CallServerAsync = u1.async(function(u11, ...) -- Line: 34
    -- upvalues: u1 (copy)
    local u12 = { ... };

    return u1.Promise.defer(function(p13) -- Line: 36
        -- upvalues: u11 (copy), u12 (copy)
        p13((u11.instance:InvokeServer(unpack(u12))));
    end);
end);

return {
    default = u3
};