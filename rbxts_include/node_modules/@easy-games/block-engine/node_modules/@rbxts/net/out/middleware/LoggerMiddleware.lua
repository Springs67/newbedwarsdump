-- Decompiled with Potassium's decompiler.

local RunService = game:GetService("RunService");
local HttpService = game:GetService("HttpService");
local u1 = RunService:IsStudio();

local function defaultLogger(p2, p3) -- Line: 5
    -- upvalues: u1 (copy), HttpService (copy)
    if u1 then
        local v4 = {};

        for _, v in ipairs(p3) do
            table.insert(v4, HttpService:JSONEncode(v));
        end;

        print("[rbx-net] Called", p2, "with arguments", "[ " .. table.concat(v4, ", ") .. " ]");
    end;
end;

return function(p5) -- Line: 15
    -- upvalues: defaultLogger (copy)
    local u6 = p5 or {};
    local u7 = u6.Logger or defaultLogger;

    return function(u8, p9) -- Line: 19
        -- upvalues: u6 (ref), u7 (copy)
        local u10 = u6.Name or p9:GetInstance().Name;

        return function(p11, ...) -- Line: 21
            -- upvalues: u7 (ref), u10 (copy), u8 (copy)
            u7(u10, { ... });

            return u8(p11, ...);
        end;
    end;
end;