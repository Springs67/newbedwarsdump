-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local SyncEvent = v1.import(script, v1.getModule(script, "@easy-games", "sync-event").out).SyncEvent;
local ClientBlockDamageEffectEvent = v1.import(script, script.Parent, "break", "client-block-damage-effect-event").ClientBlockDamageEffectEvent;
local ClientBlockDamageEvent = v1.import(script, script.Parent, "break", "client-block-damage-event").ClientBlockDamageEvent;
local ClientBeforeBlockHighlightEvent = v1.import(script, script.Parent, "highlight", "event", "before-block-highlight-event").ClientBeforeBlockHighlightEvent;
local ClientBlockPlaceEvent = v1.import(script, script.Parent, "placement", "client-block-place-event").ClientBlockPlaceEvent;

return {
    BlockEngineClientEvents = {
        PlaceBlock = SyncEvent.new(ClientBlockPlaceEvent),
        DamageBlock = SyncEvent.new(ClientBlockDamageEvent),
        DamageBlockEffect = SyncEvent.new(ClientBlockDamageEffectEvent),
        BeforeHighlightBlock = SyncEvent.new(ClientBeforeBlockHighlightEvent)
    }
};