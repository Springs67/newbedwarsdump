-- Decompiled with Potassium's decompiler.

local BLOCK_SIZE = _G[script].import(script, script.Parent.Parent, "block", "block-constants").BLOCK_SIZE;
local u12 = {
    convertToBlockPosition = function(p1) -- Line: 8, Name: convertToBlockPosition
        -- upvalues: BLOCK_SIZE (copy)
        local v2 = p1 / BLOCK_SIZE;
        local v3 = math.round(v2.X);
        local v4 = math.round(v2.Y);
        local v5 = math.round(v2.Z);

        return Vector3.new(v3, v4, v5);
    end,

    convertToWorldPosition = function(p6) -- Line: 14, Name: convertToWorldPosition
        -- upvalues: BLOCK_SIZE (copy)
        return p6 * BLOCK_SIZE;
    end,

    isEqual = function(p7, p8) -- Line: 18, Name: isEqual
        local v9;

        if p7.X == p8.X and p7.Y == p8.Y then
            v9 = p7.Z == p8.Z;
        else
            v9 = false;
        end;

        return v9;
    end,

    getAdjacentBlockPosition = function(p10, p11) -- Line: 22, Name: getAdjacentBlockPosition
        return p10 + Vector3.FromNormalId(p11);
    end
};

function u12.getAllAdjacentBlockPositions(p13) -- Line: 28
    -- upvalues: u12 (copy)
    local v14 = {};

    for _, v in Enum.NormalId:GetEnumItems() do
        local v15 = u12.getAdjacentBlockPosition(p13, v);
        table.insert(v14, v15);
    end;

    return v14;
end;

return {
    PositionUtil = u12
};