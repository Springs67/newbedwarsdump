-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local SyncEvent = v1.import(script, v1.getModule(script, "@easy-games", "sync-event").out).SyncEvent;
local DestroyBlockSyncEvent = v1.import(script, script.Parent, "sync-events", "destroy-block-event").DestroyBlockSyncEvent;

return {
    BlockEngineSharedSyncEvents = {
        DestroyBlock = SyncEvent.new(DestroyBlockSyncEvent)
    }
};