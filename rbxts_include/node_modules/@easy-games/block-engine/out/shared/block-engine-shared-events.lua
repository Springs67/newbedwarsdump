local v1 = _G[script]
local v2 = v1.import(script, v1.getModule(script, "@easy-games", "sync-event").out).SyncEvent
local v3 = v1.import(script, script.Parent, "sync-events", "destroy-block-event").DestroyBlockSyncEvent
return {
    ["BlockEngineSharedSyncEvents"] = {
        ["DestroyBlock"] = v2.new(v3)
    }
}