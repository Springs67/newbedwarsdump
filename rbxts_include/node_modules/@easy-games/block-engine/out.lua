-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = {
    BlockEngineServerSyncEvents = v1.import(script, script, "server", "block-engine-server-events").BlockEngineServerSyncEvents
};

for i, v in v1.import(script, script, "server", "sync-events", "damage-block-event") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "server", "sync-events", "place-block-event") or {} do
    v2[i] = v;
end;

v2.BreakBlockDamageType = v1.import(script, script, "shared", "block", "block-meta").BreakBlockDamageType;

for i, v in v1.import(script, script, "shared", "handler", "block-handler") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "handler", "handlers", "greedy", "greedy-block-handler") or {} do
    v2[i] = v;
end;

for i, v in v1.import(script, script, "shared", "handler", "handlers", "prefab-block-handler") or {} do
    v2[i] = v;
end;

v2.BlockEngineRemotes = v1.import(script, script, "shared", "remotes").BlockEngineRemotes;
v2.BlockEngine = v1.import(script, script, "shared", "block-engine").BlockEngine.new();

return v2;