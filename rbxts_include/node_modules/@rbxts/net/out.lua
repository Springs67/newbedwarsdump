-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local v2 = v1.import(script, script, "server");
local v3 = v1.import(script, script, "client");
local default = v1.import(script, script, "definitions").default;

return {
    Client = v3,
    Server = v2,
    Definitions = default,
    DIST = "TS",
    VERSION = "3.0.10",
    Middleware = v1.import(script, script, "middleware").NetMiddleware,

    CreateDefinitions = function(p4, p5) -- Line: 62, Name: CreateDefinitions
        -- upvalues: default (copy)
        return default.Create(p4, p5);
    end
};