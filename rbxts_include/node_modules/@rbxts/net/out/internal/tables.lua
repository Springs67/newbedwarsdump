-- Decompiled with Potassium's decompiler.

local function isMixed(p1) -- Line: 1
    -- upvalues: isMixed (copy)
    local v2 = type(p1) == "table";
    assert(v2);
    local v3 = nil;
    local v4 = false;

    for i, v in next, p1 do
        if v3 and v3 ~= type(i) then
            return true;
        end;

        v3 = type(i);

        if type(v) == "table" then
            v4 = v4 and isMixed(v) and (getmetatable(v) and true or false);
        end;
    end;

    return v4;
end;

return {
    mixed = isMixed
};