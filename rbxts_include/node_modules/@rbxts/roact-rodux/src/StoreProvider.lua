-- Decompiled with Potassium's decompiler.

local src = require(script.Parent.Parent.Parent.roact.src);
local StoreContext = require(script.Parent.StoreContext);
local v1 = src.Component:extend("StoreProvider");

function v1.validateProps(p2) -- Line: 8
    if p2.store == nil then
        return false, "Error initializing StoreProvider. Expected a `store` prop to be a Rodux store.";
    end;

    return true;
end;

function v1.init(p3, p4) -- Line: 16
    p3.store = p4.store;
end;

function v1.render(p5) -- Line: 20
    -- upvalues: src (copy), StoreContext (copy)
    return src.createElement(StoreContext.Provider, {
        value = p5.store
    }, src.oneChild(p5.props[src.Children]));
end;

return v1;