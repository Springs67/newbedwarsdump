-- Decompiled with Potassium's decompiler.

return function(u1) -- Line: 1, Name: createUseValue
    return function(p2) -- Line: 2
        -- upvalues: u1 (copy)
        local v3 = u1;
        v3.hookCounter = v3.hookCounter + 1;
        local hookCounter = u1.hookCounter;

        if u1.values == nil then
            u1.values = {};
        end;

        if u1.values[hookCounter] == nil then
            u1.values[hookCounter] = {
                value = p2
            };
        end;

        return u1.values[hookCounter];
    end;
end;