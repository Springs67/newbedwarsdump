-- Decompiled with Potassium's decompiler.

return function(p1, u2, u3) -- Line: 1, Name: createUseContext
    local u4 = setmetatable({}, {
        __index = p1
    });

    return function(p5) -- Line: 9
        -- upvalues: u4 (copy), u3 (copy), u2 (copy)
        p5.Consumer.init(u4);
        local contextEntry = u4.contextEntry;
        local v6, u7 = u3(contextEntry.value);
        u2(function() -- Line: 15
            -- upvalues: contextEntry (copy), u7 (copy)
            return contextEntry.onUpdate:subscribe(u7);
        end, {});

        return v6;
    end;
end;