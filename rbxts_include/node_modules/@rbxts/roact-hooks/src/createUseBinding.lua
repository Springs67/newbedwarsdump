-- Decompiled with Potassium's decompiler.

return function(u1, u2) -- Line: 1, Name: createUseBinding
    return function(p3) -- Line: 2
        -- upvalues: u2 (copy), u1 (copy)
        local value = u2({ u1.createBinding(p3) }).value;

        return unpack(value);
    end;
end;