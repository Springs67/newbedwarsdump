-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local copy = require(Parent.copy);
local table = require(Parent.Parent.t).table;

return function(p1, p2) -- Line: 9, Name: removeKey
    -- upvalues: table (copy), copy (copy)
    assert(table(p1));
    local v3 = copy(p1);
    v3[p2] = nil;

    return v3;
end;