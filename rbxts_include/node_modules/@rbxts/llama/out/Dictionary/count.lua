-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);

local function alwaysTrue() -- Line: 6
    return true;
end;

local u1 = t.tuple(t.table, t.optional(t.callback));

return function(p2, p3) -- Line: 12, Name: count
    -- upvalues: u1 (copy), alwaysTrue (copy)
    assert(u1(p2, p3));
    local v4 = p3 or alwaysTrue;
    local v5 = 0;

    for i, v in pairs(p2) do
        if v4(v, i) then
            v5 = v5 + 1;
        end;
    end;

    return v5;
end;