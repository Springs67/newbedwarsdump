-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local copyDeep = require(Parent.copyDeep);
local Parent2 = Parent.Parent;
local None = require(script.Parent.Parent.None);
local table = require(Parent2.t).table;

local function mergeDeep(...) -- Line: 10
    -- upvalues: table (copy), None (copy), copyDeep (copy), mergeDeep (copy)
    local v1 = {};

    for i = 1, select("#", ...) do
        local v2 = select(i, ...);

        if v2 ~= nil then
            assert(table(v2));

            for i2, v in pairs(v2) do
                if v == None then
                    v1[i2] = nil;
                elseif type(v) == "table" then
                    if v1[i2] == nil or type(v1[i2]) ~= "table" then
                        v1[i2] = copyDeep(v);
                    else
                        v1[i2] = mergeDeep(v1[i2], v);
                    end;
                else
                    v1[i2] = v;
                end;
            end;
        end;
    end;

    return v1;
end;

return mergeDeep;