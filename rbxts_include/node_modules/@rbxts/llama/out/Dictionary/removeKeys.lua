-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local copy = require(Parent.copy);
local table = require(Parent.Parent.t).table;

return function(p1, ...) -- Line: 9, Name: removeKeys
    -- upvalues: table (copy), copy (copy)
    assert(table(p1));
    local v2 = copy(p1);

    for i = 1, select("#", ...) do
        v2[select(i, ...)] = nil;
    end;

    return v2;
end;