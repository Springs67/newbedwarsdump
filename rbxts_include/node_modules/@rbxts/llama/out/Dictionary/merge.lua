-- Decompiled with Potassium's decompiler.

local Parent = script.Parent.Parent;
local None = require(Parent.None);
local table = require(Parent.t).table;

return function(...) -- Line: 9, Name: merge
    -- upvalues: table (copy), None (copy)
    local v1 = {};

    for i = 1, select("#", ...) do
        local v2 = select(i, ...);

        if v2 ~= nil then
            assert(table(v2));

            for i2, v in pairs(v2) do
                if v == None then
                    v1[i2] = nil;
                else
                    v1[i2] = v;
                end;
            end;
        end;
    end;

    return v1;
end;