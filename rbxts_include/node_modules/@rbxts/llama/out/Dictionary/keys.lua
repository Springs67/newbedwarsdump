-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1) -- Line: 8, Name: keys
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = {};
    local v3 = 1;

    for i, _ in pairs(p1) do
        v2[v3] = i;
        v3 = v3 + 1;
    end;

    return v2;
end;