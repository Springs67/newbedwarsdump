-- Decompiled with Potassium's decompiler.

local Parent = script.Parent.Parent;
local t = require(Parent.t);
local toSet = require(Parent.List.toSet);
local table = t.table;

return function(p1, ...) -- Line: 11, Name: removeValues
    -- upvalues: table (copy), toSet (copy)
    assert(table(p1));
    local v2 = toSet({ ... });
    local v3 = {};

    for i, v in pairs(p1) do
        if not v2[v] then
            v3[i] = v;
        end;
    end;

    return v3;
end;