-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.table);

return function(p2, p3) -- Line: 9, Name: fromLists
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));
    local v4 = #p2;
    assert(v4 == #p3, "lists must be same size");
    local v5 = {};

    for i = 1, v4 do
        v5[p2[i]] = p3[i];
    end;

    return v5;
end;