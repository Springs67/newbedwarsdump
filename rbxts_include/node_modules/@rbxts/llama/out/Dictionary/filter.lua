-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.callback);

return function(p2, p3) -- Line: 8, Name: filter
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));
    local v4 = {};

    for i, v in pairs(p2) do
        if p3(v, i) then
            v4[i] = v;
        end;
    end;

    return v4;
end;