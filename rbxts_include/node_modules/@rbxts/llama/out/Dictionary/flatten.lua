-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.optional(t.intersection(t.integer, t.numberMin(0))));

local function flatten(p2, p3) -- Line: 8
    -- upvalues: u1 (copy), flatten (copy)
    assert(u1(p2, p3));
    local v4 = {};

    for i, v in pairs(p2) do
        if type(v) == "table" and (not p3 or p3 > 0) then
            local v5;

            if p3 then
                v5 = p3 - 1;
            else
                v5 = p3;
            end;

            local v6 = flatten(v, v5);

            for i2, v2 in pairs(v4) do
                v6[i2] = v2;
            end;

            v4 = v6;
        else
            v4[i] = v;
        end;
    end;

    return v4;
end;

return flatten;