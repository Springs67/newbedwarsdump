-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1) -- Line: 8, Name: flip
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = {};

    for i, v in pairs(p1) do
        v2[v] = i;
    end;

    return v2;
end;