-- Decompiled with Potassium's decompiler.

local Parent = script.Parent.Parent;
local None = require(Parent.None);
local table = require(Parent.t).table;

return function(...) -- Line: 9, Name: concat
    -- upvalues: table (copy), None (copy)
    local v1 = {};
    local v2 = 1;

    for i = 1, select("#", ...) do
        local v3 = select(i, ...);

        if v3 ~= nil then
            assert(table(v3));

            for i2 = 1, #v3 do
                if v3[i2] ~= None then
                    v1[v2] = v3[i2];
                    v2 = v2 + 1;
                end;
            end;
        end;
    end;

    return v1;
end;