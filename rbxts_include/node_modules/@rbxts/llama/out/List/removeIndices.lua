-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local table = t.table;
local integer = t.integer;

return function(p1, ...) -- Line: 9, Name: removeIndices
    -- upvalues: table (copy), integer (copy)
    assert(table(p1));
    local v2 = #p1;
    local v3 = {};

    for i = 1, select("#", ...) do
        local v4 = select(i, ...);
        assert(integer(v4));

        if v4 < 1 then
            v4 = v2 + v4;
        end;

        local v5;

        if v4 > 0 then
            v5 = v4 <= v2;
        else
            v5 = false;
        end;

        assert(v5, string.format("index %d out of bounds of list of length %d", v4, v2));
        v3[v4] = true;
    end;

    local v6 = {};
    local v7 = 1;

    for i = 1, v2 do
        if not v3[i] then
            v6[v7] = p1[i];
            v7 = v7 + 1;
        end;
    end;

    return v6;
end;