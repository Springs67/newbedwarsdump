-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.callback);

return function(p2, p3) -- Line: 8, Name: some
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));

    for i = 1, #p2 do
        if p3(p2[i], i) then
            return true;
        end;
    end;

    return false;
end;