-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local toSet = require(Parent.toSet);
local table = require(Parent.Parent.t).table;

return function(p1, ...) -- Line: 9, Name: removeValue
    -- upvalues: table (copy), toSet (copy)
    assert(table(p1));
    local v2 = toSet({ ... });
    local v3 = {};
    local v4 = 1;

    for i = 1, #p1 do
        if not v2[p1[i]] then
            v3[v4] = p1[i];
            v4 = v4 + 1;
        end;
    end;

    return v3;
end;