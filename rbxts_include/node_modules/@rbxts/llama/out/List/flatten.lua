-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.optional(t.intersection(t.integer, t.numberMin(0))));

local function flatten(p2, p3) -- Line: 8
    -- upvalues: u1 (copy), flatten (copy)
    assert(u1(p2));
    local v4 = {};
    local v5 = 1;

    for i = 1, #p2 do
        if type(p2[i]) == "table" and (not p3 or p3 > 0) then
            local v6;

            if p3 then
                v6 = p3 - 1;
            else
                v6 = p3;
            end;

            local v7 = flatten(p2[i], v6);

            for i2 = 1, #v7 do
                v4[v5] = v7[i2];
                v5 = v5 + 1;
            end;
        else
            v4[v5] = p2[i];
            v5 = v5 + 1;
        end;
    end;

    return v4;
end;

return flatten;