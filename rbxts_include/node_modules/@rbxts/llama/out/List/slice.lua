-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local v1 = t.optional(t.integer);
local u2 = t.tuple(t.table, v1, v1);

return function(p3, p4, p5) -- Line: 9, Name: slice
    -- upvalues: u2 (copy)
    assert(u2(p3, p4, p5));
    local v6 = #p3;
    local v7 = p4 or 1;
    local v8 = p5 or v6;

    if v7 < 1 then
        v7 = v6 + v7;
    end;

    if v8 < 1 then
        v8 = v6 + v8;
    end;

    local v9;

    if v7 > 0 then
        v9 = v7 <= v6 + 1;
    else
        v9 = false;
    end;

    assert(v9, string.format("index %d out of bounds of list of length %d", v7, v6));
    local v10;

    if v8 > 0 then
        v10 = v8 <= v6 + 1;
    else
        v10 = false;
    end;

    assert(v10, string.format("index %d out of bounds of list of length %d", v8, v6));
    assert(v7 <= v8, string.format("start index %d cannot be greater than end index %d", v7, v8));
    local v11 = {};
    local v12 = 1;

    for i = v7, v8 do
        v11[v12] = p3[i];
        v12 = v12 + 1;
    end;

    return v11;
end;