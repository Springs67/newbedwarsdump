-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1) -- Line: 8, Name: reverse
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = #p1;
    local v3 = v2 + 1;
    local v4 = {};

    for i = 1, v2 do
        v4[i] = p1[v3 - i];
    end;

    return v4;
end;