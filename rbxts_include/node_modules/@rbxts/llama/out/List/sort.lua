-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local copy = require(Parent.copy);
local t = require(Parent.Parent.t);
local u1 = t.tuple(t.table, t.optional(t.callback));

return function(p2, p3) -- Line: 9, Name: sort
    -- upvalues: u1 (copy), copy (copy)
    assert(u1(p2, p3));
    local v4 = copy(p2);
    table.sort(v4, p3);

    return v4;
end;