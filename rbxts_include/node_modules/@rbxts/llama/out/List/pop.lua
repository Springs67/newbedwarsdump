-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.optional(t.intersection(t.integer, t.numberMin(0))));

return function(p2, p3) -- Line: 8, Name: pop
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));
    local v4 = #p2;
    local v5 = p3 or 1;
    local v6;

    if v5 > 0 then
        v6 = v5 <= v4 + 1;
    else
        v6 = false;
    end;

    assert(v6, string.format("index %d out of bounds of list of length %d", v5, v4));
    local v7 = {};

    for i = 1, #p2 - v5 do
        v7[i] = p2[i];
    end;

    return v7;
end;