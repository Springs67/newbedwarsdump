-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local copyDeep = require(Parent.copyDeep);
local Parent2 = Parent.Parent;
local None = require(Parent2.None);
local table = require(Parent2.t).table;

return function(...) -- Line: 10, Name: concatDeep
    -- upvalues: table (copy), None (copy), copyDeep (copy)
    local v1 = 1;
    local v2 = {};

    for i = 1, select("#", ...) do
        local v3 = select(i, ...);

        if v3 ~= nil then
            assert(table(v3));

            for i2 = 1, #v3 do
                if v3[i2] ~= None then
                    if type(v3[i2]) == "table" then
                        v2[v1] = copyDeep(v3[i2]);
                    else
                        v2[v1] = v3[i2];
                    end;

                    v1 = v1 + 1;
                end;
            end;
        end;
    end;

    return v2;
end;