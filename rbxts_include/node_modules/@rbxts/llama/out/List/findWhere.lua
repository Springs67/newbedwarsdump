-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.callback, t.optional(t.integer));

return function(p2, p3, p4) -- Line: 8, Name: findWhere
    -- upvalues: u1 (copy)
    assert(u1(p2, p3, p4));
    local v5 = #p2;
    local v6 = p4 or 1;

    if v6 < 1 then
        v6 = v5 + v6;
    end;

    local v7;

    if v6 > 0 then
        v7 = v6 <= v5 + 1;
    else
        v7 = false;
    end;

    assert(v7, string.format("index %d out of bounds of list of length %d", v6, v5));

    for i = v6, v5 do
        if p3(p2[i], i) then
            return i;
        end;
    end;

    return nil;
end;