-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1, ...) -- Line: 8, Name: push
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = #p1;
    local v3 = {};

    for i = 1, v2 do
        v3[i] = p1[i];
    end;

    for i = 1, select("#", ...) do
        v3[v2 + i] = select(i, ...);
    end;

    return v3;
end;