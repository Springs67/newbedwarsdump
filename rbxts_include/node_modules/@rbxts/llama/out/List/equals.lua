-- Decompiled with Potassium's decompiler.

local equalObjects = require(script.Parent.Parent.equalObjects);

local function compare(p1, p2) -- Line: 6
    if type(p1) ~= "table" or type(p2) ~= "table" then
        return p1 == p2;
    end;

    if #p1 ~= #p2 then
        return false;
    end;

    for i = 1, #p1 do
        if p1[i] ~= p2[i] then
            return false;
        end;
    end;

    return true;
end;

return function(...) -- Line: 26, Name: equals
    -- upvalues: equalObjects (copy), compare (copy)
    if equalObjects(...) then
        return true;
    end;

    local v3 = select("#", ...);
    local v4 = select(1, ...);

    for i = 2, v3 do
        if not compare(v4, (select(i, ...))) then
            return false;
        end;
    end;

    return true;
end;