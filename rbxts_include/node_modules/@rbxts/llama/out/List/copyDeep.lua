-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

local function copyDeep(p1) -- Line: 8
    -- upvalues: table (copy), copyDeep (copy)
    assert(table(p1));
    local v2 = {};

    for i = 1, #p1 do
        if type(p1[i]) == "table" then
            v2[i] = copyDeep(p1[i]);
        else
            v2[i] = p1[i];
        end;
    end;

    return v2;
end;

return copyDeep;