-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1, ...) -- Line: 8, Name: unshift
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = select("#", ...);
    local v3 = {};

    for i = 1, v2 do
        v3[i] = select(i, ...);
    end;

    for i = 1, #p1 do
        v3[v2 + i] = p1[i];
    end;

    return v3;
end;