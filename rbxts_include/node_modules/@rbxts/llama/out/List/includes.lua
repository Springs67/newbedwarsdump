-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1, p2) -- Line: 8, Name: includes
    -- upvalues: table (copy)
    assert(table(p1));

    for i = 1, #p1 do
        if p1[i] == p2 then
            return true;
        end;
    end;

    return false;
end;