-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.intersection(t.integer, t.numberMin(0)), t.any);

return function(p2, p3) -- Line: 8, Name: recur
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));
    local v4 = {};

    for i = 1, p2 do
        v4[i] = p3;
    end;

    return v4;
end;