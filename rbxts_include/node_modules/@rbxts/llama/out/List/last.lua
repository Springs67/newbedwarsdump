-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1) -- Line: 8, Name: last
    -- upvalues: table (copy)
    assert(table(p1));

    return p1[#p1];
end;