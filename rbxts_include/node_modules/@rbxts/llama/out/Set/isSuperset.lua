-- Decompiled with Potassium's decompiler.

local Parent = script.Parent;
local isSubset = require(Parent.isSubset);
local t = require(Parent.Parent.t);
local u1 = t.tuple(t.table, t.table);

return function(p2, p3) -- Line: 10, Name: isSuperset
    -- upvalues: u1 (copy), isSubset (copy)
    assert(u1(p2, p3));

    return isSubset(p3, p2);
end;