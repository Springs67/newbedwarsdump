-- Decompiled with Potassium's decompiler.

local t = require(script.Parent.Parent.t);
local u1 = t.tuple(t.table, t.table);

return function(p2, p3) -- Line: 9, Name: isSubset
    -- upvalues: u1 (copy)
    assert(u1(p2, p3));

    for i, v in pairs(p2) do
        if p3[i] ~= v then
            return false;
        end;
    end;

    return true;
end;