-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1, ...) -- Line: 8, Name: subtract
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = {};

    for i, _ in pairs(p1) do
        v2[i] = true;
    end;

    for i = 1, select("#", ...) do
        v2[select(i, ...)] = nil;
    end;

    return v2;
end;