-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(...) -- Line: 8, Name: union
    -- upvalues: table (copy)
    local v1 = {};

    for i = 1, select("#", ...) do
        local v2 = select(i, ...);
        assert(table(v2));

        for i2, _ in pairs(v2) do
            v1[i2] = true;
        end;
    end;

    return v1;
end;