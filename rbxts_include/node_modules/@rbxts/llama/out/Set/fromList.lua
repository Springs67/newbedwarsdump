-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.Parent.t).table;

return function(p1) -- Line: 8, Name: fromList
    -- upvalues: table (copy)
    assert(table(p1));
    local v2 = {};

    for i = 1, #p1 do
        v2[p1[i]] = true;
    end;

    return v2;
end;