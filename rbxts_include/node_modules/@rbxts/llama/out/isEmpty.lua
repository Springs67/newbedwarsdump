-- Decompiled with Potassium's decompiler.

local table = require(script.Parent.t).table;

return function(p1) -- Line: 6, Name: isEmpty
    -- upvalues: table (copy)
    assert(table(p1));

    return next(p1) == nil;
end;