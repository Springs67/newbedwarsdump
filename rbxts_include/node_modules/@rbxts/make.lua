-- Decompiled with Potassium's decompiler.

return function(p1, p2) -- Line: 18, Name: Make
    local Children = p2.Children;
    local Parent = p2.Parent;
    local v3 = Instance.new(p1);

    for i, v in pairs(p2) do
        if i ~= "Children" and i ~= "Parent" then
            local v4 = v3[i];

            if typeof(v4) == "RBXScriptSignal" then
                v4:Connect(v);
            else
                v3[i] = v;
            end;
        end;
    end;

    if Children then
        for _, v in ipairs(Children) do
            v.Parent = v3;
        end;
    end;

    v3.Parent = Parent;

    return v3;
end;