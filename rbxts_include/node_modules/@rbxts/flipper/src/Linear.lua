-- Decompiled with Potassium's decompiler.

local u1 = {};
u1.__index = u1;

function u1.new(p2, p3) -- Line: 4
    -- upvalues: u1 (copy)
    assert(p2, "Missing argument #1: targetValue");

    return setmetatable({
        _targetValue = p2,
        _velocity = (p3 or {}).velocity or 1
    }, u1);
end;

function u1.step(p4, p5, p6) -- Line: 15
    local value = p5.value;
    local _velocity = p4._velocity;
    local _targetValue = p4._targetValue;
    local v7 = p6 * _velocity;
    local v8 = math.abs(_targetValue - value) <= v7;
    local v9 = value + v7 * (value < _targetValue and 1 or -1);

    if v8 then
        v9 = p4._targetValue;
        _velocity = 0;
    end;

    return {
        complete = v8,
        value = v9,
        velocity = _velocity
    };
end;

return u1;