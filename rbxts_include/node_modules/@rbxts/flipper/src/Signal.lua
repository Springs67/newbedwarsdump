local u1 = {}
u1.__index = u1
function u1.new(p2, p3) --[[ Line: 4 ]]
    --[[
    Upvalues:
        [1] = u1
    --]]
    local v4 = u1
    return setmetatable({
        ["connected"] = true,
        ["signal"] = p2,
        ["_handler"] = p3
    }, v4)
end
function u1.disconnect(p5) --[[ Line: 12 ]]
    if p5.connected then
        p5.connected = false
        for v6, v7 in pairs(p5.signal._connections) do
            if v7 == p5 then
                table.remove(p5.signal._connections, v6)
                return
            end
        end
    end
end
local u8 = {}
u8.__index = u8
function u8.new() --[[ Line: 28 ]]
    --[[
    Upvalues:
        [1] = u8
    --]]
    local v9 = u8
    return setmetatable({
        ["_connections"] = {},
        ["_threads"] = {}
    }, v9)
end
function u8.fire(p10, ...) --[[ Line: 35 ]]
    for _, v11 in pairs(p10._connections) do
        v11._handler(...)
    end
    for _, v12 in pairs(p10._threads) do
        coroutine.resume(v12, ...)
    end
    p10._threads = {}
end
function u8.connect(p13, p14) --[[ Line: 47 ]]
    --[[
    Upvalues:
        [1] = u1
    --]]
    local v15 = u1.new(p13, p14)
    local v16 = p13._connections
    table.insert(v16, v15)
    return v15
end
function u8.wait(p17) --[[ Line: 53 ]]
    local v18 = p17._threads
    local v19 = coroutine.running
    table.insert(v18, v19())
    return coroutine.yield()
end
return u8