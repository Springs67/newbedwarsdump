-- Decompiled with Potassium's decompiler.

local Parent = script.Parent.Parent.Parent;
local src = require(Parent.roact.src);
local src2 = require(Parent.flipper.src);
local AssignedBinding = require(script.Parent.AssignedBinding);

return function(p1) -- Line: 7, Name: getBinding
    -- upvalues: src2 (copy), AssignedBinding (copy), src (copy)
    assert(p1, "Missing argument #1: motor");

    if not src2.isMotor(p1) then
        error("Provided value is not a motor!", 2);
    end;

    if p1[AssignedBinding] then
        return p1[AssignedBinding];
    end;

    local v2, v3 = src.createBinding(p1:getValue());
    p1:onStep(v3);
    p1[AssignedBinding] = v2;

    return v2;
end;