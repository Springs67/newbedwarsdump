-- Decompiled with Potassium's decompiler.

local useMotor = require(script.Parent.useMotor);
local getBinding = require(script.Parent.getBinding);

local function getInitialValue(p1) -- Line: 4
    -- upvalues: getInitialValue (copy)
    if p1.step then
        return p1._targetValue;
    end;

    local v2 = {};

    for i, v in pairs(p1) do
        v2[i] = getInitialValue(v);
    end;

    return v2;
end;

return function(p3, p4) -- Line: 16, Name: useGoal
    -- upvalues: useMotor (copy), getInitialValue (copy), getBinding (copy)
    local v5 = useMotor(p3, (getInitialValue(p4)));
    v5:setGoal(p4);

    return getBinding(v5), v5;
end;