-- Decompiled with Potassium's decompiler.

local src = require(script.Parent.Parent.Parent.flipper.src);

local function createMotor(p1) -- Line: 4
    -- upvalues: src (copy)
    local v2 = type(p1);

    if v2 == "number" then
        return src.SingleMotor.new(p1);
    end;

    if v2 == "table" then
        return src.GroupMotor.new(p1);
    end;

    error(("Invalid type for initialValue. Expected \"number\" or \"table\", got \"%s\""):format(v2));
end;

return function(p3, p4) -- Line: 15, Name: useMotor
    -- upvalues: createMotor (copy)
    return p3.useValue(createMotor(p4)).value;
end;