-- Decompiled with Potassium's decompiler.

local src = require(script.Parent.Parent.Parent.flipper.src);
local useGoal = require(script.Parent.useGoal);

return function(p1, p2, p3) -- Line: 6, Name: useSpring
    -- upvalues: useGoal (copy), src (copy)
    return useGoal(p1, src.Spring.new(p2, p3));
end;