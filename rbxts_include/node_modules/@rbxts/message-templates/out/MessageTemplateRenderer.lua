-- Decompiled with Potassium's decompiler.

local TemplateTokenKind = _G[script].import(script, script.Parent, "MessageTemplateToken").TemplateTokenKind;

return {
    MessageTemplateRenderer = {
        constructor = function(p1, p2) -- Line: 7, Name: constructor
            p1.tokens = p2;
        end,

        Render = function(p3, p4) -- Line: 10, Name: Render
            -- upvalues: TemplateTokenKind (copy)
            local v5 = "";

            for _, v in ipairs(p3.tokens) do
                local kind = v.kind;

                if kind == TemplateTokenKind.Property then
                    v5 = v5 .. p3:RenderPropertyToken(v, p4[v.propertyName]);
                elseif kind == TemplateTokenKind.Text then
                    v5 = v5 .. p3:RenderTextToken(v);
                end;
            end;

            return v5;
        end
    }
};