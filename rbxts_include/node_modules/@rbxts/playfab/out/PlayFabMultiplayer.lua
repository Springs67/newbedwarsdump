-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    CancelAllMatchmakingTicketsForPlayer = function(p1, u2, u3) -- Line: 17, Name: CancelAllMatchmakingTicketsForPlayer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 18
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CancelAllMatchmakingTicketsForPlayer", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    CancelAllServerBackfillTicketsForPlayer = function(p6, u7, u8) -- Line: 28, Name: CancelAllServerBackfillTicketsForPlayer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 29
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CancelAllServerBackfillTicketsForPlayer", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    CancelMatchmakingTicket = function(p11, u12, u13) -- Line: 39, Name: CancelMatchmakingTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 40
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CancelMatchmakingTicket", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    CancelServerBackfillTicket = function(p16, u17, u18) -- Line: 50, Name: CancelServerBackfillTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 51
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CancelServerBackfillTicket", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    CreateBuildAlias = function(p21, u22, u23) -- Line: 61, Name: CreateBuildAlias
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 62
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/CreateBuildAlias", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    CreateBuildWithCustomContainer = function(p26, u27, u28) -- Line: 72, Name: CreateBuildWithCustomContainer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 73
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/CreateBuildWithCustomContainer", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end,

    CreateBuildWithManagedContainer = function(p31, u32, u33) -- Line: 83, Name: CreateBuildWithManagedContainer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p34, p35) -- Line: 84
            -- upvalues: u32 (copy), IPlayFabHttps (ref), u33 (copy)
            if not u32 then
                p35("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/CreateBuildWithManagedContainer", u33 or {}, "X-EntityToken", u32, p34, p35);
        end);
    end,

    CreateBuildWithProcessBasedServer = function(p36, u37, u38) -- Line: 94, Name: CreateBuildWithProcessBasedServer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 95
            -- upvalues: u37 (copy), IPlayFabHttps (ref), u38 (copy)
            if not u37 then
                p40("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/CreateBuildWithProcessBasedServer", u38 or {}, "X-EntityToken", u37, p39, p40);
        end);
    end,

    CreateMatchmakingTicket = function(p41, u42, u43) -- Line: 105, Name: CreateMatchmakingTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p44, p45) -- Line: 106
            -- upvalues: u42 (copy), IPlayFabHttps (ref), u43 (copy)
            if not u42 then
                p45("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CreateMatchmakingTicket", u43 or {}, "X-EntityToken", u42, p44, p45);
        end);
    end,

    CreateRemoteUser = function(p46, u47, u48) -- Line: 116, Name: CreateRemoteUser
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p49, p50) -- Line: 117
            -- upvalues: u47 (copy), IPlayFabHttps (ref), u48 (copy)
            if not u47 then
                p50("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/CreateRemoteUser", u48 or {}, "X-EntityToken", u47, p49, p50);
        end);
    end,

    CreateServerBackfillTicket = function(p51, u52, u53) -- Line: 129, Name: CreateServerBackfillTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p54, p55) -- Line: 130
            -- upvalues: u52 (copy), IPlayFabHttps (ref), u53 (copy)
            if not u52 then
                p55("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CreateServerBackfillTicket", u53 or {}, "X-EntityToken", u52, p54, p55);
        end);
    end,

    CreateServerMatchmakingTicket = function(p56, u57, u58) -- Line: 141, Name: CreateServerMatchmakingTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 142
            -- upvalues: u57 (copy), IPlayFabHttps (ref), u58 (copy)
            if not u57 then
                p60("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/CreateServerMatchmakingTicket", u58 or {}, "X-EntityToken", u57, p59, p60);
        end);
    end,

    DeleteAsset = function(p61, u62, u63) -- Line: 152, Name: DeleteAsset
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p64, p65) -- Line: 153
            -- upvalues: u62 (copy), IPlayFabHttps (ref), u63 (copy)
            if not u62 then
                p65("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteAsset", u63 or {}, "X-EntityToken", u62, p64, p65);
        end);
    end,

    DeleteBuild = function(p66, u67, u68) -- Line: 163, Name: DeleteBuild
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p69, p70) -- Line: 164
            -- upvalues: u67 (copy), IPlayFabHttps (ref), u68 (copy)
            if not u67 then
                p70("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteBuild", u68 or {}, "X-EntityToken", u67, p69, p70);
        end);
    end,

    DeleteBuildAlias = function(p71, u72, u73) -- Line: 174, Name: DeleteBuildAlias
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p74, p75) -- Line: 175
            -- upvalues: u72 (copy), IPlayFabHttps (ref), u73 (copy)
            if not u72 then
                p75("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteBuildAlias", u73 or {}, "X-EntityToken", u72, p74, p75);
        end);
    end,

    DeleteBuildRegion = function(p76, u77, u78) -- Line: 185, Name: DeleteBuildRegion
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p79, p80) -- Line: 186
            -- upvalues: u77 (copy), IPlayFabHttps (ref), u78 (copy)
            if not u77 then
                p80("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteBuildRegion", u78 or {}, "X-EntityToken", u77, p79, p80);
        end);
    end,

    DeleteCertificate = function(p81, u82, u83) -- Line: 196, Name: DeleteCertificate
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p84, p85) -- Line: 197
            -- upvalues: u82 (copy), IPlayFabHttps (ref), u83 (copy)
            if not u82 then
                p85("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteCertificate", u83 or {}, "X-EntityToken", u82, p84, p85);
        end);
    end,

    DeleteContainerImageRepository = function(p86, u87, u88) -- Line: 207, Name: DeleteContainerImageRepository
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p89, p90) -- Line: 208
            -- upvalues: u87 (copy), IPlayFabHttps (ref), u88 (copy)
            if not u87 then
                p90("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteContainerImageRepository", u88 or {}, "X-EntityToken", u87, p89, p90);
        end);
    end,

    DeleteRemoteUser = function(p91, u92, u93) -- Line: 218, Name: DeleteRemoteUser
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p94, p95) -- Line: 219
            -- upvalues: u92 (copy), IPlayFabHttps (ref), u93 (copy)
            if not u92 then
                p95("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/DeleteRemoteUser", u93 or {}, "X-EntityToken", u92, p94, p95);
        end);
    end,

    EnableMultiplayerServersForTitle = function(p96, u97, u98) -- Line: 229, Name: EnableMultiplayerServersForTitle
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p99, p100) -- Line: 230
            -- upvalues: u97 (copy), IPlayFabHttps (ref), u98 (copy)
            if not u97 then
                p100("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/EnableMultiplayerServersForTitle", u98 or {}, "X-EntityToken", u97, p99, p100);
        end);
    end,

    GetAssetUploadUrl = function(p101, u102, u103) -- Line: 240, Name: GetAssetUploadUrl
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p104, p105) -- Line: 241
            -- upvalues: u102 (copy), IPlayFabHttps (ref), u103 (copy)
            if not u102 then
                p105("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetAssetUploadUrl", u103 or {}, "X-EntityToken", u102, p104, p105);
        end);
    end,

    GetBuild = function(p106, u107, u108) -- Line: 251, Name: GetBuild
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p109, p110) -- Line: 252
            -- upvalues: u107 (copy), IPlayFabHttps (ref), u108 (copy)
            if not u107 then
                p110("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetBuild", u108 or {}, "X-EntityToken", u107, p109, p110);
        end);
    end,

    GetBuildAlias = function(p111, u112, u113) -- Line: 262, Name: GetBuildAlias
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p114, p115) -- Line: 263
            -- upvalues: u112 (copy), IPlayFabHttps (ref), u113 (copy)
            if not u112 then
                p115("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetBuildAlias", u113 or {}, "X-EntityToken", u112, p114, p115);
        end);
    end,

    GetContainerRegistryCredentials = function(p116, u117, u118) -- Line: 273, Name: GetContainerRegistryCredentials
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p119, p120) -- Line: 274
            -- upvalues: u117 (copy), IPlayFabHttps (ref), u118 (copy)
            if not u117 then
                p120("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetContainerRegistryCredentials", u118 or {}, "X-EntityToken", u117, p119, p120);
        end);
    end,

    GetMatch = function(p121, u122, u123) -- Line: 284, Name: GetMatch
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p124, p125) -- Line: 285
            -- upvalues: u122 (copy), IPlayFabHttps (ref), u123 (copy)
            if not u122 then
                p125("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/GetMatch", u123 or {}, "X-EntityToken", u122, p124, p125);
        end);
    end,

    GetMatchmakingQueue = function(p126, u127, u128) -- Line: 295, Name: GetMatchmakingQueue
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p129, p130) -- Line: 296
            -- upvalues: u127 (copy), IPlayFabHttps (ref), u128 (copy)
            if not u127 then
                p130("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/GetMatchmakingQueue", u128 or {}, "X-EntityToken", u127, p129, p130);
        end);
    end,

    GetMatchmakingTicket = function(p131, u132, u133) -- Line: 306, Name: GetMatchmakingTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p134, p135) -- Line: 307
            -- upvalues: u132 (copy), IPlayFabHttps (ref), u133 (copy)
            if not u132 then
                p135("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/GetMatchmakingTicket", u133 or {}, "X-EntityToken", u132, p134, p135);
        end);
    end,

    GetMultiplayerServerDetails = function(p136, u137, u138) -- Line: 317, Name: GetMultiplayerServerDetails
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p139, p140) -- Line: 318
            -- upvalues: u137 (copy), IPlayFabHttps (ref), u138 (copy)
            if not u137 then
                p140("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetMultiplayerServerDetails", u138 or {}, "X-EntityToken", u137, p139, p140);
        end);
    end,

    GetMultiplayerServerLogs = function(p141, u142, u143) -- Line: 328, Name: GetMultiplayerServerLogs
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p144, p145) -- Line: 329
            -- upvalues: u142 (copy), IPlayFabHttps (ref), u143 (copy)
            if not u142 then
                p145("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetMultiplayerServerLogs", u143 or {}, "X-EntityToken", u142, p144, p145);
        end);
    end,

    GetMultiplayerSessionLogsBySessionId = function(p146, u147, u148) -- Line: 339, Name: GetMultiplayerSessionLogsBySessionId
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p149, p150) -- Line: 340
            -- upvalues: u147 (copy), IPlayFabHttps (ref), u148 (copy)
            if not u147 then
                p150("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetMultiplayerSessionLogsBySessionId", u148 or {}, "X-EntityToken", u147, p149, p150);
        end);
    end,

    GetQueueStatistics = function(p151, u152, u153) -- Line: 350, Name: GetQueueStatistics
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p154, p155) -- Line: 351
            -- upvalues: u152 (copy), IPlayFabHttps (ref), u153 (copy)
            if not u152 then
                p155("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/GetQueueStatistics", u153 or {}, "X-EntityToken", u152, p154, p155);
        end);
    end,

    GetRemoteLoginEndpoint = function(p156, u157, u158) -- Line: 361, Name: GetRemoteLoginEndpoint
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p159, p160) -- Line: 362
            -- upvalues: u157 (copy), IPlayFabHttps (ref), u158 (copy)
            if not u157 then
                p160("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetRemoteLoginEndpoint", u158 or {}, "X-EntityToken", u157, p159, p160);
        end);
    end,

    GetServerBackfillTicket = function(p161, u162, u163) -- Line: 372, Name: GetServerBackfillTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p164, p165) -- Line: 373
            -- upvalues: u162 (copy), IPlayFabHttps (ref), u163 (copy)
            if not u162 then
                p165("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/GetServerBackfillTicket", u163 or {}, "X-EntityToken", u162, p164, p165);
        end);
    end,

    GetTitleEnabledForMultiplayerServersStatus = function(p166, u167, u168) -- Line: 383, Name: GetTitleEnabledForMultiplayerServersStatus
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p169, p170) -- Line: 384
            -- upvalues: u167 (copy), IPlayFabHttps (ref), u168 (copy)
            if not u167 then
                p170("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetTitleEnabledForMultiplayerServersStatus", u168 or {}, "X-EntityToken", u167, p169, p170);
        end);
    end,

    GetTitleMultiplayerServersQuotas = function(p171, u172, u173) -- Line: 394, Name: GetTitleMultiplayerServersQuotas
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p174, p175) -- Line: 395
            -- upvalues: u172 (copy), IPlayFabHttps (ref), u173 (copy)
            if not u172 then
                p175("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/GetTitleMultiplayerServersQuotas", u173 or {}, "X-EntityToken", u172, p174, p175);
        end);
    end,

    JoinMatchmakingTicket = function(p176, u177, u178) -- Line: 405, Name: JoinMatchmakingTicket
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p179, p180) -- Line: 406
            -- upvalues: u177 (copy), IPlayFabHttps (ref), u178 (copy)
            if not u177 then
                p180("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/JoinMatchmakingTicket", u178 or {}, "X-EntityToken", u177, p179, p180);
        end);
    end,

    ListArchivedMultiplayerServers = function(p181, u182, u183) -- Line: 416, Name: ListArchivedMultiplayerServers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p184, p185) -- Line: 417
            -- upvalues: u182 (copy), IPlayFabHttps (ref), u183 (copy)
            if not u182 then
                p185("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListArchivedMultiplayerServers", u183 or {}, "X-EntityToken", u182, p184, p185);
        end);
    end,

    ListAssetSummaries = function(p186, u187, u188) -- Line: 427, Name: ListAssetSummaries
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p189, p190) -- Line: 428
            -- upvalues: u187 (copy), IPlayFabHttps (ref), u188 (copy)
            if not u187 then
                p190("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListAssetSummaries", u188 or {}, "X-EntityToken", u187, p189, p190);
        end);
    end,

    ListBuildAliases = function(p191, u192, u193) -- Line: 439, Name: ListBuildAliases
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p194, p195) -- Line: 440
            -- upvalues: u192 (copy), IPlayFabHttps (ref), u193 (copy)
            if not u192 then
                p195("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListBuildAliases", u193 or {}, "X-EntityToken", u192, p194, p195);
        end);
    end,

    ListBuildSummaries = function(p196, u197, u198) -- Line: 451, Name: ListBuildSummaries
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p199, p200) -- Line: 452
            -- upvalues: u197 (copy), IPlayFabHttps (ref), u198 (copy)
            if not u197 then
                p200("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListBuildSummaries", u198 or {}, "X-EntityToken", u197, p199, p200);
        end);
    end,

    ListBuildSummariesV2 = function(p201, u202, u203) -- Line: 463, Name: ListBuildSummariesV2
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p204, p205) -- Line: 464
            -- upvalues: u202 (copy), IPlayFabHttps (ref), u203 (copy)
            if not u202 then
                p205("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListBuildSummariesV2", u203 or {}, "X-EntityToken", u202, p204, p205);
        end);
    end,

    ListCertificateSummaries = function(p206, u207, u208) -- Line: 474, Name: ListCertificateSummaries
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p209, p210) -- Line: 475
            -- upvalues: u207 (copy), IPlayFabHttps (ref), u208 (copy)
            if not u207 then
                p210("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListCertificateSummaries", u208 or {}, "X-EntityToken", u207, p209, p210);
        end);
    end,

    ListContainerImages = function(p211, u212, u213) -- Line: 485, Name: ListContainerImages
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p214, p215) -- Line: 486
            -- upvalues: u212 (copy), IPlayFabHttps (ref), u213 (copy)
            if not u212 then
                p215("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListContainerImages", u213 or {}, "X-EntityToken", u212, p214, p215);
        end);
    end,

    ListContainerImageTags = function(p216, u217, u218) -- Line: 496, Name: ListContainerImageTags
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p219, p220) -- Line: 497
            -- upvalues: u217 (copy), IPlayFabHttps (ref), u218 (copy)
            if not u217 then
                p220("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListContainerImageTags", u218 or {}, "X-EntityToken", u217, p219, p220);
        end);
    end,

    ListMatchmakingQueues = function(p221, u222, u223) -- Line: 507, Name: ListMatchmakingQueues
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p224, p225) -- Line: 508
            -- upvalues: u222 (copy), IPlayFabHttps (ref), u223 (copy)
            if not u222 then
                p225("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/ListMatchmakingQueues", u223 or {}, "X-EntityToken", u222, p224, p225);
        end);
    end,

    ListMatchmakingTicketsForPlayer = function(p226, u227, u228) -- Line: 518, Name: ListMatchmakingTicketsForPlayer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p229, p230) -- Line: 519
            -- upvalues: u227 (copy), IPlayFabHttps (ref), u228 (copy)
            if not u227 then
                p230("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/ListMatchmakingTicketsForPlayer", u228 or {}, "X-EntityToken", u227, p229, p230);
        end);
    end,

    ListMultiplayerServers = function(p231, u232, u233) -- Line: 529, Name: ListMultiplayerServers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p234, p235) -- Line: 530
            -- upvalues: u232 (copy), IPlayFabHttps (ref), u233 (copy)
            if not u232 then
                p235("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListMultiplayerServers", u233 or {}, "X-EntityToken", u232, p234, p235);
        end);
    end,

    ListPartyQosServers = function(p236, u237) -- Line: 540, Name: ListPartyQosServers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p238, p239) -- Line: 541
            -- upvalues: IPlayFabHttps (ref), u237 (copy)
            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListPartyQosServers", u237 or {}, nil, nil, p238, p239);
        end);
    end,

    ListQosServersForTitle = function(p240, u241, u242) -- Line: 551, Name: ListQosServersForTitle
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p243, p244) -- Line: 552
            -- upvalues: u241 (copy), IPlayFabHttps (ref), u242 (copy)
            if not u241 then
                p244("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListQosServersForTitle", u242 or {}, "X-EntityToken", u241, p243, p244);
        end);
    end,

    ListServerBackfillTicketsForPlayer = function(p245, u246, u247) -- Line: 562, Name: ListServerBackfillTicketsForPlayer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p248, p249) -- Line: 563
            -- upvalues: u246 (copy), IPlayFabHttps (ref), u247 (copy)
            if not u246 then
                p249("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/ListServerBackfillTicketsForPlayer", u247 or {}, "X-EntityToken", u246, p248, p249);
        end);
    end,

    ListVirtualMachineSummaries = function(p250, u251, u252) -- Line: 573, Name: ListVirtualMachineSummaries
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p253, p254) -- Line: 574
            -- upvalues: u251 (copy), IPlayFabHttps (ref), u252 (copy)
            if not u251 then
                p254("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ListVirtualMachineSummaries", u252 or {}, "X-EntityToken", u251, p253, p254);
        end);
    end,

    RemoveMatchmakingQueue = function(p255, u256, u257) -- Line: 584, Name: RemoveMatchmakingQueue
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p258, p259) -- Line: 585
            -- upvalues: u256 (copy), IPlayFabHttps (ref), u257 (copy)
            if not u256 then
                p259("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/RemoveMatchmakingQueue", u257 or {}, "X-EntityToken", u256, p258, p259);
        end);
    end,

    RequestMultiplayerServer = function(p260, u261, u262) -- Line: 596, Name: RequestMultiplayerServer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p263, p264) -- Line: 597
            -- upvalues: u261 (copy), IPlayFabHttps (ref), u262 (copy)
            if not u261 then
                p264("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/RequestMultiplayerServer", u262 or {}, "X-EntityToken", u261, p263, p264);
        end);
    end,

    RolloverContainerRegistryCredentials = function(p265, u266, u267) -- Line: 607, Name: RolloverContainerRegistryCredentials
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p268, p269) -- Line: 608
            -- upvalues: u266 (copy), IPlayFabHttps (ref), u267 (copy)
            if not u266 then
                p269("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/RolloverContainerRegistryCredentials", u267 or {}, "X-EntityToken", u266, p268, p269);
        end);
    end,

    SetMatchmakingQueue = function(p270, u271, u272) -- Line: 618, Name: SetMatchmakingQueue
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p273, p274) -- Line: 619
            -- upvalues: u271 (copy), IPlayFabHttps (ref), u272 (copy)
            if not u271 then
                p274("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Match/SetMatchmakingQueue", u272 or {}, "X-EntityToken", u271, p273, p274);
        end);
    end,

    ShutdownMultiplayerServer = function(p275, u276, u277) -- Line: 629, Name: ShutdownMultiplayerServer
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p278, p279) -- Line: 630
            -- upvalues: u276 (copy), IPlayFabHttps (ref), u277 (copy)
            if not u276 then
                p279("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/ShutdownMultiplayerServer", u277 or {}, "X-EntityToken", u276, p278, p279);
        end);
    end,

    UntagContainerImage = function(p280, u281, u282) -- Line: 640, Name: UntagContainerImage
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p283, p284) -- Line: 641
            -- upvalues: u281 (copy), IPlayFabHttps (ref), u282 (copy)
            if not u281 then
                p284("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/UntagContainerImage", u282 or {}, "X-EntityToken", u281, p283, p284);
        end);
    end,

    UpdateBuildAlias = function(p285, u286, u287) -- Line: 651, Name: UpdateBuildAlias
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p288, p289) -- Line: 652
            -- upvalues: u286 (copy), IPlayFabHttps (ref), u287 (copy)
            if not u286 then
                p289("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/UpdateBuildAlias", u287 or {}, "X-EntityToken", u286, p288, p289);
        end);
    end,

    UpdateBuildRegion = function(p290, u291, u292) -- Line: 662, Name: UpdateBuildRegion
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p293, p294) -- Line: 663
            -- upvalues: u291 (copy), IPlayFabHttps (ref), u292 (copy)
            if not u291 then
                p294("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/UpdateBuildRegion", u292 or {}, "X-EntityToken", u291, p293, p294);
        end);
    end,

    UpdateBuildRegions = function(p295, u296, u297) -- Line: 673, Name: UpdateBuildRegions
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p298, p299) -- Line: 674
            -- upvalues: u296 (copy), IPlayFabHttps (ref), u297 (copy)
            if not u296 then
                p299("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/UpdateBuildRegions", u297 or {}, "X-EntityToken", u296, p298, p299);
        end);
    end,

    UploadCertificate = function(p300, u301, u302) -- Line: 684, Name: UploadCertificate
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p303, p304) -- Line: 685
            -- upvalues: u301 (copy), IPlayFabHttps (ref), u302 (copy)
            if not u301 then
                p304("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/MultiplayerServer/UploadCertificate", u302 or {}, "X-EntityToken", u301, p303, p304);
        end);
    end
};