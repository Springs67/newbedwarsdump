-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    ExecuteEntityCloudScript = function(p1, u2, u3) -- Line: 18, Name: ExecuteEntityCloudScript
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 19
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/ExecuteEntityCloudScript", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    ExecuteFunction = function(p6, u7, u8) -- Line: 30, Name: ExecuteFunction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 31
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/ExecuteFunction", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    ListFunctions = function(p11, u12, u13) -- Line: 41, Name: ListFunctions
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 42
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/ListFunctions", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    ListHttpFunctions = function(p16, u17, u18) -- Line: 52, Name: ListHttpFunctions
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 53
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/ListHttpFunctions", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    ListQueuedFunctions = function(p21, u22, u23) -- Line: 63, Name: ListQueuedFunctions
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 64
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/ListQueuedFunctions", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    PostFunctionResultForEntityTriggeredAction = function(p26, u27, u28) -- Line: 74, Name: PostFunctionResultForEntityTriggeredAction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 75
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/PostFunctionResultForEntityTriggeredAction", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end,

    PostFunctionResultForFunctionExecution = function(p31, u32, u33) -- Line: 85, Name: PostFunctionResultForFunctionExecution
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p34, p35) -- Line: 86
            -- upvalues: u32 (copy), IPlayFabHttps (ref), u33 (copy)
            if not u32 then
                p35("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/PostFunctionResultForFunctionExecution", u33 or {}, "X-EntityToken", u32, p34, p35);
        end);
    end,

    PostFunctionResultForPlayerTriggeredAction = function(p36, u37, u38) -- Line: 96, Name: PostFunctionResultForPlayerTriggeredAction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 97
            -- upvalues: u37 (copy), IPlayFabHttps (ref), u38 (copy)
            if not u37 then
                p40("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/PostFunctionResultForPlayerTriggeredAction", u38 or {}, "X-EntityToken", u37, p39, p40);
        end);
    end,

    PostFunctionResultForScheduledTask = function(p41, u42, u43) -- Line: 107, Name: PostFunctionResultForScheduledTask
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p44, p45) -- Line: 108
            -- upvalues: u42 (copy), IPlayFabHttps (ref), u43 (copy)
            if not u42 then
                p45("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/PostFunctionResultForScheduledTask", u43 or {}, "X-EntityToken", u42, p44, p45);
        end);
    end,

    RegisterHttpFunction = function(p46, u47, u48) -- Line: 118, Name: RegisterHttpFunction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p49, p50) -- Line: 119
            -- upvalues: u47 (copy), IPlayFabHttps (ref), u48 (copy)
            if not u47 then
                p50("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/RegisterHttpFunction", u48 or {}, "X-EntityToken", u47, p49, p50);
        end);
    end,

    RegisterQueuedFunction = function(p51, u52, u53) -- Line: 129, Name: RegisterQueuedFunction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p54, p55) -- Line: 130
            -- upvalues: u52 (copy), IPlayFabHttps (ref), u53 (copy)
            if not u52 then
                p55("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/RegisterQueuedFunction", u53 or {}, "X-EntityToken", u52, p54, p55);
        end);
    end,

    UnregisterFunction = function(p56, u57, u58) -- Line: 140, Name: UnregisterFunction
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 141
            -- upvalues: u57 (copy), IPlayFabHttps (ref), u58 (copy)
            if not u57 then
                p60("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/CloudScript/UnregisterFunction", u58 or {}, "X-EntityToken", u57, p59, p60);
        end);
    end
};