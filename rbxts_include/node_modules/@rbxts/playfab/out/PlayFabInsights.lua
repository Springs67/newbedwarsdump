-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    GetDetails = function(p1, u2, u3) -- Line: 18, Name: GetDetails
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 19
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/GetDetails", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    GetLimits = function(p6, u7, u8) -- Line: 30, Name: GetLimits
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 31
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/GetLimits", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    GetOperationStatus = function(p11, u12, u13) -- Line: 41, Name: GetOperationStatus
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 42
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/GetOperationStatus", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    GetPendingOperations = function(p16, u17, u18) -- Line: 52, Name: GetPendingOperations
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 53
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/GetPendingOperations", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    SetPerformance = function(p21, u22, u23) -- Line: 63, Name: SetPerformance
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 64
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/SetPerformance", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    SetStorageRetention = function(p26, u27, u28) -- Line: 74, Name: SetStorageRetention
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 75
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Insights/SetStorageRetention", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end
};