-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);
local PlayFabSettings = require(script.Parent.PlayFabSettings);

return {
    settings = PlayFabSettings.settings,

    AddCharacterVirtualCurrency = function(p1, u2) -- Line: 17, Name: AddCharacterVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p3, p4) -- Line: 18
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u2 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p4("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddCharacterVirtualCurrency", u2 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p3, p4);
        end);
    end,

    AddFriend = function(p5, u6) -- Line: 31, Name: AddFriend
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p7, p8) -- Line: 32
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u6 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p8("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddFriend", u6 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p7, p8);
        end);
    end,

    AddGenericID = function(p9, u10) -- Line: 46, Name: AddGenericID
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p11, p12) -- Line: 47
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u10 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p12("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddGenericID", u10 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p11, p12);
        end);
    end,

    AddPlayerTag = function(p13, u14) -- Line: 59, Name: AddPlayerTag
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p15, p16) -- Line: 60
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u14 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p16("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddPlayerTag", u14 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p15, p16);
        end);
    end,

    AddSharedGroupMembers = function(p17, u18) -- Line: 75, Name: AddSharedGroupMembers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 76
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u18 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p20("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddSharedGroupMembers", u18 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p19, p20);
        end);
    end,

    AddUserVirtualCurrency = function(p21, u22) -- Line: 88, Name: AddUserVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p23, p24) -- Line: 89
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u22 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p24("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AddUserVirtualCurrency", u22 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p23, p24);
        end);
    end,

    AuthenticateSessionTicket = function(p25, u26) -- Line: 101, Name: AuthenticateSessionTicket
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p27, p28) -- Line: 102
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u26 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p28("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AuthenticateSessionTicket", u26 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p27, p28);
        end);
    end,

    AwardSteamAchievement = function(p29, u30) -- Line: 114, Name: AwardSteamAchievement
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p31, p32) -- Line: 115
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u30 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p32("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/AwardSteamAchievement", u30 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p31, p32);
        end);
    end,

    BanUsers = function(p33, u34) -- Line: 127, Name: BanUsers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p35, p36) -- Line: 128
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u34 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p36("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/BanUsers", u34 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p35, p36);
        end);
    end,

    ConsumeItem = function(p37, u38) -- Line: 140, Name: ConsumeItem
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 141
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u38 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p40("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/ConsumeItem", u38 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p39, p40);
        end);
    end,

    CreateSharedGroup = function(p41, u42) -- Line: 156, Name: CreateSharedGroup
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p43, p44) -- Line: 157
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u42 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p44("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/CreateSharedGroup", u42 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p43, p44);
        end);
    end,

    DeleteCharacterFromUser = function(p45, u46) -- Line: 169, Name: DeleteCharacterFromUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p47, p48) -- Line: 170
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u46 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p48("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/DeleteCharacterFromUser", u46 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p47, p48);
        end);
    end,

    DeletePlayer = function(p49, u50) -- Line: 182, Name: DeletePlayer
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p51, p52) -- Line: 183
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u50 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p52("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/DeletePlayer", u50 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p51, p52);
        end);
    end,

    DeletePushNotificationTemplate = function(p53, u54) -- Line: 195, Name: DeletePushNotificationTemplate
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p55, p56) -- Line: 196
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u54 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p56("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/DeletePushNotificationTemplate", u54 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p55, p56);
        end);
    end,

    DeleteSharedGroup = function(p57, u58) -- Line: 210, Name: DeleteSharedGroup
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 211
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u58 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p60("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/DeleteSharedGroup", u58 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p59, p60);
        end);
    end,

    DeregisterGame = function(p61, u62) -- Line: 223, Name: DeregisterGame
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p63, p64) -- Line: 224
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u62 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p64("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/DeregisterGame", u62 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p63, p64);
        end);
    end,

    EvaluateRandomResultTable = function(p65, u66) -- Line: 237, Name: EvaluateRandomResultTable
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p67, p68) -- Line: 238
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u66 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p68("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/EvaluateRandomResultTable", u66 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p67, p68);
        end);
    end,

    ExecuteCloudScript = function(p69, u70) -- Line: 250, Name: ExecuteCloudScript
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p71, p72) -- Line: 251
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u70 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p72("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/ExecuteCloudScript", u70 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p71, p72);
        end);
    end,

    GetAllSegments = function(p73, u74) -- Line: 264, Name: GetAllSegments
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p75, p76) -- Line: 265
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u74 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p76("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetAllSegments", u74 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p75, p76);
        end);
    end,

    GetAllUsersCharacters = function(p77, u78) -- Line: 278, Name: GetAllUsersCharacters
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p79, p80) -- Line: 279
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u78 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p80("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetAllUsersCharacters", u78 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p79, p80);
        end);
    end,

    GetCatalogItems = function(p81, u82) -- Line: 291, Name: GetCatalogItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p83, p84) -- Line: 292
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u82 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p84("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCatalogItems", u82 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p83, p84);
        end);
    end,

    GetCharacterData = function(p85, u86) -- Line: 304, Name: GetCharacterData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p87, p88) -- Line: 305
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u86 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p88("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterData", u86 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p87, p88);
        end);
    end,

    GetCharacterInternalData = function(p89, u90) -- Line: 317, Name: GetCharacterInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p91, p92) -- Line: 318
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u90 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p92("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterInternalData", u90 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p91, p92);
        end);
    end,

    GetCharacterInventory = function(p93, u94) -- Line: 330, Name: GetCharacterInventory
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p95, p96) -- Line: 331
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u94 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p96("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterInventory", u94 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p95, p96);
        end);
    end,

    GetCharacterLeaderboard = function(p97, u98) -- Line: 343, Name: GetCharacterLeaderboard
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p99, p100) -- Line: 344
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u98 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p100("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterLeaderboard", u98 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p99, p100);
        end);
    end,

    GetCharacterReadOnlyData = function(p101, u102) -- Line: 356, Name: GetCharacterReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p103, p104) -- Line: 357
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u102 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p104("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterReadOnlyData", u102 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p103, p104);
        end);
    end,

    GetCharacterStatistics = function(p105, u106) -- Line: 369, Name: GetCharacterStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p107, p108) -- Line: 370
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u106 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p108("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetCharacterStatistics", u106 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p107, p108);
        end);
    end,

    GetContentDownloadUrl = function(p109, u110) -- Line: 387, Name: GetContentDownloadUrl
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p111, p112) -- Line: 388
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u110 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p112("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetContentDownloadUrl", u110 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p111, p112);
        end);
    end,

    GetFriendLeaderboard = function(p113, u114) -- Line: 401, Name: GetFriendLeaderboard
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p115, p116) -- Line: 402
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u114 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p116("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetFriendLeaderboard", u114 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p115, p116);
        end);
    end,

    GetFriendsList = function(p117, u118) -- Line: 415, Name: GetFriendsList
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p119, p120) -- Line: 416
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u118 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p120("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetFriendsList", u118 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p119, p120);
        end);
    end,

    GetLeaderboard = function(p121, u122) -- Line: 428, Name: GetLeaderboard
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p123, p124) -- Line: 429
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u122 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p124("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetLeaderboard", u122 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p123, p124);
        end);
    end,

    GetLeaderboardAroundCharacter = function(p125, u126) -- Line: 441, Name: GetLeaderboardAroundCharacter
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p127, p128) -- Line: 442
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u126 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p128("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetLeaderboardAroundCharacter", u126 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p127, p128);
        end);
    end,

    GetLeaderboardAroundUser = function(p129, u130) -- Line: 454, Name: GetLeaderboardAroundUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p131, p132) -- Line: 455
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u130 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p132("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetLeaderboardAroundUser", u130 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p131, p132);
        end);
    end,

    GetLeaderboardForUserCharacters = function(p133, u134) -- Line: 467, Name: GetLeaderboardForUserCharacters
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p135, p136) -- Line: 468
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u134 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p136("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetLeaderboardForUserCharacters", u134 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p135, p136);
        end);
    end,

    GetPlayerCombinedInfo = function(p137, u138) -- Line: 481, Name: GetPlayerCombinedInfo
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p139, p140) -- Line: 482
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u138 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p140("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerCombinedInfo", u138 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p139, p140);
        end);
    end,

    GetPlayerProfile = function(p141, u142) -- Line: 494, Name: GetPlayerProfile
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p143, p144) -- Line: 495
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u142 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p144("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerProfile", u142 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p143, p144);
        end);
    end,

    GetPlayerSegments = function(p145, u146) -- Line: 507, Name: GetPlayerSegments
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p147, p148) -- Line: 508
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u146 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p148("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerSegments", u146 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p147, p148);
        end);
    end,

    GetPlayersInSegment = function(p149, u150) -- Line: 524, Name: GetPlayersInSegment
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p151, p152) -- Line: 525
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u150 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p152("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayersInSegment", u150 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p151, p152);
        end);
    end,

    GetPlayerStatistics = function(p153, u154) -- Line: 537, Name: GetPlayerStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p155, p156) -- Line: 538
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u154 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p156("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerStatistics", u154 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p155, p156);
        end);
    end,

    GetPlayerStatisticVersions = function(p157, u158) -- Line: 550, Name: GetPlayerStatisticVersions
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p159, p160) -- Line: 551
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u158 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p160("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerStatisticVersions", u158 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p159, p160);
        end);
    end,

    GetPlayerTags = function(p161, u162) -- Line: 563, Name: GetPlayerTags
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p163, p164) -- Line: 564
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u162 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p164("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayerTags", u162 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p163, p164);
        end);
    end,

    GetPlayFabIDsFromFacebookIDs = function(p165, u166) -- Line: 576, Name: GetPlayFabIDsFromFacebookIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p167, p168) -- Line: 577
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u166 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p168("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromFacebookIDs", u166 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p167, p168);
        end);
    end,

    GetPlayFabIDsFromFacebookInstantGamesIds = function(p169, u170) -- Line: 589, Name: GetPlayFabIDsFromFacebookInstantGamesIds
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p171, p172) -- Line: 590
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u170 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p172("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromFacebookInstantGamesIds", u170 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p171, p172);
        end);
    end,

    GetPlayFabIDsFromGenericIDs = function(p173, u174) -- Line: 604, Name: GetPlayFabIDsFromGenericIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p175, p176) -- Line: 605
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u174 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p176("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromGenericIDs", u174 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p175, p176);
        end);
    end,

    GetPlayFabIDsFromNintendoSwitchDeviceIds = function(p177, u178) -- Line: 617, Name: GetPlayFabIDsFromNintendoSwitchDeviceIds
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p179, p180) -- Line: 618
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u178 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p180("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromNintendoSwitchDeviceIds", u178 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p179, p180);
        end);
    end,

    GetPlayFabIDsFromPSNAccountIDs = function(p181, u182) -- Line: 630, Name: GetPlayFabIDsFromPSNAccountIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p183, p184) -- Line: 631
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u182 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p184("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromPSNAccountIDs", u182 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p183, p184);
        end);
    end,

    GetPlayFabIDsFromSteamIDs = function(p185, u186) -- Line: 644, Name: GetPlayFabIDsFromSteamIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p187, p188) -- Line: 645
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u186 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p188("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromSteamIDs", u186 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p187, p188);
        end);
    end,

    GetPlayFabIDsFromXboxLiveIDs = function(p189, u190) -- Line: 657, Name: GetPlayFabIDsFromXboxLiveIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p191, p192) -- Line: 658
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u190 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p192("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPlayFabIDsFromXboxLiveIDs", u190 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p191, p192);
        end);
    end,

    GetPublisherData = function(p193, u194) -- Line: 670, Name: GetPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p195, p196) -- Line: 671
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u194 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p196("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetPublisherData", u194 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p195, p196);
        end);
    end,

    GetRandomResultTables = function(p197, u198) -- Line: 684, Name: GetRandomResultTables
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p199, p200) -- Line: 685
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u198 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p200("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetRandomResultTables", u198 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p199, p200);
        end);
    end,

    GetServerCustomIDsFromPlayFabIDs = function(p201, u202) -- Line: 697, Name: GetServerCustomIDsFromPlayFabIDs
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p203, p204) -- Line: 698
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u202 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p204("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetServerCustomIDsFromPlayFabIDs", u202 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p203, p204);
        end);
    end,

    GetSharedGroupData = function(p205, u206) -- Line: 712, Name: GetSharedGroupData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p207, p208) -- Line: 713
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u206 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p208("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetSharedGroupData", u206 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p207, p208);
        end);
    end,

    GetStoreItems = function(p209, u210) -- Line: 725, Name: GetStoreItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p211, p212) -- Line: 726
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u210 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p212("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetStoreItems", u210 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p211, p212);
        end);
    end,

    GetTime = function(p213, u214) -- Line: 738, Name: GetTime
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p215, p216) -- Line: 739
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u214 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p216("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetTime", u214 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p215, p216);
        end);
    end,

    GetTitleData = function(p217, u218) -- Line: 751, Name: GetTitleData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p219, p220) -- Line: 752
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u218 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p220("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetTitleData", u218 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p219, p220);
        end);
    end,

    GetTitleInternalData = function(p221, u222) -- Line: 764, Name: GetTitleInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p223, p224) -- Line: 765
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u222 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p224("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetTitleInternalData", u222 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p223, p224);
        end);
    end,

    GetTitleNews = function(p225, u226) -- Line: 777, Name: GetTitleNews
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p227, p228) -- Line: 778
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u226 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p228("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetTitleNews", u226 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p227, p228);
        end);
    end,

    GetUserAccountInfo = function(p229, u230) -- Line: 790, Name: GetUserAccountInfo
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p231, p232) -- Line: 791
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u230 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p232("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserAccountInfo", u230 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p231, p232);
        end);
    end,

    GetUserBans = function(p233, u234) -- Line: 803, Name: GetUserBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p235, p236) -- Line: 804
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u234 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p236("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserBans", u234 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p235, p236);
        end);
    end,

    GetUserData = function(p237, u238) -- Line: 816, Name: GetUserData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p239, p240) -- Line: 817
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u238 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p240("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserData", u238 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p239, p240);
        end);
    end,

    GetUserInternalData = function(p241, u242) -- Line: 829, Name: GetUserInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p243, p244) -- Line: 830
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u242 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p244("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserInternalData", u242 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p243, p244);
        end);
    end,

    GetUserInventory = function(p245, u246) -- Line: 842, Name: GetUserInventory
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p247, p248) -- Line: 843
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u246 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p248("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserInventory", u246 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p247, p248);
        end);
    end,

    GetUserPublisherData = function(p249, u250) -- Line: 855, Name: GetUserPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p251, p252) -- Line: 856
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u250 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p252("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserPublisherData", u250 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p251, p252);
        end);
    end,

    GetUserPublisherInternalData = function(p253, u254) -- Line: 868, Name: GetUserPublisherInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p255, p256) -- Line: 869
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u254 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p256("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserPublisherInternalData", u254 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p255, p256);
        end);
    end,

    GetUserPublisherReadOnlyData = function(p257, u258) -- Line: 881, Name: GetUserPublisherReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p259, p260) -- Line: 882
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u258 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p260("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserPublisherReadOnlyData", u258 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p259, p260);
        end);
    end,

    GetUserReadOnlyData = function(p261, u262) -- Line: 894, Name: GetUserReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p263, p264) -- Line: 895
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u262 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p264("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GetUserReadOnlyData", u262 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p263, p264);
        end);
    end,

    GrantCharacterToUser = function(p265, u266) -- Line: 908, Name: GrantCharacterToUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p267, p268) -- Line: 909
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u266 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p268("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GrantCharacterToUser", u266 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p267, p268);
        end);
    end,

    GrantItemsToCharacter = function(p269, u270) -- Line: 921, Name: GrantItemsToCharacter
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p271, p272) -- Line: 922
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u270 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p272("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GrantItemsToCharacter", u270 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p271, p272);
        end);
    end,

    GrantItemsToUser = function(p273, u274) -- Line: 934, Name: GrantItemsToUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p275, p276) -- Line: 935
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u274 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p276("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GrantItemsToUser", u274 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p275, p276);
        end);
    end,

    GrantItemsToUsers = function(p277, u278) -- Line: 947, Name: GrantItemsToUsers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p279, p280) -- Line: 948
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u278 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p280("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/GrantItemsToUsers", u278 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p279, p280);
        end);
    end,

    LinkPSNAccount = function(p281, u282) -- Line: 960, Name: LinkPSNAccount
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p283, p284) -- Line: 961
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u282 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p284("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LinkPSNAccount", u282 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p283, p284);
        end);
    end,

    LinkServerCustomId = function(p285, u286) -- Line: 973, Name: LinkServerCustomId
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p287, p288) -- Line: 974
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u286 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p288("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LinkServerCustomId", u286 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p287, p288);
        end);
    end,

    LinkXboxAccount = function(p289, u290) -- Line: 986, Name: LinkXboxAccount
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p291, p292) -- Line: 987
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u290 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p292("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LinkXboxAccount", u290 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p291, p292);
        end);
    end,

    LoginWithServerCustomId = function(p293, u294) -- Line: 1000, Name: LoginWithServerCustomId
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p295, p296) -- Line: 1001
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u294 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p296("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LoginWithServerCustomId", u294 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p295, p296);
        end);
    end,

    LoginWithXbox = function(p297, u298) -- Line: 1014, Name: LoginWithXbox
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p299, p300) -- Line: 1015
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u298 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p300("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LoginWithXbox", u298 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p299, p300);
        end);
    end,

    LoginWithXboxId = function(p301, u302) -- Line: 1028, Name: LoginWithXboxId
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p303, p304) -- Line: 1029
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u302 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p304("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/LoginWithXboxId", u302 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p303, p304);
        end);
    end,

    ModifyItemUses = function(p305, u306) -- Line: 1041, Name: ModifyItemUses
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p307, p308) -- Line: 1042
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u306 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p308("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/ModifyItemUses", u306 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p307, p308);
        end);
    end,

    MoveItemToCharacterFromCharacter = function(p309, u310) -- Line: 1054, Name: MoveItemToCharacterFromCharacter
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p311, p312) -- Line: 1055
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u310 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p312("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/MoveItemToCharacterFromCharacter", u310 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p311, p312);
        end);
    end,

    MoveItemToCharacterFromUser = function(p313, u314) -- Line: 1067, Name: MoveItemToCharacterFromUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p315, p316) -- Line: 1068
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u314 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p316("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/MoveItemToCharacterFromUser", u314 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p315, p316);
        end);
    end,

    MoveItemToUserFromCharacter = function(p317, u318) -- Line: 1080, Name: MoveItemToUserFromCharacter
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p319, p320) -- Line: 1081
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u318 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p320("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/MoveItemToUserFromCharacter", u318 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p319, p320);
        end);
    end,

    NotifyMatchmakerPlayerLeft = function(p321, u322) -- Line: 1093, Name: NotifyMatchmakerPlayerLeft
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p323, p324) -- Line: 1094
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u322 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p324("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/NotifyMatchmakerPlayerLeft", u322 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p323, p324);
        end);
    end,

    RedeemCoupon = function(p325, u326) -- Line: 1107, Name: RedeemCoupon
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p327, p328) -- Line: 1108
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u326 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p328("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RedeemCoupon", u326 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p327, p328);
        end);
    end,

    RedeemMatchmakerTicket = function(p329, u330) -- Line: 1120, Name: RedeemMatchmakerTicket
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p331, p332) -- Line: 1121
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u330 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p332("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RedeemMatchmakerTicket", u330 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p331, p332);
        end);
    end,

    RefreshGameServerInstanceHeartbeat = function(p333, u334) -- Line: 1133, Name: RefreshGameServerInstanceHeartbeat
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p335, p336) -- Line: 1134
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u334 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p336("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RefreshGameServerInstanceHeartbeat", u334 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p335, p336);
        end);
    end,

    RegisterGame = function(p337, u338) -- Line: 1146, Name: RegisterGame
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p339, p340) -- Line: 1147
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u338 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p340("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RegisterGame", u338 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p339, p340);
        end);
    end,

    RemoveFriend = function(p341, u342) -- Line: 1159, Name: RemoveFriend
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p343, p344) -- Line: 1160
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u342 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p344("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RemoveFriend", u342 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p343, p344);
        end);
    end,

    RemoveGenericID = function(p345, u346) -- Line: 1172, Name: RemoveGenericID
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p347, p348) -- Line: 1173
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u346 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p348("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RemoveGenericID", u346 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p347, p348);
        end);
    end,

    RemovePlayerTag = function(p349, u350) -- Line: 1185, Name: RemovePlayerTag
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p351, p352) -- Line: 1186
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u350 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p352("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RemovePlayerTag", u350 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p351, p352);
        end);
    end,

    RemoveSharedGroupMembers = function(p353, u354) -- Line: 1201, Name: RemoveSharedGroupMembers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p355, p356) -- Line: 1202
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u354 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p356("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RemoveSharedGroupMembers", u354 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p355, p356);
        end);
    end,

    ReportPlayer = function(p357, u358) -- Line: 1215, Name: ReportPlayer
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p359, p360) -- Line: 1216
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u358 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p360("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/ReportPlayer", u358 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p359, p360);
        end);
    end,

    RevokeAllBansForUser = function(p361, u362) -- Line: 1228, Name: RevokeAllBansForUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p363, p364) -- Line: 1229
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u362 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p364("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RevokeAllBansForUser", u362 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p363, p364);
        end);
    end,

    RevokeBans = function(p365, u366) -- Line: 1241, Name: RevokeBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p367, p368) -- Line: 1242
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u366 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p368("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RevokeBans", u366 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p367, p368);
        end);
    end,

    RevokeInventoryItem = function(p369, u370) -- Line: 1254, Name: RevokeInventoryItem
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p371, p372) -- Line: 1255
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u370 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p372("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RevokeInventoryItem", u370 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p371, p372);
        end);
    end,

    RevokeInventoryItems = function(p373, u374) -- Line: 1267, Name: RevokeInventoryItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p375, p376) -- Line: 1268
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u374 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p376("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/RevokeInventoryItems", u374 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p375, p376);
        end);
    end,

    SavePushNotificationTemplate = function(p377, u378) -- Line: 1280, Name: SavePushNotificationTemplate
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p379, p380) -- Line: 1281
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u378 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p380("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SavePushNotificationTemplate", u378 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p379, p380);
        end);
    end,

    SendCustomAccountRecoveryEmail = function(p381, u382) -- Line: 1294, Name: SendCustomAccountRecoveryEmail
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p383, p384) -- Line: 1295
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u382 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p384("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SendCustomAccountRecoveryEmail", u382 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p383, p384);
        end);
    end,

    SendEmailFromTemplate = function(p385, u386) -- Line: 1307, Name: SendEmailFromTemplate
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p387, p388) -- Line: 1308
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u386 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p388("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SendEmailFromTemplate", u386 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p387, p388);
        end);
    end,

    SendPushNotification = function(p389, u390) -- Line: 1321, Name: SendPushNotification
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p391, p392) -- Line: 1322
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u390 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p392("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SendPushNotification", u390 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p391, p392);
        end);
    end,

    SendPushNotificationFromTemplate = function(p393, u394) -- Line: 1335, Name: SendPushNotificationFromTemplate
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p395, p396) -- Line: 1336
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u394 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p396("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SendPushNotificationFromTemplate", u394 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p395, p396);
        end);
    end,

    SetFriendTags = function(p397, u398) -- Line: 1348, Name: SetFriendTags
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p399, p400) -- Line: 1349
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u398 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p400("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetFriendTags", u398 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p399, p400);
        end);
    end,

    SetGameServerInstanceData = function(p401, u402) -- Line: 1361, Name: SetGameServerInstanceData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p403, p404) -- Line: 1362
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u402 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p404("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetGameServerInstanceData", u402 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p403, p404);
        end);
    end,

    SetGameServerInstanceState = function(p405, u406) -- Line: 1374, Name: SetGameServerInstanceState
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p407, p408) -- Line: 1375
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u406 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p408("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetGameServerInstanceState", u406 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p407, p408);
        end);
    end,

    SetGameServerInstanceTags = function(p409, u410) -- Line: 1387, Name: SetGameServerInstanceTags
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p411, p412) -- Line: 1388
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u410 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p412("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetGameServerInstanceTags", u410 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p411, p412);
        end);
    end,

    SetPlayerSecret = function(p413, u414) -- Line: 1401, Name: SetPlayerSecret
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p415, p416) -- Line: 1402
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u414 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p416("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetPlayerSecret", u414 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p415, p416);
        end);
    end,

    SetPublisherData = function(p417, u418) -- Line: 1414, Name: SetPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p419, p420) -- Line: 1415
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u418 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p420("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetPublisherData", u418 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p419, p420);
        end);
    end,

    SetTitleData = function(p421, u422) -- Line: 1427, Name: SetTitleData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p423, p424) -- Line: 1428
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u422 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p424("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetTitleData", u422 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p423, p424);
        end);
    end,

    SetTitleInternalData = function(p425, u426) -- Line: 1440, Name: SetTitleInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p427, p428) -- Line: 1441
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u426 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p428("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SetTitleInternalData", u426 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p427, p428);
        end);
    end,

    SubtractCharacterVirtualCurrency = function(p429, u430) -- Line: 1454, Name: SubtractCharacterVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p431, p432) -- Line: 1455
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u430 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p432("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SubtractCharacterVirtualCurrency", u430 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p431, p432);
        end);
    end,

    SubtractUserVirtualCurrency = function(p433, u434) -- Line: 1468, Name: SubtractUserVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p435, p436) -- Line: 1469
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u434 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p436("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/SubtractUserVirtualCurrency", u434 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p435, p436);
        end);
    end,

    UnlinkPSNAccount = function(p437, u438) -- Line: 1481, Name: UnlinkPSNAccount
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p439, p440) -- Line: 1482
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u438 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p440("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UnlinkPSNAccount", u438 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p439, p440);
        end);
    end,

    UnlinkServerCustomId = function(p441, u442) -- Line: 1494, Name: UnlinkServerCustomId
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p443, p444) -- Line: 1495
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u442 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p444("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UnlinkServerCustomId", u442 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p443, p444);
        end);
    end,

    UnlinkXboxAccount = function(p445, u446) -- Line: 1507, Name: UnlinkXboxAccount
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p447, p448) -- Line: 1508
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u446 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p448("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UnlinkXboxAccount", u446 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p447, p448);
        end);
    end,

    UnlockContainerInstance = function(p449, u450) -- Line: 1522, Name: UnlockContainerInstance
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p451, p452) -- Line: 1523
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u450 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p452("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UnlockContainerInstance", u450 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p451, p452);
        end);
    end,

    UnlockContainerItem = function(p453, u454) -- Line: 1538, Name: UnlockContainerItem
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p455, p456) -- Line: 1539
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u454 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p456("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UnlockContainerItem", u454 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p455, p456);
        end);
    end,

    UpdateAvatarUrl = function(p457, u458) -- Line: 1551, Name: UpdateAvatarUrl
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p459, p460) -- Line: 1552
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u458 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p460("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateAvatarUrl", u458 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p459, p460);
        end);
    end,

    UpdateBans = function(p461, u462) -- Line: 1564, Name: UpdateBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p463, p464) -- Line: 1565
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u462 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p464("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateBans", u462 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p463, p464);
        end);
    end,

    UpdateCharacterData = function(p465, u466) -- Line: 1577, Name: UpdateCharacterData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p467, p468) -- Line: 1578
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u466 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p468("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateCharacterData", u466 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p467, p468);
        end);
    end,

    UpdateCharacterInternalData = function(p469, u470) -- Line: 1590, Name: UpdateCharacterInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p471, p472) -- Line: 1591
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u470 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p472("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateCharacterInternalData", u470 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p471, p472);
        end);
    end,

    UpdateCharacterReadOnlyData = function(p473, u474) -- Line: 1603, Name: UpdateCharacterReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p475, p476) -- Line: 1604
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u474 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p476("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateCharacterReadOnlyData", u474 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p475, p476);
        end);
    end,

    UpdateCharacterStatistics = function(p477, u478) -- Line: 1616, Name: UpdateCharacterStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p479, p480) -- Line: 1617
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u478 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p480("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateCharacterStatistics", u478 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p479, p480);
        end);
    end,

    UpdatePlayerStatistics = function(p481, u482) -- Line: 1629, Name: UpdatePlayerStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p483, p484) -- Line: 1630
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u482 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p484("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdatePlayerStatistics", u482 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p483, p484);
        end);
    end,

    UpdateSharedGroupData = function(p485, u486) -- Line: 1646, Name: UpdateSharedGroupData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p487, p488) -- Line: 1647
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u486 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p488("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateSharedGroupData", u486 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p487, p488);
        end);
    end,

    UpdateUserData = function(p489, u490) -- Line: 1659, Name: UpdateUserData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p491, p492) -- Line: 1660
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u490 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p492("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserData", u490 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p491, p492);
        end);
    end,

    UpdateUserInternalData = function(p493, u494) -- Line: 1672, Name: UpdateUserInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p495, p496) -- Line: 1673
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u494 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p496("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserInternalData", u494 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p495, p496);
        end);
    end,

    UpdateUserInventoryItemCustomData = function(p497, u498) -- Line: 1685, Name: UpdateUserInventoryItemCustomData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p499, p500) -- Line: 1686
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u498 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p500("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserInventoryItemCustomData", u498 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p499, p500);
        end);
    end,

    UpdateUserPublisherData = function(p501, u502) -- Line: 1698, Name: UpdateUserPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p503, p504) -- Line: 1699
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u502 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p504("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserPublisherData", u502 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p503, p504);
        end);
    end,

    UpdateUserPublisherInternalData = function(p505, u506) -- Line: 1711, Name: UpdateUserPublisherInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p507, p508) -- Line: 1712
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u506 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p508("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserPublisherInternalData", u506 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p507, p508);
        end);
    end,

    UpdateUserPublisherReadOnlyData = function(p509, u510) -- Line: 1724, Name: UpdateUserPublisherReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p511, p512) -- Line: 1725
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u510 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p512("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserPublisherReadOnlyData", u510 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p511, p512);
        end);
    end,

    UpdateUserReadOnlyData = function(p513, u514) -- Line: 1737, Name: UpdateUserReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p515, p516) -- Line: 1738
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u514 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p516("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/UpdateUserReadOnlyData", u514 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p515, p516);
        end);
    end,

    WriteCharacterEvent = function(p517, u518) -- Line: 1750, Name: WriteCharacterEvent
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p519, p520) -- Line: 1751
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u518 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p520("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/WriteCharacterEvent", u518 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p519, p520);
        end);
    end,

    WritePlayerEvent = function(p521, u522) -- Line: 1763, Name: WritePlayerEvent
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p523, p524) -- Line: 1764
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u522 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p524("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/WritePlayerEvent", u522 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p523, p524);
        end);
    end,

    WriteTitleEvent = function(p525, u526) -- Line: 1776, Name: WriteTitleEvent
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p527, p528) -- Line: 1777
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u526 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p528("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Server/WriteTitleEvent", u526 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p527, p528);
        end);
    end
};