-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);
local PlayFabSettings = require(script.Parent.PlayFabSettings);

return {
    settings = PlayFabSettings.settings,

    GetEntityToken = function(p1, u2) -- Line: 18, Name: GetEntityToken
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p3, p4) -- Line: 19
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u2 (copy)
            local v5, v6;

            if PlayFabSettings._internalSettings.entityToken then
                v5 = PlayFabSettings._internalSettings.entityToken;
                v6 = "X-EntityToken";
            else
                v6 = nil;
                v5 = nil;
            end;

            if PlayFabSettings._internalSettings.sessionTicket then
                v5 = PlayFabSettings._internalSettings.sessionTicket;
                v6 = "X-Authorization";
            end;

            if PlayFabSettings.settings.devSecretKey then
                v5 = PlayFabSettings.settings.devSecretKey;
                v6 = "X-SecretKey";
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Authentication/GetEntityToken", u2 or {}, v6, v5, p3, p4);
        end);
    end,

    ValidateEntityToken = function(p7, u8, u9) -- Line: 42, Name: ValidateEntityToken
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p10, p11) -- Line: 43
            -- upvalues: u8 (copy), IPlayFabHttps (ref), u9 (copy)
            if not u8 then
                p11("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Authentication/ValidateEntityToken", u9 or {}, "X-EntityToken", u8, p10, p11);
        end);
    end
};