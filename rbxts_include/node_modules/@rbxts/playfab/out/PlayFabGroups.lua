-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    AcceptGroupApplication = function(p1, u2, u3) -- Line: 17, Name: AcceptGroupApplication
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 18
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/AcceptGroupApplication", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    AcceptGroupInvitation = function(p6, u7, u8) -- Line: 28, Name: AcceptGroupInvitation
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 29
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/AcceptGroupInvitation", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    AddMembers = function(p11, u12, u13) -- Line: 39, Name: AddMembers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 40
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/AddMembers", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    ApplyToGroup = function(p16, u17, u18) -- Line: 50, Name: ApplyToGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 51
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ApplyToGroup", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    BlockEntity = function(p21, u22, u23) -- Line: 61, Name: BlockEntity
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 62
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/BlockEntity", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    ChangeMemberRole = function(p26, u27, u28) -- Line: 72, Name: ChangeMemberRole
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 73
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ChangeMemberRole", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end,

    CreateGroup = function(p31, u32, u33) -- Line: 83, Name: CreateGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p34, p35) -- Line: 84
            -- upvalues: u32 (copy), IPlayFabHttps (ref), u33 (copy)
            if not u32 then
                p35("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/CreateGroup", u33 or {}, "X-EntityToken", u32, p34, p35);
        end);
    end,

    CreateRole = function(p36, u37, u38) -- Line: 94, Name: CreateRole
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 95
            -- upvalues: u37 (copy), IPlayFabHttps (ref), u38 (copy)
            if not u37 then
                p40("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/CreateRole", u38 or {}, "X-EntityToken", u37, p39, p40);
        end);
    end,

    DeleteGroup = function(p41, u42, u43) -- Line: 105, Name: DeleteGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p44, p45) -- Line: 106
            -- upvalues: u42 (copy), IPlayFabHttps (ref), u43 (copy)
            if not u42 then
                p45("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/DeleteGroup", u43 or {}, "X-EntityToken", u42, p44, p45);
        end);
    end,

    DeleteRole = function(p46, u47, u48) -- Line: 116, Name: DeleteRole
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p49, p50) -- Line: 117
            -- upvalues: u47 (copy), IPlayFabHttps (ref), u48 (copy)
            if not u47 then
                p50("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/DeleteRole", u48 or {}, "X-EntityToken", u47, p49, p50);
        end);
    end,

    GetGroup = function(p51, u52, u53) -- Line: 127, Name: GetGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p54, p55) -- Line: 128
            -- upvalues: u52 (copy), IPlayFabHttps (ref), u53 (copy)
            if not u52 then
                p55("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/GetGroup", u53 or {}, "X-EntityToken", u52, p54, p55);
        end);
    end,

    InviteToGroup = function(p56, u57, u58) -- Line: 138, Name: InviteToGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 139
            -- upvalues: u57 (copy), IPlayFabHttps (ref), u58 (copy)
            if not u57 then
                p60("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/InviteToGroup", u58 or {}, "X-EntityToken", u57, p59, p60);
        end);
    end,

    IsMember = function(p61, u62, u63) -- Line: 149, Name: IsMember
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p64, p65) -- Line: 150
            -- upvalues: u62 (copy), IPlayFabHttps (ref), u63 (copy)
            if not u62 then
                p65("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/IsMember", u63 or {}, "X-EntityToken", u62, p64, p65);
        end);
    end,

    ListGroupApplications = function(p66, u67, u68) -- Line: 160, Name: ListGroupApplications
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p69, p70) -- Line: 161
            -- upvalues: u67 (copy), IPlayFabHttps (ref), u68 (copy)
            if not u67 then
                p70("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListGroupApplications", u68 or {}, "X-EntityToken", u67, p69, p70);
        end);
    end,

    ListGroupBlocks = function(p71, u72, u73) -- Line: 171, Name: ListGroupBlocks
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p74, p75) -- Line: 172
            -- upvalues: u72 (copy), IPlayFabHttps (ref), u73 (copy)
            if not u72 then
                p75("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListGroupBlocks", u73 or {}, "X-EntityToken", u72, p74, p75);
        end);
    end,

    ListGroupInvitations = function(p76, u77, u78) -- Line: 182, Name: ListGroupInvitations
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p79, p80) -- Line: 183
            -- upvalues: u77 (copy), IPlayFabHttps (ref), u78 (copy)
            if not u77 then
                p80("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListGroupInvitations", u78 or {}, "X-EntityToken", u77, p79, p80);
        end);
    end,

    ListGroupMembers = function(p81, u82, u83) -- Line: 193, Name: ListGroupMembers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p84, p85) -- Line: 194
            -- upvalues: u82 (copy), IPlayFabHttps (ref), u83 (copy)
            if not u82 then
                p85("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListGroupMembers", u83 or {}, "X-EntityToken", u82, p84, p85);
        end);
    end,

    ListMembership = function(p86, u87, u88) -- Line: 204, Name: ListMembership
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p89, p90) -- Line: 205
            -- upvalues: u87 (copy), IPlayFabHttps (ref), u88 (copy)
            if not u87 then
                p90("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListMembership", u88 or {}, "X-EntityToken", u87, p89, p90);
        end);
    end,

    ListMembershipOpportunities = function(p91, u92, u93) -- Line: 215, Name: ListMembershipOpportunities
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p94, p95) -- Line: 216
            -- upvalues: u92 (copy), IPlayFabHttps (ref), u93 (copy)
            if not u92 then
                p95("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/ListMembershipOpportunities", u93 or {}, "X-EntityToken", u92, p94, p95);
        end);
    end,

    RemoveGroupApplication = function(p96, u97, u98) -- Line: 226, Name: RemoveGroupApplication
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p99, p100) -- Line: 227
            -- upvalues: u97 (copy), IPlayFabHttps (ref), u98 (copy)
            if not u97 then
                p100("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/RemoveGroupApplication", u98 or {}, "X-EntityToken", u97, p99, p100);
        end);
    end,

    RemoveGroupInvitation = function(p101, u102, u103) -- Line: 237, Name: RemoveGroupInvitation
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p104, p105) -- Line: 238
            -- upvalues: u102 (copy), IPlayFabHttps (ref), u103 (copy)
            if not u102 then
                p105("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/RemoveGroupInvitation", u103 or {}, "X-EntityToken", u102, p104, p105);
        end);
    end,

    RemoveMembers = function(p106, u107, u108) -- Line: 248, Name: RemoveMembers
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p109, p110) -- Line: 249
            -- upvalues: u107 (copy), IPlayFabHttps (ref), u108 (copy)
            if not u107 then
                p110("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/RemoveMembers", u108 or {}, "X-EntityToken", u107, p109, p110);
        end);
    end,

    UnblockEntity = function(p111, u112, u113) -- Line: 259, Name: UnblockEntity
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p114, p115) -- Line: 260
            -- upvalues: u112 (copy), IPlayFabHttps (ref), u113 (copy)
            if not u112 then
                p115("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/UnblockEntity", u113 or {}, "X-EntityToken", u112, p114, p115);
        end);
    end,

    UpdateGroup = function(p116, u117, u118) -- Line: 270, Name: UpdateGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p119, p120) -- Line: 271
            -- upvalues: u117 (copy), IPlayFabHttps (ref), u118 (copy)
            if not u117 then
                p120("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/UpdateGroup", u118 or {}, "X-EntityToken", u117, p119, p120);
        end);
    end,

    UpdateRole = function(p121, u122, u123) -- Line: 281, Name: UpdateRole
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p124, p125) -- Line: 282
            -- upvalues: u122 (copy), IPlayFabHttps (ref), u123 (copy)
            if not u122 then
                p125("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Group/UpdateRole", u123 or {}, "X-EntityToken", u122, p124, p125);
        end);
    end
};