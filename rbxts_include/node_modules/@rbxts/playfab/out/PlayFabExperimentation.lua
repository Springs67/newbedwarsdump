-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    CreateExclusionGroup = function(p1, u2, u3) -- Line: 17, Name: CreateExclusionGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 18
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/CreateExclusionGroup", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    CreateExperiment = function(p6, u7, u8) -- Line: 28, Name: CreateExperiment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 29
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/CreateExperiment", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    DeleteExclusionGroup = function(p11, u12, u13) -- Line: 39, Name: DeleteExclusionGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 40
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/DeleteExclusionGroup", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    DeleteExperiment = function(p16, u17, u18) -- Line: 50, Name: DeleteExperiment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 51
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/DeleteExperiment", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    GetExclusionGroups = function(p21, u22, u23) -- Line: 61, Name: GetExclusionGroups
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 62
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/GetExclusionGroups", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    GetExclusionGroupTraffic = function(p26, u27, u28) -- Line: 72, Name: GetExclusionGroupTraffic
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 73
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/GetExclusionGroupTraffic", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end,

    GetExperiments = function(p31, u32, u33) -- Line: 83, Name: GetExperiments
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p34, p35) -- Line: 84
            -- upvalues: u32 (copy), IPlayFabHttps (ref), u33 (copy)
            if not u32 then
                p35("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/GetExperiments", u33 or {}, "X-EntityToken", u32, p34, p35);
        end);
    end,

    GetLatestScorecard = function(p36, u37, u38) -- Line: 94, Name: GetLatestScorecard
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 95
            -- upvalues: u37 (copy), IPlayFabHttps (ref), u38 (copy)
            if not u37 then
                p40("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/GetLatestScorecard", u38 or {}, "X-EntityToken", u37, p39, p40);
        end);
    end,

    GetTreatmentAssignment = function(p41, u42, u43) -- Line: 105, Name: GetTreatmentAssignment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p44, p45) -- Line: 106
            -- upvalues: u42 (copy), IPlayFabHttps (ref), u43 (copy)
            if not u42 then
                p45("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/GetTreatmentAssignment", u43 or {}, "X-EntityToken", u42, p44, p45);
        end);
    end,

    StartExperiment = function(p46, u47, u48) -- Line: 116, Name: StartExperiment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p49, p50) -- Line: 117
            -- upvalues: u47 (copy), IPlayFabHttps (ref), u48 (copy)
            if not u47 then
                p50("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/StartExperiment", u48 or {}, "X-EntityToken", u47, p49, p50);
        end);
    end,

    StopExperiment = function(p51, u52, u53) -- Line: 127, Name: StopExperiment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p54, p55) -- Line: 128
            -- upvalues: u52 (copy), IPlayFabHttps (ref), u53 (copy)
            if not u52 then
                p55("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/StopExperiment", u53 or {}, "X-EntityToken", u52, p54, p55);
        end);
    end,

    UpdateExclusionGroup = function(p56, u57, u58) -- Line: 138, Name: UpdateExclusionGroup
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 139
            -- upvalues: u57 (copy), IPlayFabHttps (ref), u58 (copy)
            if not u57 then
                p60("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/UpdateExclusionGroup", u58 or {}, "X-EntityToken", u57, p59, p60);
        end);
    end,

    UpdateExperiment = function(p61, u62, u63) -- Line: 149, Name: UpdateExperiment
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p64, p65) -- Line: 150
            -- upvalues: u62 (copy), IPlayFabHttps (ref), u63 (copy)
            if not u62 then
                p65("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Experimentation/UpdateExperiment", u63 or {}, "X-EntityToken", u62, p64, p65);
        end);
    end
};