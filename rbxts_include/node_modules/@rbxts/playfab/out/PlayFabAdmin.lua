-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);
local PlayFabSettings = require(script.Parent.PlayFabSettings);

return {
    settings = PlayFabSettings.settings,

    AbortTaskInstance = function(p1, u2) -- Line: 17, Name: AbortTaskInstance
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p3, p4) -- Line: 18
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u2 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p4("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AbortTaskInstance", u2 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p3, p4);
        end);
    end,

    AddLocalizedNews = function(p5, u6) -- Line: 30, Name: AddLocalizedNews
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p7, p8) -- Line: 31
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u6 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p8("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddLocalizedNews", u6 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p7, p8);
        end);
    end,

    AddNews = function(p9, u10) -- Line: 43, Name: AddNews
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p11, p12) -- Line: 44
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u10 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p12("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddNews", u10 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p11, p12);
        end);
    end,

    AddPlayerTag = function(p13, u14) -- Line: 56, Name: AddPlayerTag
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p15, p16) -- Line: 57
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u14 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p16("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddPlayerTag", u14 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p15, p16);
        end);
    end,

    AddServerBuild = function(p17, u18) -- Line: 70, Name: AddServerBuild
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 71
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u18 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p20("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddServerBuild", u18 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p19, p20);
        end);
    end,

    AddUserVirtualCurrency = function(p21, u22) -- Line: 83, Name: AddUserVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p23, p24) -- Line: 84
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u22 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p24("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddUserVirtualCurrency", u22 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p23, p24);
        end);
    end,

    AddVirtualCurrencyTypes = function(p25, u26) -- Line: 97, Name: AddVirtualCurrencyTypes
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p27, p28) -- Line: 98
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u26 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p28("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/AddVirtualCurrencyTypes", u26 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p27, p28);
        end);
    end,

    BanUsers = function(p29, u30) -- Line: 110, Name: BanUsers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p31, p32) -- Line: 111
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u30 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p32("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/BanUsers", u30 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p31, p32);
        end);
    end,

    CheckLimitedEditionItemAvailability = function(p33, u34) -- Line: 123, Name: CheckLimitedEditionItemAvailability
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p35, p36) -- Line: 124
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u34 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p36("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CheckLimitedEditionItemAvailability", u34 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p35, p36);
        end);
    end,

    CreateActionsOnPlayersInSegmentTask = function(p37, u38) -- Line: 136, Name: CreateActionsOnPlayersInSegmentTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p39, p40) -- Line: 137
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u38 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p40("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreateActionsOnPlayersInSegmentTask", u38 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p39, p40);
        end);
    end,

    CreateCloudScriptTask = function(p41, u42) -- Line: 149, Name: CreateCloudScriptTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p43, p44) -- Line: 150
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u42 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p44("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreateCloudScriptTask", u42 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p43, p44);
        end);
    end,

    CreateInsightsScheduledScalingTask = function(p45, u46) -- Line: 162, Name: CreateInsightsScheduledScalingTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p47, p48) -- Line: 163
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u46 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p48("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreateInsightsScheduledScalingTask", u46 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p47, p48);
        end);
    end,

    CreateOpenIdConnection = function(p49, u50) -- Line: 175, Name: CreateOpenIdConnection
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p51, p52) -- Line: 176
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u50 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p52("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreateOpenIdConnection", u50 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p51, p52);
        end);
    end,

    CreatePlayerSharedSecret = function(p53, u54) -- Line: 189, Name: CreatePlayerSharedSecret
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p55, p56) -- Line: 190
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u54 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p56("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreatePlayerSharedSecret", u54 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p55, p56);
        end);
    end,

    CreatePlayerStatisticDefinition = function(p57, u58) -- Line: 203, Name: CreatePlayerStatisticDefinition
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p59, p60) -- Line: 204
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u58 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p60("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreatePlayerStatisticDefinition", u58 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p59, p60);
        end);
    end,

    CreateSegment = function(p61, u62) -- Line: 217, Name: CreateSegment
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p63, p64) -- Line: 218
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u62 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p64("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/CreateSegment", u62 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p63, p64);
        end);
    end,

    DeleteContent = function(p65, u66) -- Line: 230, Name: DeleteContent
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p67, p68) -- Line: 231
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u66 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p68("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteContent", u66 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p67, p68);
        end);
    end,

    DeleteMasterPlayerAccount = function(p69, u70) -- Line: 243, Name: DeleteMasterPlayerAccount
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p71, p72) -- Line: 244
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u70 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p72("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteMasterPlayerAccount", u70 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p71, p72);
        end);
    end,

    DeleteOpenIdConnection = function(p73, u74) -- Line: 256, Name: DeleteOpenIdConnection
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p75, p76) -- Line: 257
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u74 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p76("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteOpenIdConnection", u74 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p75, p76);
        end);
    end,

    DeletePlayer = function(p77, u78) -- Line: 269, Name: DeletePlayer
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p79, p80) -- Line: 270
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u78 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p80("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeletePlayer", u78 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p79, p80);
        end);
    end,

    DeletePlayerSharedSecret = function(p81, u82) -- Line: 283, Name: DeletePlayerSharedSecret
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p83, p84) -- Line: 284
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u82 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p84("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeletePlayerSharedSecret", u82 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p83, p84);
        end);
    end,

    DeleteSegment = function(p85, u86) -- Line: 296, Name: DeleteSegment
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p87, p88) -- Line: 297
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u86 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p88("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteSegment", u86 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p87, p88);
        end);
    end,

    DeleteStore = function(p89, u90) -- Line: 309, Name: DeleteStore
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p91, p92) -- Line: 310
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u90 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p92("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteStore", u90 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p91, p92);
        end);
    end,

    DeleteTask = function(p93, u94) -- Line: 322, Name: DeleteTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p95, p96) -- Line: 323
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u94 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p96("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteTask", u94 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p95, p96);
        end);
    end,

    DeleteTitle = function(p97, u98) -- Line: 335, Name: DeleteTitle
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p99, p100) -- Line: 336
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u98 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p100("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteTitle", u98 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p99, p100);
        end);
    end,

    DeleteTitleDataOverride = function(p101, u102) -- Line: 348, Name: DeleteTitleDataOverride
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p103, p104) -- Line: 349
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u102 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p104("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/DeleteTitleDataOverride", u102 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p103, p104);
        end);
    end,

    ExportMasterPlayerData = function(p105, u106) -- Line: 361, Name: ExportMasterPlayerData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p107, p108) -- Line: 362
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u106 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p108("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ExportMasterPlayerData", u106 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p107, p108);
        end);
    end,

    GetActionsOnPlayersInSegmentTaskInstance = function(p109, u110) -- Line: 374, Name: GetActionsOnPlayersInSegmentTaskInstance
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p111, p112) -- Line: 375
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u110 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p112("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetActionsOnPlayersInSegmentTaskInstance", u110 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p111, p112);
        end);
    end,

    GetAllSegments = function(p113, u114) -- Line: 388, Name: GetAllSegments
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p115, p116) -- Line: 389
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u114 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p116("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetAllSegments", u114 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p115, p116);
        end);
    end,

    GetCatalogItems = function(p117, u118) -- Line: 401, Name: GetCatalogItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p119, p120) -- Line: 402
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u118 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p120("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetCatalogItems", u118 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p119, p120);
        end);
    end,

    GetCloudScriptRevision = function(p121, u122) -- Line: 414, Name: GetCloudScriptRevision
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p123, p124) -- Line: 415
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u122 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p124("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetCloudScriptRevision", u122 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p123, p124);
        end);
    end,

    GetCloudScriptTaskInstance = function(p125, u126) -- Line: 427, Name: GetCloudScriptTaskInstance
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p127, p128) -- Line: 428
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u126 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p128("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetCloudScriptTaskInstance", u126 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p127, p128);
        end);
    end,

    GetCloudScriptVersions = function(p129, u130) -- Line: 441, Name: GetCloudScriptVersions
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p131, p132) -- Line: 442
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u130 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p132("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetCloudScriptVersions", u130 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p131, p132);
        end);
    end,

    GetContentList = function(p133, u134) -- Line: 454, Name: GetContentList
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p135, p136) -- Line: 455
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u134 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p136("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetContentList", u134 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p135, p136);
        end);
    end,

    GetContentUploadUrl = function(p137, u138) -- Line: 469, Name: GetContentUploadUrl
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p139, p140) -- Line: 470
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u138 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p140("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetContentUploadUrl", u138 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p139, p140);
        end);
    end,

    GetDataReport = function(p141, u142) -- Line: 482, Name: GetDataReport
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p143, p144) -- Line: 483
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u142 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p144("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetDataReport", u142 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p143, p144);
        end);
    end,

    GetMatchmakerGameInfo = function(p145, u146) -- Line: 495, Name: GetMatchmakerGameInfo
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p147, p148) -- Line: 496
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u146 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p148("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetMatchmakerGameInfo", u146 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p147, p148);
        end);
    end,

    GetMatchmakerGameModes = function(p149, u150) -- Line: 508, Name: GetMatchmakerGameModes
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p151, p152) -- Line: 509
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u150 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p152("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetMatchmakerGameModes", u150 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p151, p152);
        end);
    end,

    GetPlayedTitleList = function(p153, u154) -- Line: 521, Name: GetPlayedTitleList
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p155, p156) -- Line: 522
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u154 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p156("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayedTitleList", u154 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p155, p156);
        end);
    end,

    GetPlayerIdFromAuthToken = function(p157, u158) -- Line: 534, Name: GetPlayerIdFromAuthToken
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p159, p160) -- Line: 535
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u158 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p160("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerIdFromAuthToken", u158 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p159, p160);
        end);
    end,

    GetPlayerProfile = function(p161, u162) -- Line: 547, Name: GetPlayerProfile
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p163, p164) -- Line: 548
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u162 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p164("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerProfile", u162 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p163, p164);
        end);
    end,

    GetPlayerSegments = function(p165, u166) -- Line: 560, Name: GetPlayerSegments
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p167, p168) -- Line: 561
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u166 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p168("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerSegments", u166 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p167, p168);
        end);
    end,

    GetPlayerSharedSecrets = function(p169, u170) -- Line: 573, Name: GetPlayerSharedSecrets
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p171, p172) -- Line: 574
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u170 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p172("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerSharedSecrets", u170 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p171, p172);
        end);
    end,

    GetPlayersInSegment = function(p173, u174) -- Line: 590, Name: GetPlayersInSegment
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p175, p176) -- Line: 591
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u174 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p176("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayersInSegment", u174 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p175, p176);
        end);
    end,

    GetPlayerStatisticDefinitions = function(p177, u178) -- Line: 604, Name: GetPlayerStatisticDefinitions
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p179, p180) -- Line: 605
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u178 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p180("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerStatisticDefinitions", u178 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p179, p180);
        end);
    end,

    GetPlayerStatisticVersions = function(p181, u182) -- Line: 617, Name: GetPlayerStatisticVersions
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p183, p184) -- Line: 618
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u182 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p184("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerStatisticVersions", u182 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p183, p184);
        end);
    end,

    GetPlayerTags = function(p185, u186) -- Line: 630, Name: GetPlayerTags
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p187, p188) -- Line: 631
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u186 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p188("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPlayerTags", u186 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p187, p188);
        end);
    end,

    GetPolicy = function(p189, u190) -- Line: 643, Name: GetPolicy
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p191, p192) -- Line: 644
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u190 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p192("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPolicy", u190 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p191, p192);
        end);
    end,

    GetPublisherData = function(p193, u194) -- Line: 656, Name: GetPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p195, p196) -- Line: 657
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u194 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p196("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetPublisherData", u194 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p195, p196);
        end);
    end,

    GetRandomResultTables = function(p197, u198) -- Line: 669, Name: GetRandomResultTables
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p199, p200) -- Line: 670
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u198 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p200("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetRandomResultTables", u198 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p199, p200);
        end);
    end,

    GetSegments = function(p201, u202) -- Line: 682, Name: GetSegments
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p203, p204) -- Line: 683
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u202 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p204("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetSegments", u202 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p203, p204);
        end);
    end,

    GetServerBuildInfo = function(p205, u206) -- Line: 695, Name: GetServerBuildInfo
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p207, p208) -- Line: 696
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u206 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p208("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetServerBuildInfo", u206 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p207, p208);
        end);
    end,

    GetServerBuildUploadUrl = function(p209, u210) -- Line: 709, Name: GetServerBuildUploadUrl
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p211, p212) -- Line: 710
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u210 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p212("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetServerBuildUploadUrl", u210 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p211, p212);
        end);
    end,

    GetStoreItems = function(p213, u214) -- Line: 722, Name: GetStoreItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p215, p216) -- Line: 723
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u214 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p216("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetStoreItems", u214 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p215, p216);
        end);
    end,

    GetTaskInstances = function(p217, u218) -- Line: 735, Name: GetTaskInstances
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p219, p220) -- Line: 736
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u218 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p220("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetTaskInstances", u218 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p219, p220);
        end);
    end,

    GetTasks = function(p221, u222) -- Line: 748, Name: GetTasks
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p223, p224) -- Line: 749
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u222 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p224("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetTasks", u222 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p223, p224);
        end);
    end,

    GetTitleData = function(p225, u226) -- Line: 761, Name: GetTitleData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p227, p228) -- Line: 762
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u226 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p228("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetTitleData", u226 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p227, p228);
        end);
    end,

    GetTitleInternalData = function(p229, u230) -- Line: 774, Name: GetTitleInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p231, p232) -- Line: 775
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u230 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p232("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetTitleInternalData", u230 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p231, p232);
        end);
    end,

    GetUserAccountInfo = function(p233, u234) -- Line: 787, Name: GetUserAccountInfo
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p235, p236) -- Line: 788
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u234 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p236("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserAccountInfo", u234 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p235, p236);
        end);
    end,

    GetUserBans = function(p237, u238) -- Line: 800, Name: GetUserBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p239, p240) -- Line: 801
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u238 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p240("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserBans", u238 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p239, p240);
        end);
    end,

    GetUserData = function(p241, u242) -- Line: 813, Name: GetUserData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p243, p244) -- Line: 814
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u242 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p244("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserData", u242 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p243, p244);
        end);
    end,

    GetUserInternalData = function(p245, u246) -- Line: 826, Name: GetUserInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p247, p248) -- Line: 827
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u246 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p248("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserInternalData", u246 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p247, p248);
        end);
    end,

    GetUserInventory = function(p249, u250) -- Line: 839, Name: GetUserInventory
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p251, p252) -- Line: 840
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u250 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p252("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserInventory", u250 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p251, p252);
        end);
    end,

    GetUserPublisherData = function(p253, u254) -- Line: 852, Name: GetUserPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p255, p256) -- Line: 853
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u254 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p256("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserPublisherData", u254 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p255, p256);
        end);
    end,

    GetUserPublisherInternalData = function(p257, u258) -- Line: 865, Name: GetUserPublisherInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p259, p260) -- Line: 866
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u258 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p260("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserPublisherInternalData", u258 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p259, p260);
        end);
    end,

    GetUserPublisherReadOnlyData = function(p261, u262) -- Line: 878, Name: GetUserPublisherReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p263, p264) -- Line: 879
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u262 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p264("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserPublisherReadOnlyData", u262 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p263, p264);
        end);
    end,

    GetUserReadOnlyData = function(p265, u266) -- Line: 891, Name: GetUserReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p267, p268) -- Line: 892
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u266 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p268("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GetUserReadOnlyData", u266 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p267, p268);
        end);
    end,

    GrantItemsToUsers = function(p269, u270) -- Line: 904, Name: GrantItemsToUsers
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p271, p272) -- Line: 905
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u270 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p272("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/GrantItemsToUsers", u270 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p271, p272);
        end);
    end,

    IncrementLimitedEditionItemAvailability = function(p273, u274) -- Line: 917, Name: IncrementLimitedEditionItemAvailability
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p275, p276) -- Line: 918
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u274 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p276("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/IncrementLimitedEditionItemAvailability", u274 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p275, p276);
        end);
    end,

    IncrementPlayerStatisticVersion = function(p277, u278) -- Line: 930, Name: IncrementPlayerStatisticVersion
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p279, p280) -- Line: 931
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u278 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p280("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/IncrementPlayerStatisticVersion", u278 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p279, p280);
        end);
    end,

    ListOpenIdConnection = function(p281, u282) -- Line: 943, Name: ListOpenIdConnection
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p283, p284) -- Line: 944
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u282 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p284("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ListOpenIdConnection", u282 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p283, p284);
        end);
    end,

    ListServerBuilds = function(p285, u286) -- Line: 956, Name: ListServerBuilds
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p287, p288) -- Line: 957
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u286 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p288("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ListServerBuilds", u286 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p287, p288);
        end);
    end,

    ListVirtualCurrencyTypes = function(p289, u290) -- Line: 969, Name: ListVirtualCurrencyTypes
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p291, p292) -- Line: 970
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u290 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p292("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ListVirtualCurrencyTypes", u290 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p291, p292);
        end);
    end,

    ModifyMatchmakerGameModes = function(p293, u294) -- Line: 982, Name: ModifyMatchmakerGameModes
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p295, p296) -- Line: 983
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u294 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p296("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ModifyMatchmakerGameModes", u294 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p295, p296);
        end);
    end,

    ModifyServerBuild = function(p297, u298) -- Line: 995, Name: ModifyServerBuild
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p299, p300) -- Line: 996
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u298 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p300("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ModifyServerBuild", u298 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p299, p300);
        end);
    end,

    RefundPurchase = function(p301, u302) -- Line: 1008, Name: RefundPurchase
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p303, p304) -- Line: 1009
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u302 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p304("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RefundPurchase", u302 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p303, p304);
        end);
    end,

    RemovePlayerTag = function(p305, u306) -- Line: 1021, Name: RemovePlayerTag
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p307, p308) -- Line: 1022
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u306 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p308("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RemovePlayerTag", u306 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p307, p308);
        end);
    end,

    RemoveServerBuild = function(p309, u310) -- Line: 1035, Name: RemoveServerBuild
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p311, p312) -- Line: 1036
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u310 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p312("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RemoveServerBuild", u310 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p311, p312);
        end);
    end,

    RemoveVirtualCurrencyTypes = function(p313, u314) -- Line: 1048, Name: RemoveVirtualCurrencyTypes
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p315, p316) -- Line: 1049
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u314 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p316("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RemoveVirtualCurrencyTypes", u314 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p315, p316);
        end);
    end,

    ResetCharacterStatistics = function(p317, u318) -- Line: 1061, Name: ResetCharacterStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p319, p320) -- Line: 1062
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u318 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p320("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ResetCharacterStatistics", u318 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p319, p320);
        end);
    end,

    ResetPassword = function(p321, u322) -- Line: 1074, Name: ResetPassword
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p323, p324) -- Line: 1075
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u322 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p324("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ResetPassword", u322 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p323, p324);
        end);
    end,

    ResetUserStatistics = function(p325, u326) -- Line: 1087, Name: ResetUserStatistics
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p327, p328) -- Line: 1088
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u326 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p328("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ResetUserStatistics", u326 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p327, p328);
        end);
    end,

    ResolvePurchaseDispute = function(p329, u330) -- Line: 1100, Name: ResolvePurchaseDispute
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p331, p332) -- Line: 1101
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u330 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p332("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/ResolvePurchaseDispute", u330 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p331, p332);
        end);
    end,

    RevokeAllBansForUser = function(p333, u334) -- Line: 1113, Name: RevokeAllBansForUser
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p335, p336) -- Line: 1114
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u334 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p336("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RevokeAllBansForUser", u334 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p335, p336);
        end);
    end,

    RevokeBans = function(p337, u338) -- Line: 1126, Name: RevokeBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p339, p340) -- Line: 1127
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u338 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p340("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RevokeBans", u338 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p339, p340);
        end);
    end,

    RevokeInventoryItem = function(p341, u342) -- Line: 1139, Name: RevokeInventoryItem
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p343, p344) -- Line: 1140
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u342 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p344("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RevokeInventoryItem", u342 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p343, p344);
        end);
    end,

    RevokeInventoryItems = function(p345, u346) -- Line: 1152, Name: RevokeInventoryItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p347, p348) -- Line: 1153
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u346 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p348("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RevokeInventoryItems", u346 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p347, p348);
        end);
    end,

    RunTask = function(p349, u350) -- Line: 1165, Name: RunTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p351, p352) -- Line: 1166
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u350 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p352("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/RunTask", u350 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p351, p352);
        end);
    end,

    SendAccountRecoveryEmail = function(p353, u354) -- Line: 1180, Name: SendAccountRecoveryEmail
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p355, p356) -- Line: 1181
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u354 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p356("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SendAccountRecoveryEmail", u354 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p355, p356);
        end);
    end,

    SetCatalogItems = function(p357, u358) -- Line: 1193, Name: SetCatalogItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p359, p360) -- Line: 1194
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u358 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p360("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetCatalogItems", u358 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p359, p360);
        end);
    end,

    SetPlayerSecret = function(p361, u362) -- Line: 1206, Name: SetPlayerSecret
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p363, p364) -- Line: 1207
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u362 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p364("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetPlayerSecret", u362 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p363, p364);
        end);
    end,

    SetPublishedRevision = function(p365, u366) -- Line: 1219, Name: SetPublishedRevision
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p367, p368) -- Line: 1220
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u366 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p368("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetPublishedRevision", u366 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p367, p368);
        end);
    end,

    SetPublisherData = function(p369, u370) -- Line: 1232, Name: SetPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p371, p372) -- Line: 1233
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u370 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p372("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetPublisherData", u370 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p371, p372);
        end);
    end,

    SetStoreItems = function(p373, u374) -- Line: 1245, Name: SetStoreItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p375, p376) -- Line: 1246
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u374 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p376("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetStoreItems", u374 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p375, p376);
        end);
    end,

    SetTitleData = function(p377, u378) -- Line: 1258, Name: SetTitleData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p379, p380) -- Line: 1259
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u378 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p380("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetTitleData", u378 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p379, p380);
        end);
    end,

    SetTitleDataAndOverrides = function(p381, u382) -- Line: 1271, Name: SetTitleDataAndOverrides
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p383, p384) -- Line: 1272
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u382 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p384("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetTitleDataAndOverrides", u382 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p383, p384);
        end);
    end,

    SetTitleInternalData = function(p385, u386) -- Line: 1284, Name: SetTitleInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p387, p388) -- Line: 1285
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u386 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p388("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetTitleInternalData", u386 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p387, p388);
        end);
    end,

    SetupPushNotification = function(p389, u390) -- Line: 1299, Name: SetupPushNotification
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p391, p392) -- Line: 1300
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u390 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p392("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SetupPushNotification", u390 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p391, p392);
        end);
    end,

    SubtractUserVirtualCurrency = function(p393, u394) -- Line: 1312, Name: SubtractUserVirtualCurrency
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p395, p396) -- Line: 1313
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u394 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p396("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/SubtractUserVirtualCurrency", u394 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p395, p396);
        end);
    end,

    UpdateBans = function(p397, u398) -- Line: 1325, Name: UpdateBans
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p399, p400) -- Line: 1326
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u398 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p400("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateBans", u398 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p399, p400);
        end);
    end,

    UpdateCatalogItems = function(p401, u402) -- Line: 1338, Name: UpdateCatalogItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p403, p404) -- Line: 1339
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u402 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p404("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateCatalogItems", u402 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p403, p404);
        end);
    end,

    UpdateCloudScript = function(p405, u406) -- Line: 1352, Name: UpdateCloudScript
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p407, p408) -- Line: 1353
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u406 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p408("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateCloudScript", u406 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p407, p408);
        end);
    end,

    UpdateOpenIdConnection = function(p409, u410) -- Line: 1365, Name: UpdateOpenIdConnection
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p411, p412) -- Line: 1366
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u410 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p412("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateOpenIdConnection", u410 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p411, p412);
        end);
    end,

    UpdatePlayerSharedSecret = function(p413, u414) -- Line: 1379, Name: UpdatePlayerSharedSecret
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p415, p416) -- Line: 1380
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u414 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p416("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdatePlayerSharedSecret", u414 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p415, p416);
        end);
    end,

    UpdatePlayerStatisticDefinition = function(p417, u418) -- Line: 1392, Name: UpdatePlayerStatisticDefinition
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p419, p420) -- Line: 1393
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u418 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p420("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdatePlayerStatisticDefinition", u418 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p419, p420);
        end);
    end,

    UpdatePolicy = function(p421, u422) -- Line: 1405, Name: UpdatePolicy
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p423, p424) -- Line: 1406
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u422 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p424("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdatePolicy", u422 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p423, p424);
        end);
    end,

    UpdateRandomResultTables = function(p425, u426) -- Line: 1418, Name: UpdateRandomResultTables
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p427, p428) -- Line: 1419
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u426 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p428("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateRandomResultTables", u426 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p427, p428);
        end);
    end,

    UpdateSegment = function(p429, u430) -- Line: 1431, Name: UpdateSegment
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p431, p432) -- Line: 1432
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u430 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p432("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateSegment", u430 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p431, p432);
        end);
    end,

    UpdateStoreItems = function(p433, u434) -- Line: 1444, Name: UpdateStoreItems
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p435, p436) -- Line: 1445
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u434 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p436("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateStoreItems", u434 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p435, p436);
        end);
    end,

    UpdateTask = function(p437, u438) -- Line: 1457, Name: UpdateTask
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p439, p440) -- Line: 1458
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u438 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p440("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateTask", u438 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p439, p440);
        end);
    end,

    UpdateUserData = function(p441, u442) -- Line: 1470, Name: UpdateUserData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p443, p444) -- Line: 1471
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u442 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p444("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserData", u442 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p443, p444);
        end);
    end,

    UpdateUserInternalData = function(p445, u446) -- Line: 1483, Name: UpdateUserInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p447, p448) -- Line: 1484
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u446 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p448("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserInternalData", u446 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p447, p448);
        end);
    end,

    UpdateUserPublisherData = function(p449, u450) -- Line: 1496, Name: UpdateUserPublisherData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p451, p452) -- Line: 1497
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u450 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p452("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserPublisherData", u450 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p451, p452);
        end);
    end,

    UpdateUserPublisherInternalData = function(p453, u454) -- Line: 1509, Name: UpdateUserPublisherInternalData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p455, p456) -- Line: 1510
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u454 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p456("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserPublisherInternalData", u454 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p455, p456);
        end);
    end,

    UpdateUserPublisherReadOnlyData = function(p457, u458) -- Line: 1522, Name: UpdateUserPublisherReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p459, p460) -- Line: 1523
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u458 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p460("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserPublisherReadOnlyData", u458 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p459, p460);
        end);
    end,

    UpdateUserReadOnlyData = function(p461, u462) -- Line: 1535, Name: UpdateUserReadOnlyData
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p463, p464) -- Line: 1536
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u462 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p464("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserReadOnlyData", u462 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p463, p464);
        end);
    end,

    UpdateUserTitleDisplayName = function(p465, u466) -- Line: 1548, Name: UpdateUserTitleDisplayName
        -- upvalues: Promise (copy), PlayFabSettings (copy), IPlayFabHttps (copy)
        return Promise.new(function(p467, p468) -- Line: 1549
            -- upvalues: PlayFabSettings (ref), IPlayFabHttps (ref), u466 (copy)
            if not (PlayFabSettings.settings.titleId and PlayFabSettings.settings.devSecretKey) then
                p468("Must have PlayFabSettings.settings.devSecretKey set to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Admin/UpdateUserTitleDisplayName", u466 or {}, "X-SecretKey", PlayFabSettings.settings.devSecretKey, p467, p468);
        end);
    end
};