-- Decompiled with Potassium's decompiler.

local Promise = require(script.Parent.Promise);
local IPlayFabHttps = require(script.Parent.IPlayFabHttps);

return {
    settings = require(script.Parent.PlayFabSettings).settings,

    GetGlobalPolicy = function(p1, u2, u3) -- Line: 17, Name: GetGlobalPolicy
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p4, p5) -- Line: 18
            -- upvalues: u2 (copy), IPlayFabHttps (ref), u3 (copy)
            if not u2 then
                p5("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/GetGlobalPolicy", u3 or {}, "X-EntityToken", u2, p4, p5);
        end);
    end,

    GetProfile = function(p6, u7, u8) -- Line: 28, Name: GetProfile
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p9, p10) -- Line: 29
            -- upvalues: u7 (copy), IPlayFabHttps (ref), u8 (copy)
            if not u7 then
                p10("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/GetProfile", u8 or {}, "X-EntityToken", u7, p9, p10);
        end);
    end,

    GetProfiles = function(p11, u12, u13) -- Line: 39, Name: GetProfiles
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p14, p15) -- Line: 40
            -- upvalues: u12 (copy), IPlayFabHttps (ref), u13 (copy)
            if not u12 then
                p15("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/GetProfiles", u13 or {}, "X-EntityToken", u12, p14, p15);
        end);
    end,

    GetTitlePlayersFromMasterPlayerAccountIds = function(p16, u17, u18) -- Line: 50, Name: GetTitlePlayersFromMasterPlayerAccountIds
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p19, p20) -- Line: 51
            -- upvalues: u17 (copy), IPlayFabHttps (ref), u18 (copy)
            if not u17 then
                p20("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/GetTitlePlayersFromMasterPlayerAccountIds", u18 or {}, "X-EntityToken", u17, p19, p20);
        end);
    end,

    SetGlobalPolicy = function(p21, u22, u23) -- Line: 61, Name: SetGlobalPolicy
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p24, p25) -- Line: 62
            -- upvalues: u22 (copy), IPlayFabHttps (ref), u23 (copy)
            if not u22 then
                p25("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/SetGlobalPolicy", u23 or {}, "X-EntityToken", u22, p24, p25);
        end);
    end,

    SetProfileLanguage = function(p26, u27, u28) -- Line: 73, Name: SetProfileLanguage
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p29, p30) -- Line: 74
            -- upvalues: u27 (copy), IPlayFabHttps (ref), u28 (copy)
            if not u27 then
                p30("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/SetProfileLanguage", u28 or {}, "X-EntityToken", u27, p29, p30);
        end);
    end,

    SetProfilePolicy = function(p31, u32, u33) -- Line: 84, Name: SetProfilePolicy
        -- upvalues: Promise (copy), IPlayFabHttps (copy)
        return Promise.new(function(p34, p35) -- Line: 85
            -- upvalues: u32 (copy), IPlayFabHttps (ref), u33 (copy)
            if not u32 then
                p35("Must provide an entityToken to call this method");
            end;

            IPlayFabHttps.MakePlayFabApiCall("/Profile/SetProfilePolicy", u33 or {}, "X-EntityToken", u32, p34, p35);
        end);
    end
};