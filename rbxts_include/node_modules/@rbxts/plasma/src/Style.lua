-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Runtime);
local u1 = Runtime.createContext("Style");
local u2 = {
    bg1 = Color3.fromRGB(31, 31, 31),
    bg2 = Color3.fromRGB(42, 42, 42),
    bg3 = Color3.fromRGB(54, 54, 54),
    mutedTextColor = Color3.fromRGB(147, 147, 147),
    textColor = Color3.fromRGB(255, 255, 255)
};

return {
    get = function() -- Line: 23, Name: get
        -- upvalues: Runtime (copy), u1 (copy), u2 (copy)
        return Runtime.useContext(u1) or u2;
    end,

    set = function(p3) -- Line: 35, Name: set
        -- upvalues: Runtime (copy), u1 (copy), u2 (copy)
        local v4 = Runtime.useContext(u1) or u2;
        local v5 = {};

        for i, v in pairs(v4) do
            v5[i] = v;
        end;

        for i, v in pairs(p3) do
            v5[i] = v;
        end;

        Runtime.provideContext(u1, v5);
    end
};