-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Runtime);

return function() -- Line: 3, Name: createConnect
    -- upvalues: Runtime (copy)
    local u1 = Runtime.useEventCallback();

    return function(p2, p3, p4) -- Line: 6
        -- upvalues: u1 (copy)
        if u1 then
            return u1(p2, p3, p4);
        end;

        return p2[p3]:Connect(p4);
    end;
end;