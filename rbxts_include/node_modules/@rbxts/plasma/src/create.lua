-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Runtime);

return function(p1, p2) -- Line: 56, Name: create
    -- upvalues: Runtime (copy)
    local v3 = p2 or {};
    local v4 = Runtime.useEventCallback();
    local v5 = Instance.new(p1);

    for i, v in pairs(v3) do
        if type(v) == "function" then
            if v4 then
                v4(v5, i, v);
            else
                v5[i]:Connect(v);
            end;
        elseif type(i) == "number" then
            v.Parent = v5;
        elseif type(i) == "table" then
            i[v] = v5;

            if v3.Name == nil then
                v5.Name = v;
            end;
        else
            v5[i] = v;
        end;
    end;

    return v5;
end;