-- Decompiled with Potassium's decompiler.

local RunService = game:GetService("RunService");
local Runtime = require(script.Parent.Parent.Runtime);

return Runtime.widget(function() -- Line: 14
    -- upvalues: Runtime (copy), RunService (copy)
    local u2 = Runtime.useInstance(function(p1) -- Line: 15
        local Frame = Instance.new("Frame");
        Frame.BackgroundTransparency = 1;
        Frame.Size = UDim2.new(0, 100, 0, 100);
        local ImageLabel = Instance.new("ImageLabel");
        ImageLabel.AnchorPoint = Vector2.new(0.5, 0.5);
        ImageLabel.BackgroundTransparency = 1;
        ImageLabel.Image = "rbxassetid://2689141406";
        ImageLabel.Position = UDim2.new(0.5, 0, 0.5, 0);
        ImageLabel.Size = UDim2.new(0, 100, 0, 100);
        ImageLabel.Parent = Frame;
        p1.frame = Frame;

        return Frame;
    end);
    Runtime.useEffect(function() -- Line: 33
        -- upvalues: RunService (ref), u2 (copy)
        local u3 = RunService.RenderStepped:Connect(function() -- Line: 34
            -- upvalues: u2 (ref)
            u2.frame.ImageLabel.Rotation = os.clock() * 100 % 360;
        end);

        return function() -- Line: 38
            -- upvalues: u3 (copy)
            u3:Disconnect();
        end;
    end);
end);