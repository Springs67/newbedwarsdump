-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);

return Runtime.widget(function(p1, p2) -- Line: 24
    -- upvalues: Runtime (copy), create (copy)
    local u3 = p2 or {};
    local u5 = Runtime.useInstance(function(p4) -- Line: 27
        -- upvalues: create (ref)
        return create("Highlight", {
            [p4] = "highlight"
        });
    end);
    u5.highlight.Adornee = p1;
    Runtime.useEffect(function() -- Line: 35
        -- upvalues: u5 (copy), u3 (ref)
        u5.highlight.OutlineColor = u3.outlineColor or Color3.new(1, 1, 1);
        u5.highlight.FillColor = u3.fillColor or Color3.new(1, 0, 0);
    end, u3.fillColor, u3.outlineColor);
    u5.highlight.FillTransparency = u3.fillTransparency or 0.5;
    u5.highlight.OutlineTransparency = u3.outlineTransparency or 0;
    u5.highlight.DepthMode = u3.depthMode or Enum.HighlightDepthMode.AlwaysOnTop;
end);