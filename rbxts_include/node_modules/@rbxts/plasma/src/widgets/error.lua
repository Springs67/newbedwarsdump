-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);

return Runtime.widget(function(p1) -- Line: 4
    -- upvalues: Runtime (copy), create (copy)
    Runtime.useInstance(function(p2) -- Line: 5
        -- upvalues: create (ref)
        return create("Frame", {
            [p2] = "error",
            BackgroundTransparency = 0,
            BackgroundColor3 = Color3.fromRGB(231, 76, 60),
            Name = "Error",
            Size = UDim2.new(0, 100, 0, 75),
            AutomaticSize = Enum.AutomaticSize.XY,
            create("UIPadding", {
                PaddingBottom = UDim.new(0, 20),
                PaddingLeft = UDim.new(0, 20),
                PaddingRight = UDim.new(0, 20),
                PaddingTop = UDim.new(0, 20)
            }),
            create("UIListLayout", {}),
            create("TextLabel", {
                BackgroundTransparency = 1,
                TextSize = 25,
                Text = "⚠️  An Error Occurred",
                Font = Enum.Font.GothamBold,
                TextColor3 = Color3.fromRGB(255, 255, 255),
                AutomaticSize = Enum.AutomaticSize.X,
                Size = UDim2.fromOffset(0, 75)
            }),
            create("TextLabel", {
                Name = "error",
                BackgroundTransparency = 1,
                TextSize = 20,
                LineHeight = 1.2,
                Font = Enum.Font.GothamMedium,
                TextColor3 = Color3.fromRGB(255, 255, 255),
                AutomaticSize = Enum.AutomaticSize.XY,
                Size = UDim2.fromOffset(100, 75),
                TextXAlignment = Enum.TextXAlignment.Left,
                TextYAlignment = Enum.TextYAlignment.Top
            })
        });
    end).error.error.Text = p1;
end);