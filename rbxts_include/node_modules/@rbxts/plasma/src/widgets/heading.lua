-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);
local Style = require(script.Parent.Parent.Style);

return Runtime.widget(function(p1, p2) -- Line: 14
    -- upvalues: Runtime (copy), Style (copy), create (copy)
    local heading = Runtime.useInstance(function(p3) -- Line: 16
        -- upvalues: Style (ref), create (ref)
        local v4 = Style.get();

        return create("TextLabel", {
            [p3] = "heading",
            BackgroundTransparency = 1,
            Font = Enum.Font.GothamBold,
            AutomaticSize = Enum.AutomaticSize.XY,
            TextColor3 = v4.mutedTextColor,
            TextSize = 20,
            RichText = true
        });
    end).heading;
    heading.Text = p1;
    heading.Font = (p2 or {}).font or Enum.Font.GothamBold;
end);