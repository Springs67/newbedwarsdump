-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);
local Style = require(script.Parent.Parent.Style);
local automaticSize = require(script.Parent.Parent.automaticSize);

return Runtime.widget(function(p1) -- Line: 14
    -- upvalues: Runtime (copy), Style (copy), create (copy), automaticSize (copy)
    Runtime.useInstance(function(p2) -- Line: 15
        -- upvalues: Style (ref), create (ref), automaticSize (ref)
        local v3 = Style.get();
        create("TextLabel", {
            [p2] = "label",
            BackgroundTransparency = 1,
            Font = Enum.Font.SourceSans,
            TextColor3 = v3.textColor,
            TextSize = 20,
            RichText = true
        });
        automaticSize(p2.label);

        return p2.label;
    end).label.Text = p1;
end);