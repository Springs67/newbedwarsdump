-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);

return Runtime.widget(function(p1, p2) -- Line: 42
    -- upvalues: Runtime (copy), create (copy)
    local u3 = p2 or {};
    local u4, u5 = Runtime.useState(false);
    local u6, u7 = Runtime.useState(false);
    local checkbox = Runtime.useInstance(function(p8) -- Line: 48
        -- upvalues: create (ref), u7 (copy), u5 (copy)
        return create("Frame", {
            [p8] = "checkbox",
            BackgroundTransparency = 1,
            Name = "Checkbox",
            Size = UDim2.new(0, 30, 0, 30),
            AutomaticSize = Enum.AutomaticSize.X,
            create("TextButton", {
                BackgroundColor3 = Color3.fromRGB(54, 54, 54),
                BorderSizePixel = 0,
                Font = Enum.Font.SourceSansBold,
                Size = UDim2.new(0, 30, 0, 30),
                TextColor3 = Color3.fromRGB(153, 153, 153),
                TextSize = 24,
                create("UICorner", {
                    CornerRadius = UDim.new(0, 8)
                }),

                Activated = function() -- Line: 68, Name: Activated
                    -- upvalues: u7 (ref), u5 (ref)
                    u7(true);
                    u5(function(p9) -- Line: 70
                        return not p9;
                    end);
                end
            }),
            create("TextLabel", {
                TextSize = 18,
                RichText = true,
                BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                Font = Enum.Font.GothamMedium,
                TextColor3 = Color3.fromRGB(203, 203, 203),
                AutomaticSize = Enum.AutomaticSize.X
            }),
            create("UIListLayout", {
                FillDirection = Enum.FillDirection.Horizontal,
                Padding = UDim.new(0, 10),
                SortOrder = Enum.SortOrder.LayoutOrder,
                VerticalAlignment = Enum.VerticalAlignment.Center
            })
        });
    end).checkbox;
    checkbox.TextLabel.Text = p1;
    checkbox.TextButton.AutoButtonColor = not u3.disabled;
    Runtime.useEffect(function() -- Line: 101
        -- upvalues: u3 (ref), u4 (copy), checkbox (copy)
        local v10;

        if u3.checked == nil then
            v10 = u4;
        else
            v10 = u3.checked;
        end;

        checkbox.TextButton.Text = v10 and "✓" or "";
    end, u3.checked, u4);
    Runtime.useEffect(function() -- Line: 112
        -- upvalues: checkbox (copy), u3 (ref)
        checkbox.TextButton.BackgroundColor3 = u3.disabled and Color3.fromRGB(112, 112, 112) or Color3.fromRGB(54, 54, 54);
    end, u3.disabled);

    return {
        checked = function() -- Line: 118, Name: checked
            -- upvalues: u3 (ref), u4 (copy)
            return (u3.checked or u4) and true or false;
        end,

        clicked = function() -- Line: 125, Name: clicked
            -- upvalues: u6 (copy), u7 (copy)
            if not u6 then
                return false;
            end;

            u7(false);

            return true;
        end
    };
end);