-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local Style = require(script.Parent.Parent.Style);
local create = require(script.Parent.Parent.create);

return Runtime.widget(function(p1) -- Line: 29
    -- upvalues: Runtime (copy), Style (copy), create (copy)
    local u2, u3 = Runtime.useState(false);
    Runtime.useInstance(function(p4) -- Line: 31
        -- upvalues: Style (ref), create (ref), u3 (copy)
        local v5 = Style.get();

        return create("TextButton", {
            [p4] = "button",
            BackgroundColor3 = v5.bg3,
            BorderSizePixel = 0,
            Font = Enum.Font.SourceSans,
            Size = UDim2.new(0, 100, 0, 40),
            TextColor3 = v5.textColor,
            AutomaticSize = Enum.AutomaticSize.X,
            TextSize = 21,
            create("UIPadding", {
                PaddingLeft = UDim.new(0, 10),
                PaddingRight = UDim.new(0, 10)
            }),
            create("UICorner"),

            Activated = function() -- Line: 51, Name: Activated
                -- upvalues: u3 (ref)
                u3(true);
            end
        });
    end).button.Text = p1;

    return {
        clicked = function() -- Line: 62, Name: clicked
            -- upvalues: u2 (copy), u3 (copy)
            if not u2 then
                return false;
            end;

            u3(false);

            return true;
        end
    };
end);