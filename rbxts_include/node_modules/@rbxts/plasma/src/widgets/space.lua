-- Decompiled with Potassium's decompiler.

local Runtime = require(script.Parent.Parent.Runtime);
local create = require(script.Parent.Parent.create);

return Runtime.widget(function(u1) -- Line: 12
    -- upvalues: Runtime (copy), create (copy)
    local u3 = Runtime.useInstance(function(p2) -- Line: 13
        -- upvalues: create (ref)
        return create("Frame", {
            [p2] = "space",
            BackgroundTransparency = 1
        });
    end);
    Runtime.useEffect(function() -- Line: 20
        -- upvalues: u3 (copy), u1 (copy)
        u3.space.Size = UDim2.new(0, u1, 0, u1);
    end, u1);
end);