-- Decompiled with Potassium's decompiler.

local CollectionService = game:GetService("CollectionService");
local automaticSize = require(script.Parent.automaticSize);

return function() -- Line: 13, Name: hydrateAutomaticSize
    -- upvalues: CollectionService (copy), automaticSize (copy)
    for _, v in CollectionService:GetTagged("PlasmaAutomaticSize") do
        automaticSize(v);
    end;

    return CollectionService:GetInstanceAddedSignal("PlasmaAutomaticSize"):Connect(function(p1) -- Line: 18
        -- upvalues: automaticSize (ref)
        task.defer(automaticSize, p1);
    end);
end;