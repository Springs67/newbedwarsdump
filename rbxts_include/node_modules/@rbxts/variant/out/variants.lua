-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local identityFunc = v1.import(script, script.Parent, "identityFunc").identityFunc;
local ObjectUtils = v1.import(script, script.Parent, "ObjectUtils").ObjectUtils;
local u2 = v1.import(script, script.Parent, "variantFactory").variantFactory("type");

return {
    default = function(u3) -- Line: 7, Name: variantModule
        -- upvalues: ObjectUtils (copy), u2 (copy), identityFunc (copy)
        local v4 = ObjectUtils.keys(u3);

        local function v9(p5, p6) -- Line: 9
            -- upvalues: u3 (copy), u2 (ref), identityFunc (ref)
            local v7 = {};

            for i, v in pairs(p5) do
                v7[i] = v;
            end;

            local v8;

            if type(u3[p6]) == "function" then
                v8 = u3[p6];
            else
                v8 = identityFunc;
            end;

            v7[p6] = u2(p6, v8);

            return v7;
        end;

        local v10 = {};

        for i = 1, #v4 do
            v10 = v9(v10, v4[i], i - 1, v4);
        end;

        return v10;
    end,

    variant = u2
};