-- Decompiled with Potassium's decompiler.

local identityFunc = _G[script].import(script, script.Parent, "identityFunc").identityFunc;

return {
    variantFactory = function(u1) -- Line: 4, Name: variantFactory
        -- upvalues: identityFunc (copy)
        return function(u2, u3) -- Line: 18, Name: variantFunc
            -- upvalues: u1 (copy), identityFunc (ref)
            return setmetatable({
                key = u1,
                type = u2
            }, {
                __call = function(p4, ...) -- Line: 23, Name: __call
                    -- upvalues: u3 (copy), identityFunc (ref), u1 (ref), u2 (copy)
                    local v5 = (u3 or identityFunc)(unpack({ ... }));
                    local v6 = {
                        [u1] = u2
                    };

                    if type(v5) == "table" then
                        for i, v in pairs(v5) do
                            v6[i] = v;
                        end;
                    end;

                    return v6;
                end
            });
        end;
    end
};