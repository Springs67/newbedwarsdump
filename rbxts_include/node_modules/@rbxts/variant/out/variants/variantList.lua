-- Decompiled with Potassium's decompiler.

local variant = _G[script].import(script, script.Parent).variant;

return {
    variantList = function(p1) -- Line: 25, Name: variantList
        -- upvalues: variant (copy)
        local function _(p2) -- Line: 26
            -- upvalues: variant (ref)
            if type(p2) == "string" then
                return variant(p2);
            end;

            return p2;
        end;

        local v3 = table.create(#p1);

        for i, v in ipairs(p1) do
            local _ = i - 1;

            if type(v) == "string" then
                local v = variant(v);
            end;

            v3[i] = v;
        end;

        local function _(p4, p5) -- Line: 39
            local v6 = {};

            for i, v in pairs(p4) do
                v6[i] = v;
            end;

            v6[p5.type] = p5;

            return v6;
        end;

        local v7 = {};

        for i = 1, #v3 do
            local v8 = v3[i];
            local _ = i - 1;
            v7 = {};

            for i2, v in pairs(v7) do
                v7[i2] = v;
            end;

            v7[v8.type] = v8;
        end;

        return v7;
    end
};