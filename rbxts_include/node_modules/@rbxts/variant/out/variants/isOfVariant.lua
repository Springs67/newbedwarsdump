-- Decompiled with Potassium's decompiler.

local ObjectUtils = _G[script].import(script, script.Parent.Parent, "ObjectUtils").ObjectUtils;

local function outputTypes(u1) -- Line: 4
    -- upvalues: ObjectUtils (copy)
    local v2 = ObjectUtils.keys(u1);

    local function _(p3) -- Line: 6
        -- upvalues: u1 (copy)
        return u1[p3].type;
    end;

    local v4 = table.create(#v2);

    for i, v in ipairs(v2) do
        local _ = i - 1;
        v4[i] = u1[v].type;
    end;

    return v4;
end;

return {
    outputTypes = outputTypes,

    isOfVariant = function(u5, p6, u7) -- Line: 17, Name: isOfVariant
        -- upvalues: outputTypes (copy)
        local v8 = u5 ~= nil;

        if v8 then
            local v9 = outputTypes(p6);

            local function _(p10) -- Line: 21
                -- upvalues: u5 (copy), u7 (copy)
                local v11 = u7;

                return p10 == u5[v11 == nil and "type" or v11];
            end;

            v8 = false;

            for i, v in ipairs(v9) do
                local _ = i - 1;

                if v == u5[u7 == nil and "type" or u7] then
                    v8 = true;
                    break;
                end;
            end;
        end;

        return v8;
    end
};