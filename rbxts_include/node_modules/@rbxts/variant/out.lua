-- Decompiled with Potassium's decompiler.

local v1 = _G[script];
local fields = v1.import(script, script, "fields").fields;
local v2 = v1.import(script, script, "lookup");
local lookup = v2.lookup;
local partialLookup = v2.partialLookup;
local match = v1.import(script, script, "match").match;
local payload = v1.import(script, script, "payload").payload;
local variantFactory = v1.import(script, script, "variantFactory").variantFactory;
local v3 = v1.import(script, script, "variants");
local default = v3.default;
local variant = v3.variant;
local cast = v1.import(script, script, "variants", "cast").cast;
local v4 = v1.import(script, script, "variants", "isOfVariant");
local isOfVariant = v4.isOfVariant;
local outputTypes = v4.outputTypes;
local keymap = v1.import(script, script, "variants", "keymap").keymap;
local keys = v1.import(script, script, "variants", "keys").keys;

return {
    fields = fields,
    lookup = lookup,
    partialLookup = partialLookup,
    match = match,
    variantFactory = variantFactory,
    variantModule = default,
    variant = variant,
    cast = cast,
    isOfVariant = isOfVariant,
    outputTypes = outputTypes,
    narrow = v1.import(script, script, "variants", "narrow").narrow,
    keys = keys,
    keymap = keymap,
    payload = payload,
    variantList = v1.import(script, script, "variants", "variantList").variantList,
    default = default
};