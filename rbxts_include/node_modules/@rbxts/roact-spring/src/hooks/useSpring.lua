-- Decompiled with Potassium's decompiler.

local React = require(script.Parent.Parent.React);
local useSprings = require(script.Parent.useSprings);

return React.reconcile and function(p1, p2, p3) -- Line: 20
    -- upvalues: useSprings (copy)
    local v4 = type(p2) == "function";
    local v5, v6 = useSprings(p1, 1, not v4 and { p2 } or p2, v4 and not p3 and {} or p3);

    return v5[1], v6;
end or function(p7, p8) -- Line: 7
    -- upvalues: useSprings (copy)
    local v9 = type(p7) == "function";
    local v10, v11 = useSprings(1, not v9 and { p7 } or p7, v9 and not p8 and {} or p8);

    return v10[1], v11;
end;