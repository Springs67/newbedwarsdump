-- Decompiled with Potassium's decompiler.

local v1 = {};
local u2 = 0;

function v1.getGlobalId() -- Line: 12
    -- upvalues: u2 (ref)
    u2 = u2 + 1;

    return u2;
end;

local _ = Vector3.new;
local new = CFrame.new;
local sqrt = math.sqrt;
local Dot = (Vector3.new(0, 0, 0)).Dot;
local Cross = (Vector3.new(0, 0, 0)).Cross;

function v1.makeOrientation(p3, p4) -- Line: 43
    -- upvalues: Dot (copy), new (copy), Cross (copy), sqrt (copy)
    local unit = p4.unit;
    local v5 = Dot(Vector3.new(1, 0, 0), unit);

    if v5 > 0.99999 then
        return new(p3, p3 - Vector3.new(0, 0, 1));
    end;

    if v5 < -0.99999 then
        return new(p3, p3 + Vector3.new(0, 0, 1));
    end;

    local v6 = Cross(Vector3.new(1, 0, 0), unit);
    local v7 = 1 + v5;
    local v8 = sqrt(v6.magnitude ^ 2 + v7 * v7);
    local v9 = v6 / v8;

    return new(p3.x, p3.y, p3.z, v9.x, v9.y, v9.z, v7 / v8);
end;

return v1;